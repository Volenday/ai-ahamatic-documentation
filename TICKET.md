```markdown
# AI ahaMatic — Ticket Tracker

## Legend

| Status         | Meaning                              |
| -------------- | ------------------------------------ |
| ✅ Done        | Output produced, reviewed, and saved |
| 🔄 In Progress | Currently being executed             |
| ⏳ Next        | Ready to execute                     |
| ⬜ Pending     | Not yet started                      |

---

## Foundation Sprint (T1–T36)

| #   | Ticket Title                           | Status  | Output File                                          | Depends On     |
| --- | -------------------------------------- | ------- | ---------------------------------------------------- | -------------- |
| T1  | Context Document Map                   | ✅ Done | `docs/spec/context-document-map.md`                   | —              |
| T2  | Vision and Charter                     | ✅ Done | `docs/spec/01-business-and-ux/01-vision-and-charter.md`                     | T1             |
| T3  | Product Requirements Document          | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`                                    | T1, T2         |
| T4  | Platform Capability Model              | ✅ Done | `docs/spec/01-business-and-ux/03-platform-capability-model.md`              | T1, T2         |
| T5  | Personas and Roles                     | ✅ Done | `docs/spec/01-business-and-ux/04-personas-and-roles.md`                     | T1, T2         |
| T6  | User Journeys                          | ✅ Done | `docs/spec/01-business-and-ux/05-user-journeys.md`                          | T1, T4, T5     |
| T7  | Value Proposition and Success Metrics  | ✅ Done | `docs/spec/01-business-and-ux/06-value-proposition-and-success-metrics.md`  | T1, T2, T3     |
| T8  | System Invariants                      | ✅ Done | `docs/spec/02-governance-and-security/01-system-invariants.md`                      | T1, T2, T3     |
| T9  | Security Policy                        | ✅ Done | `docs/spec/02-governance-and-security/02-security-policy.md`                        | T1, T8         |
| T10 | Access Control and Tenancy Model       | ✅ Done | `docs/spec/02-governance-and-security/03-access-control-and-tenancy-model.md`       | T1, T5, T8     |
| T11 | Auth and Identity Spec                 | ✅ Done | `docs/spec/02-governance-and-security/04-auth-and-identity-spec.md`                 | T1, T10        |
| T12 | Compliance and Data Residency          | ✅ Done | `docs/spec/02-governance-and-security/05-compliance-and-data-residency.md`          | T1, T8         |
| T13 | Data Governance and Privacy            | ✅ Done | `docs/spec/02-governance-and-security/06-data-governance-and-privacy.md`            | T1, T8, T12    |
| T14 | Audit and Traceability                 | ✅ Done | `docs/spec/02-governance-and-security/07-audit-and-traceability.md`                 | T1, T8, T13    |
| T15 | Legal and Licensing Constraints        | ✅ Done | `docs/spec/02-governance-and-security/08-legal-and-licensing-constraints.md`        | T1             |
| T16 | Architecture Overview                  | ✅ Done | `docs/spec/03-software-and-architecture/01-architecture-overview.md`                  | T1, T2, T3, T4 |
| T17 | Domain Glossary                        | ✅ Done | `docs/spec/03-software-and-architecture/02-domain-glossary.md`                        | T1, T16        |
| T18 | Data Model and Entity Spec             | ✅ Done | `docs/spec/03-software-and-architecture/03-data-model-and-entity-spec.md`             | T1, T16, T17   |
| T19 | API Contract Spec                      | ✅ Done | `docs/spec/03-software-and-architecture/04-api-contract-spec.md`                      | T1, T16, T17   |
| T20 | Integration and Extensibility Spec     | ✅ Done | `docs/spec/03-software-and-architecture/05-integration-and-extensibility-spec.md`     | T1, T16, T19   |
| T21 | Non-Functional Requirements            | ✅ Done | `docs/spec/03-software-and-architecture/06-non-functional-requirements.md`            | T1, T3, T16    |
| T22 | Coding Standards and Patterns          | ✅ Done | `docs/spec/03-software-and-architecture/07-coding-standards-and-patterns.md`          | T1, T16, T17   |
| T23 | Environment and Config Spec            | ✅ Done | `docs/spec/04-devops-and-cloud-infra/01-environment-and-config-spec.md`            | T1, T16        |
| T24 | CI/CD Pipeline Spec                    | ✅ Done | `docs/spec/04-devops-and-cloud-infra/02-ci-cd-pipeline-spec.md`                    | T1, T23        |
| T25 | Testing and Quality Gates              | ✅ Done | `docs/spec/04-devops-and-cloud-infra/03-testing-and-quality-gates.md`              | T1, T21, T24   |
| T26 | Observability and Monitoring           | ✅ Done | `docs/spec/04-devops-and-cloud-infra/04-observability-and-monitoring.md`           | T1, T21, T23   |
| T27 | Release and Rollback Protocol          | ✅ Done | `docs/spec/04-devops-and-cloud-infra/05-release-and-rollback-protocol.md`          | T1, T24, T25   |
| T28 | Incident Response and Recovery         | ✅ Done | `docs/spec/04-devops-and-cloud-infra/06-incident-response-and-recovery.md`         | T1, T26, T27   |
| T29 | Agent Operating Charter                | ✅ Done | `docs/spec/05-meta-operations/01-agent-operating-charter.md`                | T1, T2, T3     |
| T30 | Agent Loop Constraints                 | ✅ Done | `docs/spec/05-meta-operations/02-agent-loop-constraints.md`                 | T1, T29        |
| T31 | Token and Compute Budget               | ✅ Done | `docs/spec/05-meta-operations/03-token-and-compute-budget.md`               | T1, T29        |
| T32 | Human-in-the-Loop Protocol             | ✅ Done | `docs/spec/05-meta-operations/04-human-in-the-loop-protocol.md`             | T1, T29        |
| T33 | Prompt and Context Management          | ✅ Done | `docs/spec/05-meta-operations/05-prompt-and-context-management.md`          | T1, T29        |
| T34 | Agent State and Memory Spec            | ✅ Done | `docs/spec/05-meta-operations/06-agent-state-and-memory-spec.md`            | T1, T29        |
| T35 | Self-Correction and Fallback Protocol  | ✅ Done | `docs/spec/05-meta-operations/07-self-correction-and-fallback-protocol.md`  | T1, T29, T34   |
| T36 | Change Management and Evolution Policy | ✅ Done | `docs/spec/05-meta-operations/08-change-management-and-evolution-policy.md` | T1, T29, T35   |

---

## Map Update

| #         | Ticket Title                         | Status  | Output File                         | Depends On |
| --------- | ------------------------------------ | ------- | ----------------------------------- | ---------- |
| T1-Update | Context Document Map — LCAP Revision | ✅ Done | `docs/spec/context-document-map.md` | T1         |

> **Note:** T1-Update revised the map for LCAP identity, the citizen-developer exclusion, and capabilities C-18 through C-22 (lean reconciliation; C-18–C-22 recorded as capability entries, only two new documents added).

---

## Improvement Roadmap (T37–T47) — ✅ Complete

| #   | Ticket Title                                          | Status  | Output File                                                | Type   |
| --- | ----------------------------------------------------- | ------- | ---------------------------------------------------------- | ------ |
| T37 | LCAP Identity and LCNC Categorization                 | ✅ Done | `docs/spec/01-business-and-ux/01-vision-and-charter.md`                          | Update |
| T38 | Document Citizen Developer as Strategic Exclusion     | ✅ Done | `docs/spec/01-business-and-ux/01-vision-and-charter.md`, `docs/spec/01-business-and-ux/04-personas-and-roles.md` | Update |
| T39 | Add Workflow and Process Automation Capability (C-18) | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md` | Update |
| T40 | Add AI-Assisted Builder Tooling Capability (C-19)     | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md` | Update |
| T41 | Add Mobile Application Capability (C-20)               | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md` | Update |
| T42 | Add Builder-Facing Version Control (C-21)             | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md` | Update |
| T43 | Add Multi-Language Code Export (C-22)                 | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md` | Update |
| T44 | Update Value Proposition with Market-Driven Benchmarks | ✅ Done | `docs/spec/01-business-and-ux/06-value-proposition-and-success-metrics.md`      | Update |
| T45 | Add Security Certification Roadmap                    | ✅ Done | `docs/spec/02-governance-and-security/02-security-policy.md`                             | Update |
| T46 | Competitive Landscape Reference Document              | ✅ Done | `docs/spec/01-business-and-ux/07-competitive-landscape.md`                      | Create |
| T47 | Gartner Industry Standards Reference Document         | ✅ Done | `docs/spec/01-business-and-ux/08-industry-standards-and-benchmarks.md`          | Create |

> **C-22 framing (T43):** Multi-Language Code Export is the export/generation of a built application's code in multiple **programming languages** (target languages TBD, not authorized). It has nothing to do with human-language UI localization. The agent must not conflate the two.

---

## Post-Roadmap Consistency Work (T48–T52) — ✅ Complete

| #   | Ticket Title                                          | Status  | Output File                                                        | Type   |
| --- | ---------------------------------------------------- | ------- | ----------------------------------------------------------------- | ------ |
| T48 | Propagate new capabilities/terms into the glossary   | ✅ Done | `docs/spec/03-software-and-architecture/02-domain-glossary.md`                                     | Update |
| T49 | Add user journeys for the new capabilities            | ✅ Done | `docs/spec/01-business-and-ux/05-user-journeys.md`                                       | Update |
| T50 | Add Builder-Facing Environment Management (C-23)      | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md`        | Update |
| T51 | Record citizen-developer model out-of-scope in PRD   | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md` (landed with T50)                              | Update |
| T52 | Propagate C-23 and reconcile persona capability maps | ✅ Done | `docs/spec/03-software-and-architecture/02-domain-glossary.md`, `01-business-and-ux/05-user-journeys.md`, `01-business-and-ux/04-personas-and-roles.md` | Update |

---

## Post-Review Spec Updates (T53–T62) — from the gap-review decisions — ✅ Complete

See `OPEN-GAPS-FOR-REVIEW.md`. All specification-phase; they run before the design phase.

| #   | Ticket Title                                              | Status     | Output File                                                       | Gap |
| --- | -------------------------------------------------------- | ---------- | ---------------------------------------------------------------- | --- |
| T53 | Extend security threat model for AI-assisted tooling      | ✅ Done    | `docs/spec/02-governance-and-security/02-security-policy.md`                                    | G-1 |
| T54 | Add interface coverage for C-19, C-20, C-21               | ✅ Done    | `docs/spec/03-software-and-architecture/04-api-contract-spec.md`, `docs/spec/03-software-and-architecture/05-integration-and-extensibility-spec.md` | G-2 |
| T55 | Formalize Future / Not-Yet-Authorized Capabilities category | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/context-document-map.md`           | G-6 |
| T56 | Add Cross-System Data Layer (C-24)                        | ✅ Done    | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md`      | G-3 |
| T57 | Add Connector Marketplace (C-25)                         | ✅ Done    | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md`      | G-3 |
| T58 | Add Runtime AI Automation as future capability (C-26)    | ✅ Done    | `docs/spec/01-business-and-ux/02-prd.md`, `docs/spec/01-business-and-ux/03-platform-capability-model.md`      | G-3 |
| T59 | Propagate C-24–C-26 + re-sync capability count           | ✅ Done    | `docs/spec/03-software-and-architecture/02-domain-glossary.md`, `01-business-and-ux/04-personas-and-roles.md`, `01-business-and-ux/05-user-journeys.md`, `context-document-map.md`, `01-business-and-ux/03-platform-capability-model.md`, library-wide | G-3 |
| T60 | Update competitive landscape with frontier-gap resolutions | ✅ Done    | `docs/spec/01-business-and-ux/07-competitive-landscape.md`                            | G-3 |
| T61 | Triage industry-benchmark remediation list               | ✅ Done    | `docs/spec/01-business-and-ux/08-industry-standards-and-benchmarks.md`, `BACKLOG.md`   | G-4 |
| T62 | Documentation-consistency mechanism                      | ✅ Done    | `.claude/skills/ai-aha-consistency-check/SKILL.md`, `docs/spec/05-meta-operations/08-change-management-and-evolution-policy.md`, `PROCESS.md` | G-5 |

---

## Final Spec Consistency (T63–T64) — ✅ Complete

Closing the specification phase before H1. T63 emerged from `BACKLOG.md` §4 / `PROCESS.md` §7; T64 from the `ai-aha-consistency-check` run after T62 (findings 1–2).

| #   | Ticket Title                                          | Status  | Output File                                          | Source                  |
| --- | ----------------------------------------------------- | ------- | ---------------------------------------------------- | ----------------------- |
| T63 | Extend directional-source caveat to value proposition | ✅ Done | `docs/spec/01-business-and-ux/06-value-proposition-and-success-metrics.md` | BACKLOG §4 / PROCESS §7 |
| T64 | Fix stale "recently added" framing + extend interface coverage to C-24/C-25 (assess C-23) | ✅ Done | `docs/spec/03-software-and-architecture/04-api-contract-spec.md`, `docs/spec/03-software-and-architecture/05-integration-and-extensibility-spec.md` | consistency-check findings 1–2 |

> **Consistency-check finding 3 (resolved — lead decision):** The Precedence→Binding-Rules closing convention is formally scoped to the rule-bearing governance/design-class docs; the 8 Strategy-phase / reference docs are left as-is (no retrofit). Follow-up: scope the `ai-aha-consistency-check` closing-section check to that same class so it stops flagging them.

---

## Design Phase (H-series)

Specification phase delivered **T1–T64**. The design phase produces the "how" library in `docs/design/` under `ai-aha-design-doc` / `ai-aha-design-review`.

> **⚠ The specification is NO LONGER FROZEN — lead decision, 2026-07-30.** The freeze that governed T1–T64 has been lifted, and the lead has stated the spec is **expected to keep changing**. Two consequences: (a) a design ticket may now surface a spec change rather than only flagging it, but a spec change still goes through a **spec-phase ticket** under `ai-aha-spec-doc` — a design ticket never edits `docs/spec/` inline; (b) `PROCESS.md` §1 and `CLAUDE.md` both still assert the spec is frozen during the design phase and **need updating to match** (tracked below). Until they are, this note is the authority on the freeze status.

| #  | Ticket Title                              | Status  | Output File                                  |
| -- | ------------------------------------------ | ------- | --------------------------------------------- |
| H1 | Implementation Document Map                | ✅ Done | `docs/design/implementation-document-map.md`  |
| H2 | Technology Stack & Architecture Decisions  | ✅ Done | `docs/design/01-design-foundation/01-technology-stack-design.md`      |
| H2a | Cost-to-Reverse Annotation & Dependency Correction | ✅ Done | `docs/design/implementation-document-map.md` |
| H3 | Amendment Ticket — Sync-Posture ADR-011, ADR-004/006 Amendments, Status Retrofit, Consistency-Check Remediation | ✅ Done | `docs/design/01-design-foundation/01-technology-stack-design.md` |
| H4 | Schedule the Platform's Own Data Model — `02-platform-data-model-design.md` Map Entry | ✅ Done | `docs/design/implementation-document-map.md` |
| H5 | Platform Data Model Design — the Platform's Own Persistent Schema | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md` |
| H7 | Terminology Normalization — "Tenant" as the Canonical Structural Term (`DECISIONS.md` D-17), and V1.0 Scale-Assumption Revision | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md`, `docs/design/01-design-foundation/01-technology-stack-design.md`, `docs/design/implementation-document-map.md`, `ADR-REGISTER.md` |
| H6 | ADR-001/008/009 Re-Evaluation Under All Thirteen Criteria — Default Stack Plus Reusable Per-Client Criteria (`DECISIONS.md` D-15); ADR-012 (Server-Side Caching Deferral) and ADR-013 (AI-to-AI Protocol — MCP) | ✅ Done | `docs/design/01-design-foundation/01-technology-stack-design.md` |
| H8 | Architecture Realization Design — Concrete Component Structure, Dependency-Direction Enforcement Mechanism, Three-Layer Separation, Deployment Shape; ADR-014 (Enforcement Mechanism), ADR-015 (Codebase Topology — ADR-002's Parked Question Answered), ADR-016 (Extension Authorship Under No-Code, `DECISIONS.md` D-09) | ✅ Done | `docs/design/01-design-foundation/03-architecture-realization-design.md` |
| H9 | Invariant Enforcement Design — Each of INV-01–INV-09 Realized as a Verifiable Blocking Check (Layer 2, first document of that layer). **Verified 2026-08-03:** all nine invariants covered; §8 boundaries hold (tenant-isolation named "sharpest boundary" explicitly); no ADR recorded — the map's mechanism-reversal grading confirmed, not overridden; **ADR-018 remains unconsumed** for the next document with a genuine decision | ✅ Done | `docs/design/02-identity-access-security/01-invariant-enforcement-design.md` |
| H9a | V1.0 Sizing Amendment (D-26) — ~100 applications total, lead-confirmed, replacing the ≤50/10×5 team assumption in `02-platform-data-model-design.md` §11/§14 and `01-technology-stack-design.md` §18.6. **Verified 2026-08-03:** independent arithmetic check (not merely cited) confirmed "theoretical, not pressing" still holds at ~100; third stale occurrence (§14) found by the Executor's own sweep, not named in the ticket; **one residual inconsistency caught afterward** — the registry-indirection headroom paragraph still read "from under 50" two paragraphs after §11's ~100, corrected post-handoff | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md`, `docs/design/01-design-foundation/01-technology-stack-design.md` |

> **H9 is the first step toward the builder-entity data model.** The "Design-phase work identified — not yet sequenced" table below still names "the data model" as the lead's next deliverable and says no document is positioned to deliver it — that note is **stale**: H4/H5 already delivered the platform's own schema, and H6 already closed the ADR-001/008/009 gate it also cites. What remains under "the data model" is the **builder-entity schema**, `02-data-model-and-entity-design.md` (Layer 3) — but per `implementation-document-map.md`'s own layer rule ("no design document in Layers 3–6 may be produced as though [Layer 2] did not apply to it"), that document cannot be written before Layer 2 exists. Its fine-grained dependency chain is `01-invariant-enforcement-design.md` → `02-tenant-isolation-and-access-control-design.md` → `03-authentication-and-identity-design.md` → `01-application-construction-design.md` → `02-data-model-and-entity-design.md`. H9 starts that chain at its only currently-unblocked link (its sole dependency, `03-architecture-realization-design.md`, is done). The stale note itself is left as-is per the lead's/user's direction (2026-08-04) — corrected here rather than rewritten there.

> **⚠ SUPERSEDED 2026-07-30 — read the "Lead Decisions" section below instead.** Five ADRs were approved at the 2026-07-30 standup (004, 005, 006-in-part, 007, 010) and three were deferred pending the data model (001, 008, 009). The pause described below no longer reflects reality; the note is retained only as the record of what the gate was. **Approval is not yet recorded in `DECISIONS.md`, so nothing is binding even now.**
>
> **⏸ HISTORICAL — the original pause after H2, pending lead approval of ADR-001.** H2 produced a provisional recommendation (ADR-001, recorded inline in `01-technology-stack-design.md`'s Design Decision Records section): Node.js/TypeScript across server, web (Next.js/React), and mobile-delivery runtime (React Native) — single-language, chosen over a Flutter-unified alternative on AI/LLM tooling-ecosystem-fit grounds. **This recommendation has no effect until the project lead approves it and it is recorded in `DECISIONS.md`.** No downstream document may treat ADR-001 as final until then. Five documents remain gated on this approval: `03-architecture-realization-design.md`, `04-scalability-availability-and-performance-design.md`, `09-licensing-and-dependency-compliance-design.md`, `08-coding-standards-and-patterns-design.md`, `01-environment-and-configuration-design.md`. Per the lead's sequencing override, `03-architecture-realization-design.md` is H3 once approval lands (it now follows the stack decision rather than preceding it). Datastore selection was explicitly scoped out of H2 and remains a follow-up ticket. Do not generate H3 or any gated ticket before lead approval is confirmed and logged in `DECISIONS.md`.

---

## Design-Phase Decision Queue — Ordered by Cost to Reverse

**Source:** `references/research/stack-decision-brief.md` — a decision brief supplied by the project lead (2026-07-28), read together with the 2026-07-28 team standup outcomes. (The brief was originally supplied as a PDF; it was replaced by this Markdown version on 2026-07-29, and the full text was read for the first time on 2026-07-30.)

**How to treat the source.** The brief is an **advisory, secondary input — directional, not authoritative**, and it is not a specification. Its stated premise is *"enterprise web software (HRIS, finance, inventory) plus mobile"* — the very domain framing `CLAUDE.md` forbids for AI ahaMatic, which is a generic, multi-purpose LCAP that *builds* software. **Every item below must therefore be translated through the primitive/artifact line of `docs/spec/01-business-and-ux/03-platform-capability-model.md` §3 before adoption:** a decision the brief states for a *domain application* is, in ahaMatic, either (a) a platform-core decision, or (b) builder-defined content the platform must support *generically*. Adopting a domain-shaped default into the platform core would breach **INV-05 (generality preservation)** and the builder/built line (**INV-06**). Whether the brief was aimed at the platform or at the applications built on it **was answered on 2026-07-30: it was aimed at the platform itself.** That makes the domain framing above a *confirmed defect in the input* rather than an ambiguity — the brief answered a different question than the one it was asked, so its layer defaults must be **re-derived, never adopted**, while its *method* transfers intact. Consequence table in `BACKLOG.md` §3.

**Why this ordering.** The brief's central argument is that decisions differ enormously in **cost to reverse**, and that stack selection — which attracts most of the debate — is the *cheapest* of the five and roughly 20% of the outcome. The queue below is therefore ordered by reversal cost, not by the order the items were raised. This **re-sequences** the three open standup items (marked ↩️) rather than replacing them.

| Layer | Reversal Cost | Decision | Status |
| --- | --- | --- | --- |
| 1 — Data model + database | **Brutal** | **Multi-tenancy partitioning shape** — shared schema + `tenant_id` + row-level security vs. schema-per-tenant vs. database-per-tenant. Realizes **C-01**, **INV-01**, gate **G-1**. | ✅ **Shape decided** — ADR-004 recommends **schema-per-tenant**; RLS excluded on portability grounds. Isolation *mechanics* remain with `02-tenant-isolation-and-access-control-design.md` (gated), which may overturn the shape on a scalability finding. |
| 1 | Brutal | **Target SQL engine** — Postgres / SQL Server / MySQL-MariaDB. A target is required even though the platform stays engine-agnostic, mirroring how GCP is a default-but-agnostic cloud target. | ✅ **Decided** — ADR-004: **PostgreSQL** target; MySQL/MariaDB and SQL Server supported. |
| 1 | Brutal | **ODM/ORM abstraction layer** securing portability across SQL engines; no dependence on engine-native, non-portable features. | ✅ **Decided** — ADR-004: a **typed query builder (Kysely)**, not an ORM. Builders define entities at runtime (C-05), which disqualifies ahead-of-time-generated clients such as Prisma. |
| 1 | Brutal | **Key strategy** — UUIDv7 vs. autoincrement (client-generatable identifiers). | ✅ **Decided** — ADR-004: **UUIDv7**. |
| 1 | Brutal | **Temporal and append-only support as *generic* primitives** — whether the platform's data-modeling primitive (C-05) must support effective-dating, retroactive adjustment, and append-only/reversal-entry patterns for *any* builder domain. | ⚠️ **Spec question — see `BACKLOG.md` §3** |
| 2 — Sync posture | **Very high** (constrains layer 1) | **Server-authoritative outbox queue vs. full bidirectional sync engine.** An entire decision layer neither prior evaluation considered. If bidirectional anywhere, the schema needs `updated_at` and version columns on everything syncable, tombstones instead of hard deletes, and a written per-table conflict rule — which is why it constrains layer 1. | ✅ **DECIDED 2026-07-30** — **server-authoritative, with optimistic UI supplied by one standardised client library** (`DECISIONS.md` **D-11**). **None of the bidirectional machinery is required** — no version columns, no sync tombstones, no per-table conflict rules — so ADR-004 was approved *jointly* with this rather than exposed. ⚠️ **An ADR is still owed:** the second-most-expensive decision in this queue has no design-phase record. Two limitations to carry into it — Firebase/Supabase excluded by ADR-010's portable-subset rule, and the pattern fits transactional work better than long-offline editing. |
| 3 — Architecture | High | **Modular monolith vs. monolith-plus-extracted-services vs. microservices vs. serverless.** The aging ahaMatic platform is monolithic — `references/`-only context, a contrast point, never a template. Feeds `03-architecture-realization-design.md`. | ✅ **Decided** — ADR-005: **one deployable per product, modular monolith inside**, modules mapping 1:1 onto the seven fixed components; extraction only under demonstrated, profiled pressure. Microservices rejected because they convert the spec's statically-provable dependency-direction constraints into unverifiable runtime ones. |
| 3 | High | **Architecture tests as boundary enforcement** (e.g. NetArchTest / ArchUnit / import-linter; dependency-cruiser for TypeScript). A concrete *mechanism* for boundaries the spec already mandates — INV-05, INV-06, and the allowed/forbidden dependency directions of `docs/spec/03-software-and-architecture/01-architecture-overview.md` §5.1–§5.2. | ✅ **Decided, and now concretely configured — H8, ADR-014** (`03-architecture-realization-design.md` §4, §11.1): one rule per forbidden direction, encoded against the full allowed-edge table, deny-by-default, run on every commit with no human in the loop; dependency-cruiser (or ESLint boundary rules) for the V1.0 default stack, with the `.NET`/Go equivalent named for a different client stack. |
| 3 | High | **API contract shape** — realizes **C-12** and `docs/spec/03-software-and-architecture/04-api-contract-spec.md`. | ✅ **Decided** — ADR-006: **OpenAPI + generated clients** for both the platform tier and the runtime-generated built-application tier; tRPC internal-only; **GraphQL rejected** (arbitrary query graphs make §6's "never reveals existence beyond grant" unestablishable statically); **gRPC deferred** — ADR-005's single deployable means no internal service seam exists yet. |
| 4 — Client surface shape | Moderate | **PWA-only vs. PWA + one cross-platform app vs. cross-platform everywhere vs. native per platform**, containing the React-Native-vs-Flutter question. Bears on **C-20**. | ✅ **Decided** — ADR-007 (surface shape): platform offers **both a web form and a genuine mobile artifact**, builder-selectable; PWA-only fails C-20. **Mobile runtime: ADR-007 chose Flutter, then ADR-009 reversed it back to React Native + Expo on verified evidence** — three of ADR-007's ecosystem-maintenance claims did not hold (Flutter is *not* first-party for secure storage/location/scanning; RN's architecture migration is settled; Expo SDK 54 fixed the autolinking problem). ADR-009 also withdraws ADR-007's over-claim that rendering parity discharged C-20. |
| — Cloud provider | Moderate | **GCP vs AWS vs Azure** — prior work named GCP as a reference target with no comparison; lead asked for one. | ✅ **Decided** — ADR-010: **GCP default**, AWS/Azure both viable. GCP wins on build/maintenance effort, complexity and managed-Postgres cost; AWS wins on corpus and extreme-scale depth. Key finding: **the app is portable, the infrastructure is not** — so the binding rules are provider-neutral IaC (Terraform/OpenTofu) and the portable subset only (containers, managed Postgres, object storage). |
| 5 — Stack | Real, but cheapest of the five | **Re-evaluate ADR-001 under verification-weighted criteria**, including `.NET` and Blazor, neither properly scored. | ✅ **Decided** — ADR-008: server and web **reaffirmed** (Node/TS + Next.js) on a *revised* rationale; §3's cloud-gravity ground for excluding `.NET` recorded as insufficient. `.NET` scores better on criteria 7/8/9/10 but its unified-stack advantage is unrealisable — Blazor cannot serve arbitrary published web output — and its verification edge is partly neutralised on the runtime-defined data path. Standing obligation: every enterprise-baseline dependency is a recorded decision, not a default. |

### Cross-cutting items

| Item | Status |
| --- | --- |
| **Resolve the brief's premise** — platform vs. applications-built-on-it. Determined how much of the brief applies at all. | ✅ **Answered 2026-07-30** — aimed at the **platform itself**; no longer blocking. Because the brief nonetheless *describes* a domain application, its layer-1/2/4 defaults are re-derived rather than adopted, and its sync default is unusable as stated (it is a per-domain answer). Its method transfers. See `BACKLOG.md` §3 for the full transfers/does-not-transfer table. Direct lead acknowledgment still outstanding. |
| **Expand the evaluation criteria set.** The brief weights three dimensions our six/seven-criterion method under-weighted: **machine-checkable correctness** (how much wrongness is caught with no human in the loop), **enterprise batteries off the shelf** (SSO, RBAC, audit, migrations, jobs, i18n, reporting), and **operational resilience to recurring dependency/build maintenance**. At the time this was raised, only the seventh criterion (third-party dependency minimization) existed in `01-technology-stack-design.md` §2.6, overlapping the third but covering neither of the first two. Rationale: when AI authors the code, the bottleneck shifts from *authoring* to *verifying*. | ✅ **Discharged** — ADR-003 (§13, **Approved**) adopted all three as criteria 8–10 in §2.6, and ADR-008 (§18) applied them. The brief's double-weighting of 8–10 is recorded as *the brief's judgment*, not adopted as settled method. Further criteria raised at the 2026-07-29 standup are **not yet recorded here** — pending lead confirmation. |
| **Vertical-slice validation** — build one genuinely hard slice and measure (iterations to green tests, bugs escaping to manual QA, legibility to an uncommissioning reviewer, and how well a *fresh* AI session modifies it later), instead of deciding further on paper. Directly answers the admission in `01-technology-stack-design.md` §2.3 that no empirical token benchmarking exists. Also check the two or three plugins actually depended on for maintainer activity, issue response times, and first-party status. | ⬜ New |

> **✅ GATE CLEARED 2026-07-30 — the decision queue has no blocking cross-cutting item.** Sync posture is answered (`DECISIONS.md` D-11) and ADR-004 was approved **jointly** with it, so the exposure the gate existed to flag never materialised. Approval is recorded in `DECISIONS.md` D-08: six ADRs approved, three deferred pending the data model, one sub-decision parked. **The next gate is the data model**, which the lead named as the next deliverable and on which ADR-001, ADR-008 and ADR-009 all wait. Current status view: `ADR-REGISTER.md`. The relocated-gate note below is retained as the record of what the gate was.
>
> **⚠ HISTORICAL — Gate relocated 2026-07-29.** The design phase was paused on **ADR-001**, the *cheapest*-to-reverse decision, while the expensive layers stayed open. That inversion is the thing `PROCESS.md` §12 now exists to prevent. Layers 1–5 have since been decided (ADR-004 through ADR-010), so the binding gate is no longer the stack:
>
> **The real gate is now: ADR-004 (datastore) cannot be safely approved until the sync-posture question is answered.** Sync constrains the schema — a bidirectional answer requires version columns, tombstones and per-table conflict rules, changing the shape of the most expensive decision taken. See `BACKLOG.md` §3 (elevated to blocking) and `PROCESS.md` §12.2. Approve ADR-004 **jointly** with the sync answer, or record it as explicitly exposed.
>
> Lead approval is still required for ADR-001, 004, 005, 006, 007, **008**, 009 and 010 before any gated design document proceeds — **eight** ADRs carry `Provisional — Pending Lead Approval`. ADR-008 was omitted from this list in error and is restored here; see `ADR-REGISTER.md` for the full status view. ADR-002 is closed and ADR-003 is approved.
>
> **Still to do on the ordering change** — both items applied in **H2a** (retained for history): ~~annotate `docs/design/implementation-document-map.md` with per-document reversal cost~~ ✅, and ~~decouple `02-tenant-isolation-and-access-control-design.md` from its current dependency on `03-architecture-realization-design.md`~~ ✅ — the brutal-layer document should not sit downstream of a high-layer one. Note the direct dependency is removed but **survives transitively** through `01-invariant-enforcement-design.md`, which retains it; the map records this, and whether that document's own dependency is structural is unassessed. The map's *layer* ordering stays as-is: it is a derivation/dependency order, and `PROCESS.md` §12.1 keeps it deliberately distinct from cost-to-reverse.

---

## Lead Decisions — 2026-07-30 Standup

**Source:** the 2026-07-30 tech standup, at which the `REVIEW-QUESTIONS-2026-07-30.md` sheet was walked through. Answers and their consequences are recorded in `REVIEW-FLAGS-2026-07-30.md`; this section carries only the **work they create**.

**Read this first.** The lead **lifted the specification freeze** and said the spec is expected to keep changing. Spec changes are therefore live work again — but still run as spec-phase tickets, never inline from a design ticket.

### Decisions taken

| Decision | Outcome |
|---|---|
| **Multi-tenancy in V1.0** | ✅ **Yes** — retrofitting is "a big change from a data model perspective." Also to be added to the V1.0 completion criteria |
| **Sync posture** | ✅ **Global framework standardization** — one standardized library pattern (TanStack/React Query named) giving optimistic UI with the **server authoritative underneath**. ⚠️ *Hybrid domain partitioning was an intermediate position he moved off; the meeting notes wrongly record both as decided* |
| **Offline editing needed at all** | ✅ Yes |
| **No-code** | ✅ **No-code tier only, professional builders only.** Low-code is now **excluded** — *"let's do only no code."* The citizen-developer exclusion **survives intact** |
| **Data model wanted** | ✅ **Both** the platform's own schema and the builder-entity schema, "particularly the first" |
| **API layer** | ✅ **Both** internal and external |
| **"Enterprise"** | ✅ **The market sold into**, never the product shape — *"it can be anything"* |
| **Temporal / append-only** | ✅ **Yes, generically, for everything** — audit and history are not optional |
| **BPMN for C-18** | ✅ **Yes** — gold standard; a simplified view can be derived from BPMN, but detail cannot be added to a simplified model later. *Resolves the `BACKLOG.md` §3 assumption and unblocks `04-workflow-and-process-automation-design.md`* |
| **Gartner subscription** | ❌ **No** — closes `BACKLOG.md` §4 |
| **Data Admin capability** | ✅ **Option A — a new capability, C-27** (lead-adjacent decision, 2026-07-30) |

### ADR outcomes

**Approved:** ADR-004 *(with changes, below)*, ADR-005, ADR-006 *(in part)*, ADR-007, ADR-010.
**Deferred pending the data model:** ADR-001, ADR-008, ADR-009 — *"9, 1 and 8 we still have to check."*
**Parked:** the GraphQL rejection inside ADR-006, pending research.

**Changes the lead made while approving ADR-004 — none of these are yet in the ADR:**
- **PostgreSQL only for V1.0**; MySQL and SQL Server support deferred beyond it.
- **Separation is per-app as well as per-customer** — three levels: platform-global config → per-customer → **per-app**. Hierarchy: ahaMatic has customers, customers have apps. *This is finer than ADR-004's recorded schema-per-tenant, and multiplies the migration-fan-out and connection-pool risks ADR-004 already flagged as unresolved.*
- Query builder over ORM: confirmed.
- Sortable-random keys: confirmed, with individual apps free to add their own business identifiers separately.

**New requirement added to ADR-006:** an **AI-to-AI interaction protocol** must be published so AI agents can interact with the platform. The lead could not recall the protocol's name; from his description this is likely **MCP**, though he said "AI to AI," which would be **A2A**. *Confirm which before designing.*

### Spec-phase tickets (freeze lifted)

| # | Ticket Title | Status | Output File(s) |
| --- | --- | --- | --- |
| T65 | Add Data administration capability (**C-27**) — definition. Family **Construction**, Tier 1, depends on C-04 + C-05 | ✅ Done | `docs/spec/01-business-and-ux/02-prd.md`, `03-platform-capability-model.md` |
| T66 | No-code tier commitment — replaced the low-code tier commitment; severed tier from audience. **Verified 2026-08-03:** LCAP-name tension stated explicitly in the charter; both tier definitions now distinguished by assembly method alone; the citizen-developer protection clause survived and was strengthened; scope held to the three in-scope files | ✅ Done | `01-vision-and-charter.md` §2, `03-software-and-architecture/02-domain-glossary.md` (4 entries), `01-business-and-ux/07-competitive-landscape.md` §2 (**one line only**) |

> **⚠ T66's two failure modes, both measured against the files.**
>
> **1. Over-reach — "low-code" is not one thing.** The phrase appears in **6 spec documents**, and only **3** of them are in scope. The dividing line: **the platform's own tier *commitment* inverts; the Gartner category *name* and all market description stay.** The platform remains formally an **Enterprise Low-Code Application Platform (LCAP)** — `DECISIONS.md` D-09 is explicit that D-01's LCAP identity and LCNC positioning **stand**, and only the tier commitment changes. A global find-and-replace of "low-code" would destroy the market analysis and break the surviving identity.
> - **In scope:** `01-vision-and-charter.md` §2 (3 refs) · `02-domain-glossary.md` entries LCAP, LCNC Market, Low-Code, No-Code (4 refs) · `07-competitive-landscape.md` **line 30 only** — its lines 3 and 34 name the LCNC market and the Gartner Magic Quadrant for Enterprise LCAPs and must not change.
> - **Deliberately excluded, checked and confirmed out of scope:** `06-value-proposition-and-success-metrics.md` (3 refs — all market-trajectory figures) · `08-industry-standards-and-benchmarks.md` (9 refs — all Gartner category names, maturity model, adoption benchmarks) · `context-document-map.md` (2 refs — the formal LCAP identity that survives). **These are correct as written; an Executor that "discovers" and edits them has widened scope, not found a gap.**
>
> **2. Under-reach — the two things that must actually change structurally.** (a) **Tier must be severed from audience.** The spec currently defines each tier *by its audience* — no-code's is "non-specialist users" — so committing to no-code without rewriting the definitions implicitly readmits the audience `DECISIONS.md` D-02 excludes. Tiers must be defined by **assembly method alone**, with audience set independently by the charter. (b) **The LCAP-name tension must be stated, not papered over:** the platform keeps a name containing "Low-Code" while committing to the no-code tier. A reader will read that as a contradiction unless the charter says why it isn't.
>
> **3. `DECISIONS.md` D-18 guard.** D-09 reaches the **Builder**  application-code path only. T66 must **not** narrow the spec-defined **Extender** role (`02-governance-and-security/03-access-control-and-tenancy-model.md` §6) or the SDK's "builder **or extender**" framing (`05-integration-and-extensibility-spec.md` §67) — both survive intact.
| T67 | Temporal, append-only and history as generic C-05 primitives. **Verified:** new §9 inserted before the closing sections, Precedence→§10 / Binding Rules→§11, all internal refs reconciled; privacy-erasure boundary stated without editing the privacy document; history kept distinct from both the audit trail and D-11's sync posture | ✅ Done | `03-software-and-architecture/03-data-model-and-entity-spec.md`, `01-business-and-ux/02-prd.md` |
| T68 | Propagate T65–T67 across the library, re-sync capability spans, run `ai-aha-consistency-check` | ✅ Done | `02-domain-glossary.md`, `04-personas-and-roles.md`, `05-user-journeys.md`, `context-document-map.md`, **+ 20 further spec documents carrying the stale span** |
| T69 | **Data-protection obligations** — state protection in transit, protection at rest, and key custody; name **OWASP ASVS 5.0** at a chosen assurance level as the verification baseline. Authorized by `DECISIONS.md` **D-22** (lead, 2026-08-03). ⚠ The **OWASP Top 10 is an awareness document and is not testable** — it cannot be the gate; ASVS is | ✅ Done | `02-governance-and-security/02-security-policy.md`; verification baseline reflected in `04-devops-and-cloud-infra/03-testing-and-quality-gates.md` |
| T70 | **Agent-facing contract obligation** — the external contract consumer modelled as an actor (`04-personas-and-roles.md` §2.2) and the consumption obligation attached to C-12 (`04-api-contract-spec.md` §9.7). No protocol named, no capability minted, no renumbering. Authorized by `DECISIONS.md` **D-23** | ✅ Done | `01-business-and-ux/04-personas-and-roles.md`, `03-software-and-architecture/04-api-contract-spec.md` |
| T71 | **Fix the three deferred agent-governance numerics** — six ceilings set in `06-non-functional-requirements.md` §10, with the nesting stated and the derived-vs-judgment split made legible. Authorized by `DECISIONS.md` **D-24** | ✅ Done | `03-software-and-architecture/06-non-functional-requirements.md` |
| T72 | **Close the five loose specification findings** — C-12's consumer clause (its **third** finding); the missing "Audit Trail" glossary term; C-27's empty state; the ordinal in `04-personas-and-roles.md` §2.2's title; the context map's security-policy summary. All five verified against the files 2026-08-03 | ✅ Done | `05-integration-and-extensibility-spec.md`, `02-domain-glossary.md`, `05-user-journeys.md`, `04-personas-and-roles.md`, `context-document-map.md` |

> **T72's two substantive elements — the other three are one-line corrections.**
>
> **Element A is not an append.** `05-integration-and-extensibility-spec.md` §67 defines C-12's consumers as *"a builder **or extender**"*. That single clause has now produced **three** findings — `DECISIONS.md` **D-18** (a design ADR wrongly read the Extender as config-only), **D-23** (the agent-facing gap), and this one. **Three findings against one sentence means the sentence is the defect, not each reader.** Adding a third name to a list that has failed twice sets up a fourth; the clause must stop silently excluding consumers the specification models elsewhere.
>
> **Element C is not a list extension.** `05-user-journeys.md` §7.1's existing bullet describes *"a built application with nothing yet modeled…"* — but **C-27 operates before an application exists**, which is its boundary against C-06 and C-07. Its empty state is a different shape (entities defined, no records), so it needs authoring, not appending. This is why T68 deferred it rather than sweeping it in.


> **⚠ Open finding, surfaced 2026-08-03 — the same phase inversion as D-22, found the same way.** The lead's 2026-07-30 requirement that *"an AI-to-AI interaction protocol must be published so AI agents can interact with the platform"* was recorded **only as a design decision** (ADR-013, MCP). Searching the specification for it returns nothing: the sole `MCP` occurrence in `docs/spec/` is in `07-competitive-landscape.md` §5, describing **a competitor's** offering. So the design again realizes an obligation the spec never states.
>
> **It is not obviously a spec gap, which is why T70 is not yet scoped.** Two readings: (a) MCP merely *realizes* **C-12** (the SDK — the existing programmatic contract), so no spec change is owed; or (b) it is a genuine gap, because C-12 is defined as the contract through which *"a builder or extender"* works with the platform, and **an external AI agent is neither**. The spec models four actors — the autonomous platform-operating agent (`05-meta-operations/01-agent-operating-charter.md`), builders, extenders, and end users — and the glossary explicitly holds the platform-operating agent *distinct* from builder-facing tooling. **An external AI agent consuming the platform's contract is a fifth actor class the spec does not model.** Reading (b) is the more likely correct one; confirm before generating T70.

> **⚠ T68 gained a second, unrelated item from T67 — a stale `§N` reference, independently verified 2026-08-03.** `05-meta-operations/08-change-management-and-evolution-policy.md` **line 105** cites *"`03-data-model-and-entity-spec.md` §8 and §9"*, where **§9 meant that document's Precedence section — now renumbered to §10** by T67's insertion. T67 found this and correctly refused to fix it, being outside its two authorized files.
>
> **This is the only break.** Verified library-wide: every other external reference to that document cites §3–§8, which kept their numbers. T68 must close this one alongside the span sweep — note it is a **different kind of drift** from the capability-span work, so a sweep that greps only for `C-01–C-26` will not find it.

> **⚠ T68 was under-scoped, and the four named documents are not the real list.** Measured 2026-08-02 after T65 landed C-27: the stale span `C-01–C-26` sits in **23 spec documents, 36 occurrences** — it lives in a **boilerplate preamble sentence repeated library-wide** (*"It references the canonical capabilities (C-01–C-26) and release gates (G-1–G-6)…"*), not only in the three propagation documents. An Executor working from the four-document list would leave twenty spec documents stale and the library internally inconsistent while appearing complete.
>
> **The full spec list (23):** `01-business-and-ux/` 04, 05, 06, 07, 08 · `02-governance-and-security/` 01, 02, 03, 04, 05, 06, 07, 08 · `03-software-and-architecture/` 01, 02, 03, 04, 05, 06, 07 · `04-devops-and-cloud-infra/` 01, 03. Highest counts: `02-domain-glossary.md` (4), `07-competitive-landscape.md` (3), `03-access-control-and-tenancy-model.md` (3). **`context-document-map.md` does not carry the span** — it enumerates capabilities individually instead, so it needs a C-27 *entry*, not a span edit; do not assume a grep for the span finds everything it owes.
>
> **Two occurrences fall outside a spec ticket's reach and must not be edited by T68:** `docs/design/implementation-document-map.md` line 19 (design-phase file — needs its own amendment) and `REVIEW-FLAGS-2026-07-30.md` (a dated meeting record; historical, leave as-is).

> **T65 scope (C-27).** Data Administration is a **generic administrative interface auto-derived from builder-defined entities** — define a model, get working CRUD without building an application. It is the builder-facing counterpart to the runtime-generated contract tier of ADR-006 §16.2. It is **not** C-05 (which defines shape, not record operations), **not** C-06 (which configures an application), and **not** C-07 (which runs built software for end users). Precedent for giving builder-facing tooling a capability ID: **C-19**. Per `PROCESS.md` §5 the ID is permanent and never reused; C-26 was the previous highest.

> **T66 caution.** This is a tier **inversion**, not a widening — the charter currently *"commits explicitly to its low-code tier"* and the glossary lists "no-code platform" as a **disallowed synonym** for the platform's identity. The spec also defines each tier **by its audience**, so the definitions must be rewritten to sever tier from audience; otherwise committing to no-code implicitly readmits non-specialist users, which the lead explicitly excluded. The citizen-developer exclusion is **unaffected** and must survive.

> **T68 closed 2026-08-03 — 25 files, all six elements verified.** Span zero across `docs/spec/`; "twenty-six" zero; C-27 present in all four propagation documents; the stale `§9` now reads `§10` and resolves. `ai-aha-consistency-check` reported **no drift across checks 1–7**, and a supplementary pass verified all **217** unique cross-document `§N` citation pairs library-wide — none stale. Three of the six elements were invisible to a span grep, which is what the corrected scope existed to catch.

---

> **🎉 The design library has zero open prerequisites — first time in the phase (2026-08-03).** Both gates that ever existed are closed, and `implementation-document-map.md` was corrected in five places to match: three Build Status cells (`01-agent-runtime-and-control-design.md`, `02-token-and-compute-budget-design.md`, `06-self-correction-and-fallback-design.md`), the readiness legend, and the closing prerequisites note. **Zero `Gated — prerequisite open` cells remain; every document in the map reads Buildable now.**
>
> The flag itself is retained in the legend for future use, explicitly noted as *"retained for future use, not because anything currently bears it"* — so its presence is not read as an unresolved gate. The cells now cite `DECISIONS.md` **D-24** rather than the closed `BACKLOG.md` §1, and the self-correction cell records that its ≤ 3 is **derived** from the ladder's three rungs rather than chosen — preserving the derived-vs-judgment distinction T71 established, at the point a design ticket will actually read it.
>
> **What this unblocks:** the three Layer 6 agent documents above, plus `03-data-administration-design.md` (C-27, Layer 3) and `01-invariant-enforcement-design.md`. The design phase has no remaining blocker of any kind.

---

## Design Phase — Remaining Queue (H10–H48)

**Durably recorded 2026-08-03, before executing H10, at explicit user request — the full remaining design phase, sequenced.** Row titles, output files, and dependencies are drawn directly from `implementation-document-map.md`; nothing here is invented. This is a **schedule of rows, not pre-written ticket prompts** — `PROCESS.md` §4's generation rule stands: each ticket's full system prompt is still generated just-in-time, from current on-disk state, right before it runs. What is durable now is the queue itself, so it survives independently of this conversation.

> **Corrected 2026-08-03 — this was NOT a map defect; my earlier note here was wrong and is superseded (`BACKLOG.md` §1c).** `implementation-document-map.md` already discloses and justifies `09-ai-assisted-builder-tooling-design.md`'s dependency on three Layer 6 documents (`01-agent-runtime-and-control-design.md`, `03-human-in-the-loop-design.md`, `04-prompt-and-context-assembly-design.md`) in its own "Design layers and dependency ordering" section, under **"One cross-layer exception"** — naming it, explaining why (C-19 inherits the Meta-Operations guardrails per `01-business-and-ux/02-prd.md` §4), and stating it is the *only* such case. An exhaustive topological sort of all 39 unwritten documents independently confirmed that last claim. **What the sort still earns its place for:** confirming, rather than assuming, that this is the only exception, and placing the document correctly at **H45** — after its three Layer 6 prerequisites — which is what the map's own exception note implies once read together with the per-row dependencies.

| # | Ticket Title | Output File | Depends On (design docs) | Status |
|---|---|---|---|---|
| H10 | Tenant Isolation and Access Control Design. **Verified 2026-08-03:** all 13 sections present; five actor classes genuinely covered; RLS-reconsideration finding (§9) independently engages the isolation-strength question rather than repeating ADR-004's portability rationale, and its MySQL/MariaDB citation checked against `01-technology-stack-design.md` §14.5; no new tables invented (`application_metadata` confirmed pre-existing); **ADR-018 recorded** — the one genuine decision, with question + criteria as first-class fields per the extended convention | ✅ Done | `docs/design/02-identity-access-security/02-tenant-isolation-and-access-control-design.md` |
| H11 | Authentication and Identity Design. **Verified 2026-08-03:** Authenticated Actor Identity object cross-checked field-by-field against tenant-isolation's actual Registry Accessor precondition and Context Resolution Point step 3 — no overreach, no missing field; Authentication Gate placement verified against `03-architecture-realization-design.md`'s literal component text, not assumed; **ADR-019 recorded** — full question + criteria-applied fields, honest non-discriminating criterion retained rather than dropped | ✅ Done | `docs/design/02-identity-access-security/03-authentication-and-identity-design.md` |
| H12 | Scalability, Availability, and Performance Design. **Verified 2026-08-03:** connection-pooling mechanism (SET ROLE per-checkout) decouples pool size from schema count, correctly reframing the crossover question; crossover finding (~500/~2,000 schemas) explicitly flagged as reasoned engineering waypoint, not verified benchmark; synchronous-replication no-ADR call checked — genuine forced consequence of NFR §7's zero-data-loss floor, not a dodge; **ADR-020 recorded** with full question + criteria fields | ✅ Done | `docs/design/01-design-foundation/04-scalability-availability-and-performance-design.md` |
| H13 | Security Controls Design. **Verified 2026-08-06:** all 11 sections present; §5's three-origin scope checked against `03-architecture-realization-design.md` §10.2's *amended* ADR-016 record (not the withdrawn original) — the §5.2 manifest is stated as platform-team-authored-only in three separate places (§5.1 table, §5.3, §11), so the uniform-treatment error ADR-016's amendment exists to prevent cannot be reintroduced by a careless reader; §6.1's six-row evidence list confirmed to mirror `01-invariant-enforcement-design.md` §6's hand-off pattern rather than invent a second one; §7 (data protection, the section the ticket flagged as most likely to be skipped) realized in full, building on `02-platform-data-model-design.md` §8's key hierarchy without restating or altering it; **ADR-021 recorded** with full question + criteria-applied fields, honestly marked *reasoned* throughout and **Provisional — Pending Lead Approval** | ✅ Done | `docs/design/02-identity-access-security/04-security-controls-design.md` |
| H14 | AI Tooling Security Design. **Verified 2026-08-06:** all 11 sections present; the tooling/security split the ticket flagged as central risk is held — §5 designs the provenance *requirement and enforcement point* and explicitly leaves the attribute's storage shape and the confirmation workflow to H45, so neither document designs the other's half; §3.3 states the honest limit (a misled *suggestion* is not preventable, a misled *action* is) rather than overclaiming, correctly marked reasoned; C-19/C-26 discipline held throughout; two open gaps flagged rather than absorbed (§4.3 license-encumbered generated content; the provenance attribute's storage shape); **ADR-022 recorded** with full question + criteria fields. **Two findings raised at close** — (a) ✅ **fixed 2026-08-06 on explicit user direction:** three citations of `04-prompt-and-context-assembly-design.md` (§3.2, §9, §10) named the wrong group folder (`03-data-construction-contracts/`, where that number is `04-workflow-and-process-automation-design.md`; the document is Layer 6), now corrected to the bare-numbered-filename convention with a Layer 6 marker; (b) 🔶 **open — ADR-022's Consequences clause binds a frozen specification document to change** (`ADR-REGISTER.md` live issue 6) — `04-devops-and-cloud-infra/02-ci-cd-pipeline-spec.md` §5 enumerates the Merge Gate's mandatory checks as a closed list and, unlike §4's stages, carries no "additional checks may exist" allowance, so a sixth check is a spec gap requiring a spec-phase ticket, not a design-phase consequence | ✅ Done | `docs/design/02-identity-access-security/05-ai-tooling-security-design.md` |
| H15 | Compliance and Data Residency Design. **Verified 2026-08-06:** all 13 sections present, closing order correct; the region-scoping mechanism is genuinely built here rather than deferred, and composes with tenant isolation as instructed — one extra field on the Registry Accessor's existing return, one inline check in the existing connection-acquisition function, no second accessor or scoping path; **grep-confirmed zero named jurisdictions, regimes, or regulations** anywhere in the document, the hardest constraint the ticket set and the one most likely to slip; every citation resolves and follows the post-reorg convention (bare numbered filenames for design, group-folder paths for spec) — the first document in this batch with a clean citation sweep; **ADR-023 recorded** with full question + criteria fields and an honestly-stated Very-high reversal cost. **The Executor's own correction was checked and is right:** the residency handover lives in `02-tenant-isolation-and-access-control-design.md` §2, not §11 as this ticket's prompt asserted — §11 carries no residency, region, or compliance text at all (`BACKLOG.md` §1e). **Contrast worth recording against H14:** ADR-023 also binds `04-devops-and-cloud-infra/02-ci-cd-pipeline-spec.md`, but *legitimately* — it realizes the residency clause §6 already contains, rather than requiring the spec to add one, which is exactly the line ADR-022 crossed | ✅ Done | `docs/design/02-identity-access-security/06-compliance-and-data-residency-design.md` |
| H16 | Data Governance and Privacy Design. **Verified 2026-08-06:** all 12 sections present, closing order correct; **the redaction determination the ticket made central was genuinely performed, not assumed** — §7.1 confirms the existing filter's *architecture* fits (one mandatory choke point per channel), §7.2 states plainly that its *matching criteria* do not (secret-shaped names and entropy do not catch `email`, `full_name`, `date_of_birth`, which are neither credential-named nor high-entropy), and §7.3 extends that same filter with two match classes rather than building a second one — exactly the composition discipline this layer has settled on; grep-confirmed zero named jurisdictions or regulations; §4.3's honest two-shape definition of "deleted" checked against `02-platform-data-model-design.md` §10, which does fix the never-hard-deleted convention on INV-08 and audit-referential grounds — and H16 correctly reproduced that document's own caution not to mistake it for D-12 compliance; **ADR-024 recorded**, bundling five sub-decisions under ADR-023's precedent, with full question + criteria fields and an honestly-stated Very-high reversal cost that names irrecoverable deletion as a worse failure mode than an expensive migration. **Closing sequence confirmed run** — first ticket under the 2026-08-06 `PROCESS.md` §4 Closing Sequence rule; review reported clean on first pass. **One ticket-authoring defect, mine not the Executor's:** `02-platform-data-model-design.md` was named in the ticket's Writing Rules but omitted from its Dependency Inputs list, so the Executor read it and disclosed the discrepancy rather than either skipping the instruction or reading silently — the correct handling of a contradictory prompt | ✅ Done | `docs/design/02-identity-access-security/07-data-governance-and-privacy-design.md` |
| H17 | Audit and Traceability Design. **Verified 2026-08-06:** all 13 sections present, closing order correct; **the consolidation this ticket existed to perform was genuinely performed** — §4.3 reconciles all 22 inherited evidence rows (6+3+6+7, arithmetic checked against the five source lists) into one `event_type`-discriminated record rather than five parallel logs, with three same-event conflicts resolved explicitly and two imperfect-fit rows captured-and-flagged rather than dropped; **the erasure/immutability tension is resolved by construction, not by exception** — actor and target references are opaque identifiers, so the audit trail never captures content the erasure obligation would need to reach, and §7's closing paragraph correctly argues why that is this document's own contribution rather than a restatement of either upstream document; §6.3 states the tamper-evidence limit with unusual precision — hash-chaining proves internal consistency but not resistance to a full-table-privileged rewrite, the external anchor closes that only from its own point forward, and a compromise of the anchor's own storage is outside what the mechanism detects; grep-confirmed no jurisdiction, vendor, product, or cryptographic library named anywhere; **ADR-025 recorded** with all ten convention fields, graded **High** rather than Very High with the distinction reasoned (append-only means future change is additive; this mechanism does not itself destroy or transform data the way residency enforcement or retention deletion does). **The mandatory review pass earned its place here** — it caught and fixed real dependency-scope violations before finalizing, including two documents cited as load-bearing mechanism sources that were not dependency inputs at all; this is the first ticket where the closing sequence demonstrably changed the deliverable. **ADR-025 binds the already-written `03-authentication-and-identity-design.md`** to populate identity-and-session events in this document's base record shape — checked, and **legitimate**: that document's own §9 boundary already states it fixes only that such events occur and where, "never how they are captured, stored, or made tamper-evident," so the obligation was pre-accepted rather than imposed retroactively | ✅ Done | `docs/design/02-identity-access-security/08-audit-and-traceability-design.md` |
| H18 | Licensing and Dependency Compliance Design. **Verified 2026-08-06 — Layer 2 now closed at 9/9, all nine files confirmed on disk:** all 13 sections present, closing order correct; **H14's open question is genuinely answered, not re-deferred** — §8 adopts a distinct content-similarity technique at the *same* chokepoint as the dependency check, and encodes its own epistemic limit into the evidence vocabulary: the result is `no-match-against-corpus`, **never `clear`**, so the audit record cannot later be misread as a certification the check never made; the "no specification gap" conclusion was checked and is correct — spec §4 already states that category governs "whether content originates from an open community, a commercial vendor, or **generated output**," so this document supplies a missing *mechanism*, not missing spec text, staying the right side of the line ADR-022 crossed; **ADR-012's standing obligation is discharged with the gap ADR-012 could only name** — two re-evaluation triggers, since a licence can change with zero change to the consumer's manifest entry, so an event-driven-only trigger would carry a now-Prohibited dependency forward indefinitely; evidence expressed as five `event_type` values inside `08-audit-and-traceability-design.md`'s consolidated base record rather than a sixth free-standing list — the consolidation is working as designed on its first use; grep-confirmed no scanning tool, similarity product, corpus, or vendor named; **ADR-026 recorded** with all ten fields and held honestly at **Low** reversal cost, explicitly not inflated to match its neighbours. **The review pass again changed the deliverable** — five out-of-scope citations caught and removed, the second consecutive ticket where it did real work | ✅ Done | `docs/design/02-identity-access-security/09-licensing-and-dependency-compliance-design.md` |
| H19 | Application Construction Design. **Verified 2026-08-06 — opens Layer 3:** all 13 sections present, closing order correct; **the load-bearing constraint held — grep-confirmed zero domain leakage**, no vertical, no example entity, no field-type vocabulary anywhere, on the one document where a single illustrative HR field or invoice line would have narrowed the platform; the configuration model (constructs plus structural/behavioral/access bindings) is genuinely generic, and §4.2's closed platform-owned vocabulary is the correct mechanism for keeping it so; composition with the two upstream enforcement mechanisms is clean — no second identity or scoping path; four `event_type` values land in `08-audit-and-traceability-design.md`'s base record, with three honestly admitted as imperfect fits under an existing category rather than forced into an invented ninth one; **ADR-027 recorded** with all ten fields at Very-high reversal cost. **One finding — `BACKLOG.md` §1f:** §4.3 defers the concrete construct/binding-kind enumeration to `02-data-model-and-entity-design.md`, which **depends on** this document and whose charter is builder-*defined* structures, not a platform-*owned* closed vocabulary — so the enumeration the domain-neutrality argument rests on currently has no owner. The refusal to invent a taxonomy without specification evidence was correct; only the destination is wrong. H20's prompt assigns it explicitly | ✅ Done | `docs/design/03-data-construction-contracts/01-application-construction-design.md` |
| H20 | Data Model and Entity Design. **Verified 2026-08-06 — the heaviest ticket since H17, and a clean review pass:** all 16 sections present, closing order correct; grep-confirmed zero domain leakage, matching H19's discipline; **the composition risk this ticket carried was handled correctly** — the Entity Access Gateway is a genuinely new mechanism, but §11 places it *inside* a connection `02-tenant-isolation-and-access-control-design.md` §5.1 has already scoped, refuses any caller-supplied tenant/application/schema identifier, inherits the mandatory self-verification read, and grants migration and erasure no exemption, so ADR-018's "no downstream document may design a second tenant-scoping mechanism" is honored rather than quietly breached by a new chokepoint; §8.2 reconciles D-12's append-only mandate with the cross-version erasure requirement by erasing field *content* in place while preserving version existence and `record_id`/`version_id` continuity, keeping `08-audit-and-traceability-design.md` §7's opaque-reference resolution intact; relationships enforced at the Validation Engine rather than by native foreign keys, correctly reasoned from versioned rows binding an FK to a physical version rather than the logical record; **all five inherited storage-shape obligations addressed, none silently dropped** — three discharged, two declined with grounds (`BACKLOG.md` §1g, and provenance to H45); **ADR-028 recorded** with all ten fields at Brutal grade. **The contested enumeration was determined, not dodged** — §3.4 declines it back to `01-application-construction-design.md` on three checked grounds, and establishes that no rework follows wherever it lands, since the `kind` columns already exist (`BACKLOG.md` §1f-resolution) | ✅ Done | `docs/design/03-data-construction-contracts/02-data-model-and-entity-design.md` |
| H20a | **Amendment** — Construct and Binding-Kind Vocabulary. **Verified 2026-08-06, clean review pass, closes `BACKLOG.md` §1f:** two construct kinds (Surface, Command), three binding kinds coextensive with §4.1's families, two action classes (View, Invoke) — each *derived* rather than asserted, Surface and Command argued as what remains once every capability this document does not own and every relation a binding already expresses is set aside; a grouping construct explicitly excluded because structural bindings already express composition; **binding-kind cardinality checked against the sibling and confirms rather than contradicts it** — `02-data-model-and-entity-design.md` §3.3 had already fixed `relationship_catalog.kind` as taking one of three values, so subdividing a family would have broken shipped design; §4.2.5 adds a three-part admission test (genericity, irreducibility, non-duplication) so "closed" keeps meaning closed; §4.3 rewritten rather than left pointing at a decline; the honest-limit paragraph **refuses to invent a fourth kind against no evidence**, citing §1f's lesson in the opposite direction; extended in place, no renumbering, no inbound-citation sweep owed; zero domain leakage. **ADR-029 consumed.** Residual, disclosed and out of scope: three "(H20, not yet written)" parentheticals at §2, §6, §11 are now factually wrong | ✅ Done | `docs/design/03-data-construction-contracts/01-application-construction-design.md` *(amended in place; commit `4d5450f`)* |
| H20b | **Amendment** — Region-of-Record on the Platform Registry. **Verified 2026-08-06 from the working-tree diff — no handoff summary was provided for this ticket** (`PROCESS.md` §3's known silent failure mode; recorded rather than assumed): `region_of_record` added to `platform.tenants` and `platform.applications`, both FK → the **existing** `platform.regions` catalog, so no second region catalog is introduced; an unset value resolves to *unresolved* and is surfaced as such, never coerced to a permissive default, with the refusal rule left to `06-compliance-and-data-residency-design.md` §4 rather than re-decided; a change is an ordinary current-state update captured by the audit trail per this document's own §10 convention, explicitly not a new versioned row, and the approval hold correctly attributed to ADR-023's Region Boundary Check rather than realized as a temporal structure here; scope creep into residency design avoided, and the classification-tier mechanism correctly not duplicated; **no ADR recorded — judged explicitly with grounds, so ADR-030 remains free**; extended in place with no section renumbering, which was this ticket's specific flagged risk (`ADR-REGISTER.md` live issue 1 concerns inbound citations to this file). Closes `BACKLOG.md` §1g. **Uncommitted as of this close** | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md` *(amended in place; **not yet committed**)* |
| H21 | Data Administration Design (C-27). **Verified 2026-08-06, clean review pass:** all 12 sections present, closing order correct; grep-confirmed zero domain leakage; the derivation produces a **runtime-computed** Interface Description rather than a stored artifact, correctly reasoned from synchronization drift across schema evolution; all four operations map onto the Gateway's existing vocabulary with **no administrative exemption** from validation, consent, or classification — the exact shape of exemption an isolation breach hides in; the empty state is designed as a structural consequence of the derivation rather than a guarded special case, honoring `05-user-journeys.md` §7.1's protection of it; **the §1h determination is half-resolved on grounds that hold** — the professional-builder scope limit was checked against `01-business-and-ux/02-prd.md` §4's C-27 row, which does state the capability "operates strictly within the professional-builder model, extended only by the administrative access a tenant grants (C-03)," so the narrowing is licensed by C-27's own definition rather than convenient; the administrative-scope grant is narrowing-only and its check runs *upstream* of the Gateway rather than becoming a fourth step inside it; **ADR-030 recorded** at Moderate grade. **Two findings — (a) `BACKLOG.md` §1i:** the grant table is placed in the per-application schema, whose contents `02-platform-data-model-design.md` §3.3/§12 partitions between exactly two parties, admitting no third contributor — a coordination gap in the project's only Brutal-graded schema, **caused by this ticket's own prompt omitting that document from its dependency inputs**, which the Executor disclosed rather than papering over; **(b) `BACKLOG.md` §1h-resolution:** end-user-role-scoped, construct-mediated record access is named open at §5.5 with no destination assigned — the same ownerless-deferral shape §1f had | ✅ Done | `docs/design/03-data-construction-contracts/03-data-administration-design.md` |
| H21a | **Amendment** — Administrative-Scope Grant Placement. **Verified 2026-08-06, clean review pass, closes `BACKLOG.md` §1i:** determination was **(a) relocate** — the grant's physical table moves to `tenant_<id>.administrative_scope_grants`, defined by `02-platform-data-model-design.md` §3.2, with `03-data-administration-design.md` §5.3 citing rather than defining it. **This resolved the gap rather than legalizing it:** §3.3's two-way partition statement needed no amendment — it is simply true again once the third structure is gone, so the library ends with one fewer exception rather than one more. Grounds checked and sound: the grant is the same family of artifact as `tenant_user_application_scope`, one level further down, and every per-tenant access-scoping table already references applications by `application_id` rather than reaching into a per-application schema — the original placement inverted that direction. **A real consequence of relocation was caught rather than glossed** — the grant must now name which application, so an `application_id` FK was added; the cross-schema cost inverts honestly, `tenant_user_id` becoming an ordinary same-schema FK and `entity_id` taking over as the plain unenforced value. §3.4's hierarchy boundary table and §12's "On C-27" bullet both corrected — the latter had asserted this document supplies nothing for C-27. **ADR-030 amended in place, not superseded**, correctly, since no semantics, column meaning, check position, or narrowing rule changed. No section inserted or renumbered in either document — on the one file where that has already gone wrong once | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md` + `docs/design/03-data-construction-contracts/03-data-administration-design.md` *(both amended in place)* |
| H22 | Workflow and Process Automation Design (C-18). **Verified 2026-08-06:** all 14 sections present, closing order correct; **grep-confirmed no BPMN engine named anywhere** — D-20's buy-don't-build principle held without drifting into selection, and no claim made about what any engine currently supports, licenses under, or persists; grep-confirmed zero domain leakage on the subject most likely to invite an approval-chain or purchase-order example; **§4.3 is the ticket's central ask and it delivered** — six hard constraints on any future engine selection, each derived from the platform's own precedent rather than asserted, with constraint 1 disqualifying any engine whose only multi-tenancy is a shared store distinguished by an identifier column, on exactly the ground ADR-004 already rejected that pattern for the platform's own datastore; a seventh constraint (concurrent multi-version execution, in-flight instances bound to their originating version) surfaces separately at §8.2; automated rules are Gateway callers with no engine-originated exemption, and human-task routing composes with the existing access-binding and tenant-isolation mechanisms rather than inventing a fourth; §3.2 fixes exactly four points at which a process step crosses into governed platform mechanism, and states there is no fifth; **ADR-031 recorded** with all ten fields at Very-high grade. **One handover, well-founded — `BACKLOG.md` §1j:** the process-definition catalog and instance-anchor record are handed to `02-data-model-and-entity-design.md` as a representability constraint. Unlike §1f's failed deferral this runs in the correct direction — a process definition is *builder-authored* content, which is precisely that document's charge — and it models itself explicitly on `01-application-construction-design.md` §6's constrain-without-owning pattern. **Recorded but deliberately not ticketed**: it is the fourth storage obligation discovered for H20 since H20 shipped, and §1j recommends collecting the rest and running one consolidated amendment when Layer 3 closes | ✅ Done | `docs/design/03-data-construction-contracts/04-workflow-and-process-automation-design.md` |
| H23 | API Contract Design. **Verified 2026-08-07:** all 16 sections present, closing order correct; grep-confirmed zero domain leakage; **the trap this ticket flagged was avoided precisely, and by name** — §8.1 classifies an unresolved breaking change into `02-security-policy.md` §6's *existing* Critical/High vocabulary, consumed by `02-ci-cd-pipeline-spec.md` §5's **already-existing** vulnerability bullet, adding no bullet to that document's closed list, and it cites ADR-022 as the shape it does not repeat (`ADR-REGISTER.md` live issue 6); the two tiers stay structurally distinct throughout, with built-application-tier compatibility discharged by composition with `02-data-model-and-entity-design.md` §7 rather than independently re-diffed — plus one genuinely-found exception at §8.3, that a change to the *generation logic itself* needs a sampled diff since no builder action would otherwise trigger a check; sign-off differs by tier and reuses the builder's existing confirmation flow rather than inventing a second approval; the MCP surface inherits the OpenAPI contract's versioning rules wholly rather than carrying its own; **an honest limit stated as absolute rather than hedged** — schema-identical *behavioral* breaking changes are invisible to diffing entirely, not merely uncertain, and no mechanism here closes that; one time-sensitive tooling claim properly verified 2026-08-07 with the tool named as a category example, not a commitment, on the ADR-012/Valkey pattern; **ADR-032 recorded** with all ten fields at High grade. **`BACKLOG.md` §1h assessed and declined on structural grounds, with a destination named** — the reasoning holds: by the time a response reaches the contract layer, the Entity Access Gateway has already bounded what content it may carry, so no response-shape rule could express the outcome. Routed to a fourth Gateway check at `02-data-model-and-entity-design.md` §4.1 (`BACKLOG.md` §1h-destination) — the fifth obligation on that document, folded into §1j's consolidated-amendment recommendation | ✅ Done | `docs/design/03-data-construction-contracts/05-api-contract-design.md` |
| H24 | Integration and Extensibility Design. **Verified 2026-08-07 — closes the library's longest-open question, for the Extender origin:** all 15 sections present, closing order correct; grep-confirmed zero domain leakage; **the first-order question `03-architecture-realization-design.md` §10.3 and `04-security-controls-design.md` §5.3 both deferred here is answered, not re-deferred** — Extender-authored code never executes in the platform-core process; it is compiled to WebAssembly and run in an embedded, capability-scoped host behind a new Extension Invocation Point that extends the existing registry rather than replacing it; **the four isolation strengths were weighed on the record** (in-process rejected outright, OS-process rejected as default-allow and non-portable, VM/microVM rejected on proportionality, WASM/WASI selected as default-deny by construction) rather than a conclusion asserted — which is what §10.4's withdrawn narrowing required; **the load-bearing technology claim was properly verified 2026-08-07**, including that Node's built-in `node:wasi` is stated by its own maintainers to be insufficient as a security boundary for untrusted code — precisely the class of unchecked ecosystem claim that produced ADR-009; host functions map 1:1 to `builder-tooling`-tagged operations within the extension's grant, composing with `05-api-contract-design.md` rather than defining a second SDK versioning scheme; the `authority-refusal` event is **reused with a new `source_mechanism` value** rather than a new type; the marketplace origin is left genuinely open — mechanism offered for adoption, no determination made on another document's behalf. **§5.5 is the sharpest honest limit in the library to date:** confinement bounds *reach*, not *intent within a granted reach*; an in-grant extension composing two legitimate capabilities undesirably violates no dimension, no isolation mechanism of any strength closes it by definition, platform-team code closes the same risk by review which this origin structurally cannot receive, and the residual mitigation is grant narrowness — with an explicit clause stating this is not a re-deferral of the answered question. **ADR-033 recorded** with all ten fields at High grade, flagging exposure to three Provisional upstream ADRs per `PROCESS.md` §12.2. **One handover, well-founded — `BACKLOG.md` §1k:** the per-tenant extension-registration table is constrained but not defined, following §1i's precedent rather than repeating its error | ✅ Done | `docs/design/03-data-construction-contracts/06-integration-and-extensibility-design.md` |
| H25 | Cross-System Data Layer Design (C-24). **Verified 2026-08-07:** all 11 sections present, closing order correct; grep-confirmed zero domain leakage and **no external product named** on the subject that invites it most; **the ticket's sharp edge was answered rather than cited around** — §4 realizes each of C-24's five guarantees as an *extension of its existing mechanism at the point the path leaves the platform*, never a competing one, and the temptation to write five citations and call it done was not taken; the residency case resolves correctly — a connector without a declared region-of-record resolves to **refusal**, extending the unresolvable-resolves-to-most-restrictive discipline rather than inventing a default; data crossing *in* is held at the most restrictive tier and category until committed to a configured field; **the INV-03 treatment was checked closely and is honest, not a weakening** — §4.2 frames the credential crossing as "a deliberate crossing, not a leak," scopes the guarantee explicitly to the platform's own side of it, and §10 restates invariants-are-floors naming INV-03; **§6 states the limit in three tiers** — what is structurally prevented, what can only be detected ("verified" degrades to "evidenced" once the path leaves), and what cannot be bounded at all, correctly inheriting `06-integration-and-extensibility-design.md` §5.5's residual, which had named this layer as its vector; four existing event types reused, one added; **ADR-034 recorded** with all ten fields at High grade, not inflated to Brutal. **One finding — `BACKLOG.md` §1l, and the cause is this ticket's own prompt:** `connector-registration` may duplicate H24's `extension-registration`, and the check could not be performed because the prompt excluded §11 of the very document it told the Executor to check against. The Executor handled it correctly — declining to reuse a name visible only outside authorized scope and deriving a self-contained type instead — which is why this surfaced as a clean question rather than a silent inconsistency | ✅ Done | `docs/design/03-data-construction-contracts/07-cross-system-data-layer-design.md` |
| H26 | Coding Standards and Patterns Design. **Verified 2026-08-07 — Layer 3 now 8/9, with only the cross-layer H45 exception outstanding:** all 12 sections present, closing order correct; grep-confirmed zero domain leakage; **the Low grade was held honestly at 221 lines** — brevity treated as correct rather than as a shortfall, matching the `09-licensing-and-dependency-compliance-design.md` precedent, and the document explicitly states no further ADR is warranted rather than manufacturing one; **the checklist trap was avoided** — all fourteen §7 rows classified Mechanical/Partial/Review-only and mapped to the mechanism already owning each, with nothing added to `02-ci-cd-pipeline-spec.md` §5/§7's closed lists; the dependency-direction, component-attribution, builder/built-separation and region-agnostic-core patterns are realized **entirely by reusing ADR-014's existing dependency-cruiser mechanism**, the single extension resting on `03-architecture-realization-design.md` §12's own handover disclaiming "the concrete lint configuration"; the canonical-vocabulary check is the session's genuine contribution — a denylist generated from `02-domain-glossary.md`'s own synonym columns, regenerated on glossary change, and **honestly bounded** to synonyms the glossary has already documented; **reuse-vs-rebuild is stated as a judgment no tool can decide in general**, with duplication detection named assistive only — exactly the false assurance the ticket warned against, refused; every named tool's maintenance status **verified, not reasoned**, per §12.3; the new `standards-check` type carries explicit grounds distinguishing ADR-014's *static source-graph proof* from `01-invariant-enforcement-design.md`'s *runtime* evaluation of the same invariants, plus an imperfect-fit disclosure in the established register — a real determination, unlike §1l's scoping-accident case; self-review caught two internal defects before close, including a citation to a subsection of this document that does not exist. **One finding — `BACKLOG.md` §1m:** ADR-035 carries nine of the convention's ten fields; `Context` is absent — the first omission in fourteen ADRs, a convention breach rather than a content defect, since question, criteria, alternatives and consequences are all present and substantive | ✅ Done | `docs/design/03-data-construction-contracts/08-coding-standards-and-patterns-design.md` |
| H26a | **Amendment (consolidated)** — Entity-Model Obligations from Layer 3. **Verified 2026-08-07 — closes `BACKLOG.md` §1j and §1h in full:** top-level sections confirmed unchanged at 1–16, only subsections added (§3.5, §4.4, §9.5–§9.7, §13.2), so no inbound-citation sweep was owed — checked and confirmed unnecessary rather than skipped, on the one library whose known failure mode is section insertion; **the consolidation earned itself** — §9.7 resolves the interaction the two obligations create, finding the fourth check's precondition (an end-user role reaching content through a construct) is *never satisfied* by an automated process step, so it is **inapplicable there rather than bypassed**, while interaction steps and human-task completion do trigger it; **§1m's caution was applied one ticket after it was raised** — ADR-036 carries all ten convention fields including `Context`; the fourth check reuses `01-application-construction-design.md`'s existing behavioral-binding mechanism rather than minting a grant table, runs last in the fixed order, and treats an absent narrowing as the builder's own choice of unrestricted reach rather than a denial; §9.6 states non-overlap with `03-data-administration-design.md` §5.4 on actor class and trigger point; **zero new event types minted** — `authority-refusal` extended with a further `source_mechanism` value, explicitly avoiding a third instance of §1l's redundant-type risk; obligation 1 correctly took **no** new ADR, amending ADR-028 in place on H21a's precedent; honest limit retained — how a Surface's content is bound to entity data in the first place remains unowned. **One finding disclosed by the Executor and confirmed sharper than reported — `BACKLOG.md` §1o:** three siblings still enumerate the Gateway as three checks, and `03-data-administration-design.md` §5's ADR-030 criterion 4 explicitly *rejected* "folding a fourth step into the Gateway" — not a contradiction, since §9.6/§9.7 govern different actor classes, but a reader meeting that criterion without §9.6 would reasonably conclude otherwise | ✅ Done | `docs/design/03-data-construction-contracts/02-data-model-and-entity-design.md` *(amended in place)* |
| H26b | **Amendment (consolidated)** — Platform-Schema Obligations from Layer 3. **Verified 2026-08-07 — closes `BACKLOG.md` §1k and §1n:** sections confirmed unchanged at 1–14, no renumbering, no citation sweep owed — the third consecutive amendment to the project's only Brutal-graded document to extend in place rather than insert; both tables landed in §3.2; **the consolidation earned itself again** — ADR-037 determines the two structures are **genuinely distinct**, neither a specialization of the other, with the asymmetric cases named on both sides (a marketplace connector holds a connection row with no registration row; an Extender-authored extension with no external reach holds the reverse), composing only where a connector is Extender-authored and then by a plain, origin-discriminated value rather than an enforced key, since the column must also hold identifiers resolving to no row in this schema — the identical discipline `administrative_scope_grants.entity_id` already follows; **ADR-037 was judged warranted rather than declined**, correctly distinguishing this from H20b's no-ADR case: neither handing document states any relationship between the two obligations, so this document is the first positioned to determine one; all ten fields present including `Context`; the credential composes with the existing tenant key rather than adding a `platform.encryption_keys` row or touching its `scope_type` vocabulary; **the H21a consistency check was run and found necessary here** — §3.4's per-tenant cell, §5's Extender row, §12's boundary bullets and §14's raw-key-material rule were each corrected, rather than assumed unaffected; a scope slip citing an unauthorized section was caught in self-review and corrected against a full cross-reference sweep. **Supplies direct evidence for `BACKLOG.md` §1l without pre-empting H26c's call** — see §1l-narrowing | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md` *(amended in place)* |
| H26c | **Determination + correction** — Event-Type Reconciliation and ADR-035's Missing Field. **Verified 2026-08-07 — closes `BACKLOG.md` §1l and §1m:** no renumbering in any of the three amended documents; **§1l determined as two distinct types, kept, not collapsed** — and the reasoning is the right one: `authority-refusal`'s three extensions all record *one identical fact* (a refusal for exceeding a resolved grant) recurring across mechanisms, safely carried by a discriminator, whereas `extension-registration` and `connector-registration` record *two different definitional facts* with non-overlapping content, each occurring independently of the other per ADR-037's structural finding; it cites `08-audit-and-traceability-design.md` §4.3's own `basis-refusal`-vs-`authority-refusal` precedent for preserving independence, and correctly reads §4.1's rationale as asking for **one record shape, not one label per structurally distinct fact** — both types already share that shape; **no ADR recorded**, correctly, this being a reconciliation applying existing library discipline rather than a new decision; ADR-035's `Context` restored in the exact field slot ADR-033 and ADR-034 use, with no other field touched and status unchanged; the two unrelated items kept in separate documents with no shared rationale, as instructed. **Prompted the register sync below** — its handoff noted `ADR-REGISTER.md` was stale, which proved to be by **fourteen** ADRs | ✅ Done | `06-integration-and-extensibility-design.md` §11; `07-cross-system-data-layer-design.md` §7; `08-coding-standards-and-patterns-design.md` §9 *(all amended in place)* |
| H27 | Application Runtime and Lifecycle Design. **Verified 2026-08-07 — opens Layer 4:** all 14 sections present, closing order correct; grep-confirmed zero domain leakage; **the fixed deployment shape was not reopened** and is explicitly denied at §3.4, §7.5 and in the Binding Rules — the ticket's primary risk, since a runtime is where a per-application process is easiest to assume; **the Construct Invocation Point dispatches only on closed-vocabulary shape, never on domain content**, which is what keeps one runtime identical across every application; **no fifth effect mechanism** — an invocation routes to exactly one of the four already-fixed paths (Entity Access Gateway, workflow engine via an Action step, Extension Invocation Point, Connector Resolver), never a runtime-owned one; **lifecycle continuity is structural rather than procedural** — three identity anchors that never change, and the same catalog rows read and written at every stage, so there is no export, snapshot, or compiled artifact to drift between stages, which is what makes C-08 a mechanism rather than a list; the publishing boundary was checked against C-10's own wording and holds — that capability is "the means to publish built software so its intended end users can **reach** and use it," so "publishing extends reach, never re-materializes the runtime" tracks it exactly; **this is the first document to exercise ADR-036's fourth Gateway check in the register it was built for**, and it fits without strain; **no new event type minted** — with the sharper judgment that a Behavioral binding's condition failing is *built-application business logic* and would leak into platform evidence if recorded, which is a distinction no prior ticket had to draw; **ADR-038 recorded** with all ten fields including `Context`, and **registered in `ADR-REGISTER.md` at this close — the first ADR registered under `PROCESS.md` §3's new rule** rather than caught in a later sweep | ✅ Done | `docs/design/04-operation-reach-distribution/01-application-runtime-and-lifecycle-design.md` | `docs/design/04-operation-reach-distribution/01-application-runtime-and-lifecycle-design.md` | `01-application-construction-design.md` | ⬜ Pending |
| H28 | Builder-Facing Environment Management Design (C-23). **Verified 2026-08-07:** all 14 sections present, closing order correct; **both vocabulary collisions named and fixed at §3**, before any mechanism is described — the platform's Development→Staging→Production tiers against C-23's Development→Testing→Production stages, and this document's "stage" against `01-application-runtime-and-lifecycle-design.md` §7's different axis of the same word; **the central tension is resolved without breaking the constraint H27 placed on it** — sibling per-application schemas under one unchanging identity, with `platform.applications.schema_name` never reassigned and no second application row created; **the composition check that mattered most passes** — a request's stage is *resolved once at the existing Context Resolution Point*, extending the Brutal-graded scoping mechanism rather than paralleling it, and §2 explicitly disclaims owning the Registry Accessor, the Context Resolution Point, or connection scoping; a promotion moves **configuration only, never entity instance data**, which is what makes per-stage separation real at the data level rather than only the schema level; the residency question was answered rather than left unstated — not a cross-region movement, since every stage of one application shares one `region_of_record` — with an honest limit if `08-multi-region-distribution-design.md` ever differentiates stages by region; the platform's CI/CD gates are bounded against with **zero shared code path, gate, or evidence record**, avoiding the ADR-022 shape; one new event type `stage-promotion`, checked against all six authorized Evidence sections first; **ADR-039 recorded** with all ten fields at **Moderate**, matching the map's pre-assigned grade rather than inflating it, and **registered in `ADR-REGISTER.md` at this close** under `PROCESS.md` §3's rule. **One handover — `BACKLOG.md` §1p:** a per-tenant stage-schema mapping plus an `application_metadata` extension recording which stage a schema realizes; the second half closes a real self-verification gap, since a same-application/wrong-stage mis-scoping would otherwise pass the existing check undetected | ✅ Done | `docs/design/04-operation-reach-distribution/02-builder-facing-environment-management-design.md` | `docs/design/04-operation-reach-distribution/02-builder-facing-environment-management-design.md` | `01-application-runtime-and-lifecycle-design.md` | ⬜ Pending |
| H29 | Builder-Facing Version Control Design (C-21). **Verified 2026-08-07:** all 16 sections present, closing order correct; **three vocabulary collisions handled at §3 before any mechanism is described** — "stage" reused unamended from H28 rather than redefined, "provenance" split from `05-ai-tooling-security-design.md` §5's AI-suggested/builder-approved sense, and "release" declared to name **no third artifact**, discharged by version+stage, which pre-empts a collision rather than resolving one after the fact; **the safe-revert problem was solved by composition exactly as intended** — a definitional revert is an ordinary new-catalog-row proposal evaluated by the Validation Engine's *already-recorded* `backward_compatible_with_prior` / `in_flight_compatible_with_prior` determinations, never re-derived, and a backward-incompatible revert is refused the ordinary path and routed to the managed migration path rather than permitted silently; **it also declined a tempting wrong answer** — routing such a revert to `held-pending-approval`, reasoned as misrepresenting a multi-step scheduled process as a single toggle; **H28's deferred demotion is resolved rather than left hanging** — reverting a stage is within this revert model, so no cross-schema demotion mechanism is introduced; version and stage are orthogonal, and the design adds **no obligation to the promotion mechanism it does not own**; **§9's honest limit is the strongest thing in the document** — a promotion never preceded by a captured version leaves nothing to restore, and it names why precisely ("not a refusal this mechanism issues; it is the absence of a target a refusal could even name"), then declines to fix it by obliging another document's mechanism; **ADR-040 recorded** with all ten fields at **High**, reasoned as High-not-Moderate because captured history is a non-re-derivable past record rather than an index of current state, and **registered in `ADR-REGISTER.md` at this close**; self-review caught a real misattribution (insert-only privilege *provisioning* wrongly attributed to a document that supplies structure, not enforcement). **Two findings — `BACKLOG.md` §1q** (a per-tenant version-history structure, the fifth obligation on `02-platform-data-model-design.md`, and notably needing a Development row where H28's stage-schema table needs none) **and §1r** (one domain-flavored word at §9 — "discount" — the library's first in 24 documents, a one-word fix and a drift signal rather than a pattern) | ✅ Done | `docs/design/04-operation-reach-distribution/03-builder-facing-version-control-design.md` | `docs/design/04-operation-reach-distribution/03-builder-facing-version-control-design.md` | `01-application-runtime-and-lifecycle-design.md` | ⬜ Pending |
| H30 | Publishing and Delivery Design (C-10). **Verified 2026-08-07:** all 17 sections present, closing order correct; grep-confirmed zero domain leakage — H29's single slip did not propagate; **the document opens with what publishing is *not* (§3)**, the right shape given H27 and H28 had already removed most of what the word conventionally implies; **publishing reduces to exactly one fact** — a per-application status checked once per request, downstream of the Authentication Gate and Context Resolution Point and upstream of the Entity Access Gateway; **the gate is explicitly not an authorization decision** and applies only where the resolved actor holds an end-user persona, never a builder-persona role whose reach is already governed by its own grant — so neither the access-binding model nor ADR-036's fourth check is re-decided; an unauthenticated party can reach a published application exactly where a construct's binding admits the Public Consumer role, composing with the Authentication Gate's already-fixed treatment rather than adding an exemption; **genuinely-empty and failure paths are distinguished** — a published-but-unconfigured application is an ordinary successful empty response, an unpublished one a refusal disclosing nothing about why; withdrawal clears the status only and cannot stop the runtime, consistent with H27's fixed model; the multi-region boundary is stated bidirectionally with no routing logic performed or anticipated; one event type with a `result` discriminator rather than two, applying H26c's test; **ADR-041 recorded** with all ten fields at **Moderate**, and **registered in `ADR-REGISTER.md` at this close**; self-review caught real citation-scope violations, a misattribution, and a missing boundary entry. **One finding — `BACKLOG.md` §1s:** the sixth constrain-and-hand-over obligation, and **the first to decline a precedent on grounds** — a column on `platform.applications` rather than a third table, reasoned from publication status being a single current-state fact with none of the history obligation that made §1p's and §1q's structures tables | ✅ Done | `docs/design/04-operation-reach-distribution/04-publishing-and-delivery-design.md` | `01-application-runtime-and-lifecycle-design.md` | ⬜ Pending |
| H30a | **Amendment (consolidated)** — Platform-Schema Obligations from Layer 4. **Verified 2026-08-07 — closes `BACKLOG.md` §1p, §1q and §1s:** sections confirmed unchanged at 1–14, everything extended in place across §3.1–§3.4, §10, §12 and §14, so no inbound-citation sweep was owed — the **fourth consecutive** amendment to the project's only Brutal-graded document to avoid insertion; **the consolidation was treated as one design, not three additions** — §3.2 closes with an explicit "these three obligations, designed with sight of each other" passage, and the differences that only a joint view exposes are all handled: the stage-schema mapping takes **no** Development row while version-history takes **all three**, and publication stays a **column** rather than being normalized into a third table for consistency's sake, honoring H30's own reasoned departure; **the isolation-adjacent half landed** — `application_metadata` gains a `stage` column fixed at materialization, closing the same-application/wrong-stage gap the self-verification read could not previously distinguish; **the question no source document asked was answered** — publication is per-application and *is* consistent with the Production-only end-user resolution today, because Production is the only stage an end-user request reaches, stated as a **bounded finding rather than a permanent guarantee** with the condition for revisiting named; **ADR-042 recorded** for exactly that finding on the H26b pattern rather than H20b's no-ADR pattern, all ten fields, and **registered at this close**; the manifest was normalized into a child table with the distinction from the two existing array-typed columns stated explicitly so it does not read as an inconsistency; §3.4's boundary table and three new §12 bullets added, none of which existed before; self-review caught a substantive error (`schema_name` wrongly described as updated in place) plus two completeness gaps | ✅ Done | `docs/design/01-design-foundation/02-platform-data-model-design.md` *(amended in place)* |
| H30b | **Correction pass** — Gateway-Arity Staleness and One Domain Term. **Verified 2026-08-07 — closes `BACKLOG.md` §1o and §1r, emptying the backlog:** both stale disclaimers now name the End-User Record-Reach Check alongside the other three; **ADR-030 criterion 4 was corrected the right way** — the arity claim restated in the past tense ("the Gateway's step order **was fixed at three steps**"), preserving the reasoning as historically accurate rather than rewriting it, with one appended clause citing ADR-036, §4.4 and §9.6 for the fourth step's later adoption on different grounds; **ADR-030's decision and its criterion's conclusion are untouched and not reopened**; the domain-flavored member at `03-builder-facing-version-control-design.md` §9 was replaced with a structural one ("an effect a Command's own invocation once produced"), parallel to the retained member — **library-wide sweep now returns zero occurrences, restoring domain-neutrality to zero across all 25 documents**; **the two correct-in-context sites were correctly left alone** — verified untouched, which was this ticket's specific over-correction risk, since asserting the fourth check fires where its end-user precondition cannot be met would have replaced a harmless stale list with an actively wrong one; no ADR recorded, as expected | ✅ Done | `03-data-administration-design.md`; `04-workflow-and-process-automation-design.md`; `03-builder-facing-version-control-design.md` *(all amended in place)* |
| H31 | Mobile Application Delivery Design (C-20). **Verified 2026-08-10 — discharges the phase's longest-standing owed ADR:** all 14 sections present, closing order correct; grep-confirmed zero domain leakage; **ADR-017 recorded under its reserved identifier**, not the next free one — the single most consequential mistake this ticket offered, avoided; all ten fields including `Context`, graded Moderate with independent boundary reasoning rather than by instruction alone; **D-25's withheld product landscape was supplied against re-verified evidence** (2026-08-10) rather than carried forward from its 2026-08-03 findings — including that the standardised query library still does not deliver write durability by default, and, notably, an **explicit statement that RTK Query's own offline behavior was not independently re-verified** rather than assumed identical, which is the discipline ADR-009 exists to enforce; **the central offline problem was answered rather than papered over** — a queued write has cleared *no* server-side check, so every queued payload is encrypted at rest and treated as maximally sensitive by default, and Jurisdiction-Restricted/Regime-Governed content is never persisted locally at all; three honest limits stated, including one no prior document could have raised — **no mechanism can detect or control where a device physically carries cached content once served**, which is the residency guarantee meeting a boundary it cannot cross; the write queue carries a terminal outcome vocabulary so a refusal discovered on reconnect surfaces rather than silently dropping; **no storage obligation handed over — a deliberate, stated finding** and the first document to check `02-platform-data-model-design.md` under §1i's rule and correctly conclude it owes nothing, since all mechanism is client-resident and one binary serves every tenant; no new event type, a dequeued write producing exactly the evidence the unmodified server path already produces; self-review caught three citations exceeding authorized scope. **`ADR-REGISTER.md` Owed count now zero.** One thing surfaced for the lead: ADR-017's `Approved` status rests on a **team** `DECISIONS.md` entry under D-16, a third status basis distinct from both lead approval and H10–H12's self-approval — recorded in live issue 5, which now spans all three | ✅ Done | `docs/design/04-operation-reach-distribution/05-mobile-application-delivery-design.md` | Mobile Application Delivery Design (C-20) | `docs/design/04-operation-reach-distribution/05-mobile-application-delivery-design.md` | `04-publishing-and-delivery-design.md` | ⬜ Pending |
| H32 | Marketplace Design (C-13). **Verified 2026-08-10 — the hardest structural problem in Layer 4, solved:** all 14 sections present, closing order correct; grep-confirmed zero domain leakage on the subject most likely to invite an example offering; **the cross-tenant problem is resolved as "a deposit, not a door"** — an offering tenant deposits an immutable copy into platform-global, tenant-independent storage using only its own connection, and an obtaining tenant activates it through its own pre-existing mechanism, with obtaining and activating held as two distinct acts the marketplace itself never performs; **checked explicitly against all three of `02-tenant-isolation-and-access-control-design.md` §8's structural facts** — no naming path, no grant path, no silent path — rather than asserting isolation held; **the independent corroboration is the strongest thing here** — the same split is the only shape the fixed component dependency-direction table permits (Reach→Operation allowed, Reach→Construction/Extension not), so isolation and architecture arrive at the identical mechanism by separate routes; **the trust determination is per artifact shape rather than uniform**, with the deciding criterion being *where the artifact executes* rather than authorship or persona — a published application takes no isolation mechanism because the extension boundary's trigger is never met, a module adopts the WASM sandbox in full under the obtaining tenant's own registration and grant, and an SDK-distributed component composes with the existing External Contract Consumer model; it **engages H24 §10's own argument directly, partially affirming and partially declining it** rather than accepting or ignoring it; the mandatory security review is correctly identified as genuinely new — no platform-team baseline exists to gate against — and composes via a second `source_mechanism` on the existing `review-trigger` type rather than a new severity scale; **one platform-global table constrained, with a reasoned finding that no per-tenant table is owed**; honest limit stated — withdrawing an offering never reaches an instance already obtained and activated, since no structural path exists to retract it; **ADR-043 recorded** with all ten fields and **registered at this close** | ✅ Done | `docs/design/04-operation-reach-distribution/06-marketplace-design.md` | Marketplace Design (C-13) | `docs/design/04-operation-reach-distribution/06-marketplace-design.md` | `04-publishing-and-delivery-design.md`; `06-integration-and-extensibility-design.md` | ⬜ Pending |
| H33 | Connector Marketplace Design (C-25). **Verified 2026-08-10 — closes both questions `06-marketplace-design.md` §9 left open:** all 14 sections present, closing order correct after a self-review renumbering that caught Evidence placed after the ADR; grep-confirmed zero domain leakage, no external product named; **the credential boundary is drawn exactly, and goes one step further than the ticket asked** — guest code builds only protocol content, a host function resolves the tenant's own connection row, attaches the credential, and dispatches; **the destination is host-resolved and never guest-suppliable**, closing an exfiltration-via-redirection vector the existing host-side-authentication discipline does not foreclose on its own wording; what is necessarily exposed is stated unmodified from `07-cross-system-data-layer-design.md` §4.2, and the data-flow/intent residual is inherited rather than re-closed; **Question 1 answered by reapplying the method, not inheriting the conclusion** — isolation is unmodified because the four criteria resolve identically, and the existing review trigger already reaches every connector by definition, so the one genuine addition is a single deterministic manifest-inspection item inside the existing check rather than a new trigger or scale; **Question 2 answered** — a separate platform-global catalog rather than a fourth `shape` value, partly because extending another document's enum is not this ticket's edit to make; storage is a **hybrid of the two recent precedents** — one platform-global structure like H32, no per-tenant structure like H31, since activation composes with `extension_registrations` and `external_system_connections` already in place; **the architectural cross-check was run in the spirit of H32's convergence move** — C-25 sits in `components/extension`, and because a connector artifact is always opaque and never tenant-schema-resident, the deposit needs no Construction dependency and adds no dependency edge; **ADR-044 recorded** with all ten fields and **registered at this close**. **One finding, and it is the Orchestrator's — `BACKLOG.md` §1t:** this ticket's prompt misattributed a six-constraint list to `07-cross-system-data-layer-design.md` §4.3; those constraints belong to `04-workflow-and-process-automation-design.md` §4.3 and govern a different decision shape. The Executor caught it, raised it rather than proceeding, and the substance of its objection was correct | ✅ Done | `docs/design/04-operation-reach-distribution/07-connector-marketplace-design.md` | Connector Marketplace Design (C-25) | `docs/design/04-operation-reach-distribution/07-connector-marketplace-design.md` | `06-integration-and-extensibility-design.md`; `06-marketplace-design.md`; `07-cross-system-data-layer-design.md` | ⬜ Pending |
| H34 | Multi-Region Distribution Design (C-14) — **closes Layer 4**. **Verified 2026-08-11:** all 16 sections present, closing order correct (Evidence §12 → ADR §13 → Boundaries §14 → Precedence §15 → Binding Rules §16); domain-neutrality grep clean — zero jurisdiction, regime, provider or region identifier; **the placement-unit question was decided rather than assumed from either existing column** — tenant-scoped, on the ground that only `tenant_host_connections` exists to record it and application schemas are already nested under their tenant's schema name, with `platform.applications.region_of_record` correctly reclassified as an *obligation input to the Resolved Residency Obligation's union*, never a placement instruction; **`02-builder-facing-environment-management-design.md` §9's finding confirmed unamended** — the condition that document named this one as able to trigger, answered plainly rather than left open; **the second read path is genuinely justified, not asserted** — the Registry Accessor's "already-authenticated actor identity… never a client-supplied `tenant_id`" signature was quoted accurately against §3.1 and shown to structurally forbid pre-authentication, address-keyed routing, so reuse would weaken its guarantee rather than compose with it; correctness rests on the platform's own region-resident redirect, never on a provider-unique global-routing product, keeping any geo-DNS hint a non-load-bearing performance convenience inside ADR-010's subset; region admission reuses the existing Production Deploy Gate residency clause with no second gate, discharging G-4 by composition; **no new schema structure owed** — checked candidate-by-candidate, with the one infrastructure element (a regionally-resident registry replica) handed to Layer 5 as provisioning rather than schema; one new event type `locality-resolution-refusal` under the H26c test, distinct from `residency-refusal`; **ADR-045 recorded** with all ten fields, cost High reasoned independently (reversal is a governed cross-region transfer, not a redeploy), and its register row **reconciled at this close** against the owning document, having been pre-written from the superseded draft. **One finding — `BACKLOG.md` §1u:** the Region Resolution Point reads `region_of_record` from `components/distribution`, which `02-tenant-isolation-and-access-control-design.md` §5.3 forbids at Full coverage for `platform.tenants`/`platform.applications`; §8 checks the architecture-overview forbidden direction and §4.1's five rules but never mentions §5.3's sixth — correct about what it checked, silent about what it collides with. Three resolutions exist; none is H34's alone to take | ✅ Done | `docs/design/04-operation-reach-distribution/08-multi-region-distribution-design.md` | `02-tenant-isolation-and-access-control-design.md`; `01-application-runtime-and-lifecycle-design.md`; `06-compliance-and-data-residency-design.md` | ⬜ Pending |
| H35 | Environment and Configuration Design — **opens Layer 5**. **Verified 2026-08-11:** all 15 sections present, closing order correct (Evidence §11 → ADR §12 → Boundaries §13 → Precedence §14 → Binding Rules §15); domain-neutrality grep clean, provider product names appearing only where ADR-010/ADR-021 already establish that convention, never as a correctness dependency; **all four named provisioning obligations discharged, each honoring its own arriving constraint** — the per-region replica via logical replication from one designated write-authority region plus a parameterized IaC module, the audit anchor as object storage with write-once retention bound to the **≥13-month floor verified against `06-non-functional-requirements.md` §10**, the KMS credential as **an explicit non-decision** (one further instance of §5's general mechanics, stated as such rather than dressed as a mechanism), and the per-provider KMS resolution as data populating `platform.host_providers`'s already-generic portable-service-types column — **confirmed genuinely generic in the schema, so no new structure was needed**; **the circularity argument is the strongest reasoning here** — every candidate for storing tier/region self-identity resolves to "not owed" because a database-stored answer to "which region am I" is circular, explicitly modeled on H34 §6.2's own reasoning; Production's protection realized at infrastructure level (a structurally singular credential-bearing path) that **composes with rather than substitutes for** `02-ci-cd-pipeline-design.md`'s gates, correctly declining to design gate mechanics it does not own; one new event type `secret-injection-refusal` under the H26c test, landing in the existing Security events category; **ADR-046 recorded** with all ten fields, cost Moderate reasoned independently from ADR-010 §20.1's own "the application is portable, the infrastructure is not"; **self-review caught two real defects** — an unsourced claim that a provider's hardware-backed HSM tier is required by the key-custody model (`04-security-controls-design.md` §7.3 verified to state no such requirement) and a misleading internal self-reference. **One finding, and it is the Orchestrator's — `BACKLOG.md` §1t, fourth row:** this ticket's prompt cited `04-security-controls-design.md` "§10's Consequences" for the KMS obligation; §10 is that document's Precedence section and carries no Consequences field — the content is ADR-021's own Consequences at §8.1. The Executor located it, corrected it, and cited correctly, exactly as §1t's rule and the prompt's own halt-and-say-so guard intend | ✅ Done | `docs/design/05-infrastructure-deployment-operations/01-environment-and-configuration-design.md` | `03-architecture-realization-design.md`; `01-technology-stack-design.md` | 🟢 Ready — no unwritten prerequisite |
| H36 | CI/CD Pipeline Design. **Verified 2026-08-11:** all 15 sections present, closing order correct; domain-neutrality grep clean and **no CI/CD product named anywhere** — correctly declining to decide one, since no upstream ADR fixes it and deciding would need criteria this ticket did not carry; **the consolidated inventory was actually built** — nine checks placed and ordered at the Merge Gate across the ten documents that attach to it, which was this ticket's defining task and the thing a spec-only reading would have missed; **one genuine cross-document ordering dependency found and stated as a reconciliation** — the mandatory security-review trigger must be evaluated *after* vulnerability classification because `04-security-controls-design.md` §5.2's own severity fold-in depends on it, a fact neither owning document could have stated without sight of the other's placement; **the diff-based/re-run distinction is the sharpest reasoning here** — source-level checks run once at Merge and never re-run at a Deploy Gate because the promoted artifact is bit-for-bit unchanged (H35 §3.2), while vulnerability scanning re-runs at every Deploy Gate because its input genuinely changes; **the provenance-gate check is placed but flagged, not forced** — `05-ai-tooling-security-design.md` §8.1's Consequences claims to bind spec §5's closed list, which a design document cannot do, and this is exactly `ADR-REGISTER.md` **live issue 6**, verified to say precisely that and to route to a spec-phase ticket "in the shape D-23 already set"; the document places the check where ADR-022 fixes it and states the gap rather than absorbing it into an ill-fitting bullet or minting one itself; **no new event type — all eight reused, and all eight verified to pre-exist**, two of them (`standards-check`, `contract-breaking-change-detected`) in their own owning documents rather than the audit table; no new schema structure, on H35 §9's circularity reasoning extended to pipeline definition; **the builder-facing boundary was confirmed rather than assumed** — `02-builder-facing-environment-management-design.md` §8 quoted accurately from its own side, and nothing placed on either document's behalf; **ADR-047 recorded** with all ten fields, cost Low, and **registered at this close**; self-review caught a citation conflating that document's Boundaries section with its Consequences field. **One finding, and it is neither this ticket's nor any Executor's — `BACKLOG.md` §1v:** verifying the "all eight reused" claim exposed that `08-audit-and-traceability-design.md` §4.3 is a point-in-time reconciliation of five documents' rows, and at least ten later event types were never entered into it — structurally the same failure as live issue 8, and it degrades the H26c test every Executor applies | ✅ Done | `docs/design/05-infrastructure-deployment-operations/02-ci-cd-pipeline-design.md` | `01-environment-and-configuration-design.md` | ⬜ Pending |
| H37 | Testing and Quality Gates Design | `docs/design/05-infrastructure-deployment-operations/03-testing-and-quality-gates-design.md` | `02-ci-cd-pipeline-design.md`; `04-scalability-availability-and-performance-design.md` | ⬜ Pending |
| H38 | Observability and Monitoring Design | `docs/design/05-infrastructure-deployment-operations/04-observability-and-monitoring-design.md` | `01-application-runtime-and-lifecycle-design.md`; `01-environment-and-configuration-design.md` | ⬜ Pending |
| H39 | Release and Rollback Design | `docs/design/05-infrastructure-deployment-operations/05-release-and-rollback-design.md` | `02-ci-cd-pipeline-design.md`; `03-testing-and-quality-gates-design.md` | ⬜ Pending |
| H40 | Incident Response and Recovery Design | `docs/design/05-infrastructure-deployment-operations/06-incident-response-and-recovery-design.md` | `04-observability-and-monitoring-design.md`; `05-release-and-rollback-design.md` | ⬜ Pending |
| H41 | Agent Runtime and Control Design | `docs/design/06-autonomous-agent-implementation/01-agent-runtime-and-control-design.md` | `03-architecture-realization-design.md`; `01-invariant-enforcement-design.md` | 🟢 Ready — no unwritten prerequisite |
| H42 | Token and Compute Budget Design | `docs/design/06-autonomous-agent-implementation/02-token-and-compute-budget-design.md` | `01-agent-runtime-and-control-design.md` | ⬜ Pending |
| H43 | Human-in-the-Loop Design | `docs/design/06-autonomous-agent-implementation/03-human-in-the-loop-design.md` | `01-agent-runtime-and-control-design.md` | ⬜ Pending |
| H44 | Prompt and Context Assembly Design | `docs/design/06-autonomous-agent-implementation/04-prompt-and-context-assembly-design.md` | `01-agent-runtime-and-control-design.md` | ⬜ Pending |
| H45 | AI-Assisted Builder Tooling Design (C-19) | `docs/design/03-data-construction-contracts/09-ai-assisted-builder-tooling-design.md` | `01-application-construction-design.md`; `02-data-model-and-entity-design.md`; `05-ai-tooling-security-design.md`; `01-agent-runtime-and-control-design.md`; `03-human-in-the-loop-design.md`; `04-prompt-and-context-assembly-design.md` | ⬜ Pending |
| H46 | Agent State and Memory Design | `docs/design/06-autonomous-agent-implementation/05-agent-state-and-memory-design.md` | `01-agent-runtime-and-control-design.md`; `07-data-governance-and-privacy-design.md` | ⬜ Pending |
| H47 | Self-Correction and Fallback Design | `docs/design/06-autonomous-agent-implementation/06-self-correction-and-fallback-design.md` | `01-agent-runtime-and-control-design.md`; `05-agent-state-and-memory-design.md`; `04-observability-and-monitoring-design.md`; `05-release-and-rollback-design.md` | ⬜ Pending |
| H48 | Change Management and Evolution Design | `docs/design/06-autonomous-agent-implementation/07-change-management-and-evolution-design.md` | `01-agent-runtime-and-control-design.md`; `06-self-correction-and-fallback-design.md`; `05-api-contract-design.md` | ⬜ Pending |

**Reading the Status column.** `⏳ Next` is the ticket about to run. `🟢 Ready` marks documents whose design-doc prerequisites are already written — they do not block, and are not blocked by, tenant-isolation; any could run in parallel or be pulled forward. `⬜ Pending` means at least one prerequisite above it in this table is still unwritten. **This table is a schedule, not a strict must-follow-this-order mandate** — the real constraint is each row's own Depends On column, cited here from the map, never restated.

---

### Criteria-library tickets (`CR##`) — new series, 2026-08-03

The third library, established by `DECISIONS.md` **D-21** (lead decision). Folder confirmed as `docs/criteria/`. **`CR##` tickets invoke no phase writing-rules or review skill** — see `PROCESS.md` §1 for why, and which of the 12 steps they skip.

| # | Ticket Title | Status | Output File |
| --- | --- | --- | --- |
| CR01 | Establish the criteria library and its index — scope, admission test, the four boundaries, conventions, and the forward-looking index | ✅ Done | `docs/criteria/criteria-document-map.md` |
| CR02 | `technology-stack-selection-criteria.md` — distil the reusable stack-selection criteria from the worked instance. **Verified:** admission test passes with zero project leakage; condition method generalized to unnamed candidate *classes*, which ages better than named stacks | ✅ Done | `docs/criteria/technology-stack-selection-criteria.md` |
| CR03 | `workflow-engine-tool-opinion.md` — standing position on BPMN-conformant workflow engines (`DECISIONS.md` D-20). **Verified:** admission test clean; structure position-shaped (position → reasoning → cost-to-reverse → constraints → dimensions → dated survey), visibly distinct from CR02's criteria-set form; two survey claims independently spot-checked (Apache KIE incubating status, Camunda License 1.0) and both held | ✅ Done | `docs/criteria/workflow-engine-tool-opinion.md` |

> **CR01's judgment call — decided: reading (ii).** The library holds **distilled, reusable, client-portable sets**; the three dated files stay outside it as source material, unmoved. The deciding argument went further than the ticket's: reading (i) has **no natural stopping point** — once one dated, project-specific file is admitted on the strength of its content, every future meeting record has the same claim, and the library accumulates exactly the process exhaust D-21 ruled out. *Original framing retained below.*
>
> **CR01's substantive judgment call.** The three existing root files — `REVIEW-QUESTIONS-2026-07-30.md`, `REVIEW-FLAGS-2026-07-30.md`, `STANDUP-BRIEF-2026-08-03.md` — are **dated internal meeting instruments** and **fail D-21's own admission test** (criterion 2: *does it survive being handed to a client?*). CR01 must decide whether the library holds those instances directly, or holds **distilled, reusable sets** with the dated files remaining source material outside it. **Recommended: the latter** — a library justified on client-portability that opens with three un-portable meeting records contradicts the criterion that created it.
>
> **The boundary most likely to be missed:** `01-technology-stack-design.md` §9 was extended 2026-08-03 so every ADR now carries **its question and criteria as first-class fields**. That is not duplicated by this library. An ADR records criteria applied to **one decision for this platform** — the worked instance; this library holds the **reusable set** those instances distil into. Without that boundary stated, each will assume the other owns criteria.

> **Owed after CR01 lands, and not the Executor's:** `CLAUDE.md`'s folder-structure section still describes a two-library `docs/` tree, and any migration of the root files is tracker maintenance. `PROCESS.md` §1 and §3 were already updated on 2026-08-03.

---

> **⚠ `01-invariant-enforcement-design.md`'s §4.1 citations are fragile — verify before extending.** H9's self-review caught two citation errors before finalizing: INV-05 and INV-06's structural-half rows were initially swapped against `03-architecture-realization-design.md` §4.1 (row 3 is INV-06's; row 4 is INV-05's, per that document's own §4.4), and a quote blending two adjacent subsections was split and misattributed. Both were fixed, but the pattern — two invariants citing adjacent numbered rows in another document — is exactly the shape that produces a silent swap on the next edit. **Any future insertion into this document must re-verify both mappings**, not just run the usual §N sweep.


### Design-phase work identified — not yet sequenced

Sequencing waits on the data model, which the lead has made the next deliverable.

| Item | Note |
|---|---|
| **The data model** | The lead's named next deliverable, and the gate on ADR-001/008/009. Covers both the platform's own schema and the builder-entity schema. **No document in `implementation-document-map.md` is currently positioned to deliver the platform's own schema** |
| ~~**Sync-posture ADR**~~ | ✅ **Closed by H3** — recorded as ADR-011 (`01-technology-stack-design.md` §21). The spec-side interaction with T67 (temporal/append-only) remains separately tracked there. |
| ~~**ADR-004 amendment**~~ | ✅ **Closed by H3** — PostgreSQL-only for V1.0, per-application separation (finer than schema-per-tenant), and the two isolation strengths recorded in the ADR-004 record and §14.5. |
| ~~**ADR-006 amendment**~~ | ✅ **Closed by H3** — AI-to-AI protocol requirement added (identity unconfirmed, MCP/A2A); GraphQL rejection changed to Parked pending study. |
| ~~**AI-to-AI protocol identity**~~ | ✅ **Closed by H6** — recorded as **ADR-013** (`01-technology-stack-design.md` §23): MCP, first, generated from the OpenAPI contract ADR-006 already requires; A2A remains additive, not foreclosed, for a future agent-to-peer coordination requirement. |
| ~~**GraphQL research**~~ | ✅ **Closed 2026-08-03 by `DECISIONS.md` D-19 (lead decision).** Research run; it **confirmed** ADR-006's original reasoning rather than overturning it. GraphQL is **finally rejected** for the external contract tiers; an internal BFF layer stays open. **Follow-up:** ADR-006's sub-decision status must change Parked → Rejected in `01-technology-stack-design.md` — a design amendment, not yet applied |
| **Offline mobile storage engine** | **Research done 2026-08-03** (`STANDUP-BRIEF-2026-08-03.md` §2.2) — **the question is narrower than recorded here.** D-11 already made the server authoritative, so this is a *local-persistence* decision, not a sync-engine one: PowerSync/ElectricSQL/Turso solve a problem D-11 removed, and Realm is moot (EOL passed 30 Sept 2025). Recommendation: `expo-sqlite` + Drizzle, MMKV as the TanStack persister, and an **explicitly designed** mutation queue — TanStack mutations can error rather than pause when offline, so durability does not fall out of `persistQueryClient`. **ADR still owed** |
| ~~**Server-side cache handling**~~ | ✅ **Closed by H6** — recorded as **ADR-012** (`01-technology-stack-design.md` §22): deliberately deferred (no current performance driver; ADR-005's profiled-not-anticipated philosophy), with two binding constraints for whenever it arrives (never correctness-critical; stays within ADR-010's portable subset). No technology selected. |
| **Security standards** | ⚠️ **This row's original note was WRONG on the files, and is corrected here (`DECISIONS.md` D-22, lead decision 2026-08-03).** It previously read *"the spec already carries a security policy and a certification roadmap — so this is a design decision not yet made, not a spec gap."* **It is both.** Verified 2026-08-03: the specification library contains **zero** occurrences of encrypt / TLS / HTTPS / in-transit / at-rest / OWASP / ASVS — the policy covers SOC 2 and ISO 27001 attestation but states **no protection obligation at all**. Meanwhile `02-platform-data-model-design.md` §3, §8 already designs three tiers of encryption key material, so the design is realizing an obligation the spec never made — a `PROCESS.md` §1 phase inversion. **T69 authorized** (spec-phase) to state the obligations and name **ASVS 5.0** as the baseline; note the **Top 10 is an awareness document and is not testable**, so it cannot be the gate. `04-security-controls-design.md` realizes it afterwards |
| ~~**Design document for C-27**~~ | ✅ **Done 2026-08-03.** `03-data-administration-design.md` scheduled in **Layer 3** of `implementation-document-map.md`, realizing C-27; depends on `02-data-model-and-entity-design.md`, `02-tenant-isolation-and-access-control-design.md`, `03-authentication-and-identity-design.md`; **Buildable now**; cost to reverse **Moderate**. The three boundaries (vs C-05 / C-06 / C-07) are carried into its charge |
| ~~**New evaluation criteria 11–13**~~ | ✅ **Closed by H3** — added to `01-technology-stack-design.md` §2.6. Human-verifiability, commercial acceptability, and corpus **quality** (not volume). Two favour the current stack, one cuts against it. |
| ~~**Consistency-check remediation**~~ | ✅ **Closed by H3** for the items this ticket covered — ADR-008's *Consequences* corrected off Flutter, §10 corrected, the mandatory cost-to-reverse/upstream-assumed/verified-vs-reasoned fields retrofitted on all eleven ADRs, and every stale post-reversal claim (§17.2, §17.5, §18.1, §18.3, §18.5) marked superseded. `ADR-REGISTER.md` itself still needs a sync pass to reflect this closure — out of this ticket's scope. |
| **`DECISIONS.md` entries** | The five approved ADRs need recording. **`DECISIONS.md` currently holds no stack entry at all, so this would be the first binding entry in the project** — until it lands, nothing is approved in fact |
| ~~**`PROCESS.md` §1 + `CLAUDE.md`**~~ | ✅ **Closed 2026-08-02.** `PROCESS.md` §1 and §3 now record the freeze as lifted (2026-07-30), while keeping the rule that a spec change always runs as a spec-phase ticket and that an Orchestrator never edits `docs/spec/`. **`CLAUDE.md` needed no change** — it never asserted the freeze; the claim that it did was itself tracker drift. `implementation-document-map.md`'s readiness legend also de-referenced "the frozen spec" |

### Resolutions recorded 2026-07-30 (tracker pass)

| Q | Resolution |
|---|---|
| **Q1a** | Quantified NFR targets **do not apply to V1.0** — *"Does not apply to v1.0."* Recorded as **deferred as an acceptance gate, retained as a design constraint**: V1.0 need not *demonstrate* 50,000 sessions, but must not choose a partitioning that structurally precludes reaching it. **INV-01 and release gate G-1 are unaffected** — he deferred numbers, not gates, and G-1 requires isolation to hold "before anything is hosted." V1.0 is therefore **released from its dependency** on the gated `04-scalability-availability-and-performance-design.md` |
| **Q11** | Data Admin's screen vs the deferred front-end is **not a contradiction** — different layers. What was deferred is the **UI generator** for built applications; Data Admin is platform tooling. A V1.0 application is a data model plus Data Admin access, with **no end-user interface** — so C-07 and C-10 are not exercisable in V1.0, as intended |
| **Q12** | V1.0 sits **exactly on the Tier 1 / Tier 2 seam** — all five Tier 1 capabilities are in it, no Tier 2 capability is. So **no re-tiering is needed**, V1.0 stays a delivery milestone in the tracker rather than entering the PRD, and **C-27 belongs in Tier 1**. One loose end: whether V1.0 needs any of C-12 proper (Tier 3), or only the generated contract falling out of C-05 |
| **Q13** | Go fallback: **de-name it, do not close it.** Keep the principle that profiled pressure may warrant a different language for one component; withdraw the pre-commitment to Go, which the lead ruled against on grounds that hold at any scale and which was never scored under criteria 7–10. Decide the language when profiling data exists. Folds into the deferred ADR-001/008 decision |
| **Q14** | Re-evaluation happens **after the data model** — answered by the 001/008/009 deferral. Sound sequencing rather than deference: the data model produces the evidence the re-evaluation turns on. **ADR-008's closure claim is void** and its status line wrong |

> **Sizing question worth adding to the next batch.** V1.0 now has no quantified performance criteria at all, which is fine for an MVP — but one number still matters: **roughly how many customers, and how many apps each, should V1.0 handle?** Separate tables per app behave very differently at 5 customers than at 50, and it is the one figure that shows whether the partitioning shape is comfortable at launch scale.

## Strategic Pivot — 2026-07-31 Standup

**Recorded in full as `DECISIONS.md` D-15 and D-16.** This section carries only the effect on the work queue.

**The product changed.** AI ahaMatic's deliverable becomes a **library of standard documentation, specifications, and decision criteria that AI uses to build software**, plus a selected set of pre-built utilities. The platform build **continues** — as a learning exercise that informs the library, not as the product.

**What explicitly does not change**, stated by the lead: *"I don't think this should change what you're doing now"*; *"we still have to do it… I want you to build it, so we learn from that process"*; *"still keep it on version one, minimalistic, minimum viable product."* The spec and design libraries stand. V1.0 continues as scoped.

**Decision-making is delegated** (D-16). Technical and design decisions are made by the team without waiting for lead approval — on two conditions: the decision **and its criteria** are recorded, and unasked questions are **actively discovered**. Questions genuinely needing the lead are **compiled weekly for Monday**, not raised singly.

### Effect on the work queue

| Item | Effect |
| --- | --- |
| ~~**H6 — ADR-001/008/009**~~ | ✅ **Done.** Rescoped per D-15, then discharged: ADR-001 and ADR-008 are **Resolved** — a V1.0 default (Node.js/TypeScript, Next.js, React Native with Expo) plus a reusable, per-client criteria table (`01-technology-stack-design.md` §18.6–§18.10) — and ADR-009 is **Approved**, confirmed rather than reopened. The data-model evidence (`02-platform-data-model-design.md`) and criteria 11–13 close the re-evaluation ADR-003 mandated and ADR-008 wrongly closed; the `.NET`/Blazor condition table is the reusable artifact. Go's pre-naming as the extraction-fallback language is withdrawn (§7). Two further team decisions recorded alongside it: **ADR-012** (server-side caching, deferred with constraints) and **ADR-013** (AI-to-AI protocol, MCP first). |
| **ADR convention** (`01-technology-stack-design.md` §9) | Must be extended so the **question and the criteria** are first-class fields rather than supporting prose. Under D-15 the criteria are the reusable product; under D-16 a decision recorded without them is incomplete |
| **Questions-and-criteria compendium** | ✅ **Home decided 2026-08-03 — `DECISIONS.md` D-21 (lead decision): a new third top-level library under `docs/`.** Three instances exist as loose root files: `REVIEW-QUESTIONS-2026-07-30.md`, `REVIEW-FLAGS-2026-07-30.md`, `STANDUP-BRIEF-2026-08-03.md`. **Still open:** folder name (recommended `docs/criteria/`), whether it needs its own index and writing-rules skill, and the `CLAUDE.md`/`PROCESS.md` updates. **Needs a ticket** — a new `docs/` document never lands inline (`PROCESS.md` §3) |
| **Third-party tool opinions** | ✅ **Home decided — same library, `DECISIONS.md` D-21.** Named examples: email delivery, workflow engines, dashboards (**Metabase**). **The workflow-engine question is no longer open:** `DECISIONS.md` **D-20** (lead decision 2026-08-03) extends the *"buy it; do not let AI improvise one"* principle to workflow engines — C-18 adopts an existing BPMN engine rather than building one. **Camunda is not selected**; engine choice is a separate evaluation, now gated on INV-01, ADR-010's portable subset, and not becoming a second authoritative store (ADR-011) |
| **C-27 Data Administration** | **Reinforced.** Named as a utility that ships as a standard: *"we might also build the data admin… For some clients that's enough."* Supports **D-13** |
| **Generality constraint (INV-05)** | **Strengthened, not weakened.** Documentation that must apply to every client cannot encode one domain |
| **Everything else in flight** | Unchanged. T65–T69, the terminology ticket, and the Layer 2/3 sequencing all stand |

> **Propagation into `CLAUDE.md` and the charter is deliberately deferred** — see D-15. The lead expects the documentation approach itself to be revised once V1.0's learning lands: *"I'm pretty sure we're going to change the way you have done the documentation… build the tower, put the marshmallow and see what fails."* A session finding `CLAUDE.md` describing a software-builder platform should read it as accurate for the **current build**, with D-15 as the **medium-term direction**.

---

### Still open — questions and blockers

*Q1a and Q11–Q14 are answered; see the resolutions table above. What remains:*

| Item | Needs | Deadline pressure |
|---|---|---|
| ~~**🔶 Is a customer always exactly one isolated unit, or can one customer have several?**~~ ✅ **Resolved by `DECISIONS.md` D-17 and closed by H7.** "Tenant" is the canonical structural term; "customer" is commercial metadata that may map to one or several tenants, never a structural level. `02-platform-data-model-design.md`'s 188 occurrences (including the schema names `customer_<id>` and `customer_<id>_app_<id>`) were renamed to `tenant_<id>` and `tenant_<id>_app_<id>` in that ticket, along with the same drift in `01-technology-stack-design.md` §14.5/§14.7, `implementation-document-map.md`, and `ADR-REGISTER.md`. No spec change was required (D-17 criterion 1). | **The team** (D-16). | **Closed** |
| ~~**C-27's primitive family and canonical name.**~~ ✅ **Resolved 2026-08-02 (team decision, D-16 delegation) — closed by `DECISIONS.md` D-13.** Family: **Construction**, on the C-19 precedent. Name: **Data Administration** (already fixed in D-13's original text; only family was open). | **The team** (D-16). | **Closed — unblocks T65.** |
| ~~**The Flutter disclosure never happened.**~~ ✅ **Closed by H6.** ADR-009 is now **Approved** (`01-technology-stack-design.md` §19.3) — confirmed on the strength of the 2026-07-30 acceptance and not reopened, per D-16's delegation; the mobile runtime (React Native with Expo) is settled, not merely unconfirmed. | **The team** (D-16). | **Closed** |
| ~~**V1.0 sizing (Q6).**~~ ✅ **Answered by the lead 2026-08-03 — `DECISIONS.md` D-26: ~100 applications total**, roughly double the team's ≤50 working assumption. **Checked, not merely accepted:** at ~100 schemas, migration fan-out (~100 executions, ~2.4 min each within the 4-hour ceiling) and connection-pool pressure both stay comfortably inside the "theoretical, not pressing" band `02-platform-data-model-design.md` §11 already claims — this does **not** elevate `04-scalability-availability-and-performance-design.md` (H10) to a V1.0 prerequisite. **⚠ Not yet applied to `02-platform-data-model-design.md` §11** — the ≤50/≤10×≤5 figure and its "team decision, not lead-confirmed" status there are now stale and need a design-file amendment, which needs explicit user direction (`PROCESS.md` §3). H10, when written, should verify against ~100 directly rather than the current draft's abstract horizon-gap framing. | **The lead.** | **Answered — design-doc update pending.** |

> ~~**One root cause, four drift items.** The terminology question above is the same unresolved question behind three items left outstanding at the end of H3: `01-technology-stack-design.md` §14.5/§14.7 mixing "schema-per-tenant" with "per-customer/per-application"; the map's line-93 "schema-per-tenant" drift note; and the `ADR-REGISTER.md` sync pass. **Resolve the entity question and one ticket closes all four.** Resolve them separately and the term gets chosen four times.~~ ✅ **All four closed by H7** (`DECISIONS.md` D-17): `01-technology-stack-design.md` §14.5/§14.7 normalized to "tenant," the map's line-93 row normalized, and `ADR-REGISTER.md` synced.


### Loose findings — surfaced 2026-08-02, none recorded elsewhere

Found by auditing the trackers rather than by a consistency check; each would otherwise be rediscovered from scratch.

| # | Finding | Action |
|---|---|---|
| 1 | ~~**🔶 The BPMN gate in `implementation-document-map.md` is stale.**~~ ✅ **Fixed 2026-08-02**, on explicit user direction (inline Orchestrator amendment per `PROCESS.md` §3). `04-workflow-and-process-automation-design.md` now reads **Buildable now**, citing `DECISIONS.md` D-14; the readiness-flag legend and the closing "open prerequisites" note were updated to match | **Closed.** |
| 2 | **The three deferred NFR numerics are the last hard gates in the design library.** Max iterations/retries, per-task and per-session budget ceilings, and max self-correction attempts (`BACKLOG.md` §1) gate `01-agent-runtime-and-control-design.md`, `02-token-and-compute-budget-design.md`, and `06-self-correction-and-fallback-design.md` respectively. All three share one number-owner, `03-software-and-architecture/06-non-functional-requirements.md` | **One spec ticket sets all three.** Not urgent — Layer 6 is last — but they are the only remaining hard gates |
| 3 | ~~**Two malformed citations in `01-technology-stack-design.md`**~~ ✅ **Fixed 2026-08-02.** Lines 229 and 749 now cite `02-platform-data-model-design.md` §3 (the fixed platform schema), not the citing document's own §18.6 | **Closed.** |
| 4 | ~~**A second gap in `ai-aha-consistency-check`.**~~ ✅ **Fixed 2026-08-02.** Check 6 now requires a cited `§N` to resolve inside the *named* target document, not merely exist somewhere in the library; check 7 now treats a map entry with no document on disk as normal for a forward-looking schedule, flagging only a readiness/prerequisite contradiction or a written-but-unlinked document | **Closed.** |
| 5 | ~~**H8 concluded D-09 needs no spec change**~~ ✅ **Investigated and corrected 2026-08-02 — the suspicion was right, and the defect was larger than "worth one read."** `02-governance-and-security/03-access-control-and-tenancy-model.md` §6 defines the **Extender** role's action as extending the platform through modules and its programmatic contract (C-11, C-12), and `05-integration-and-extensibility-spec.md` §67 confirms it — **both predate D-09.** H8's claim that the spec specifies no authorship model was wrong, and ADR-016 had silently narrowed a spec-defined role using a decision (D-09) that never names it. Recorded as **`DECISIONS.md` D-18**; `03-architecture-realization-design.md` §10.2–§10.4, ADR-016 and §12 amended. **No spec change owed** — the spec was correct; the design's reading was not. ⚠️ **Reopens a downstream question:** the isolation strength externally-authored extension code needs is now a first-order open item for `06-integration-and-extensibility-design.md`, `06-marketplace-design.md` and `07-connector-marketplace-design.md`, where ADR-016 previously implied it was settled | **Closed** (with the downstream question now tracked below) |

```
