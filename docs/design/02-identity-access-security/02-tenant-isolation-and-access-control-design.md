# Tenant Isolation and Access Control Design — AI ahaMatic

This document designs the concrete mechanism that makes **INV-01 (tenant isolation)** and **INV-02 (authorization before access)** hold at runtime, and realizes `02-governance-and-security/03-access-control-and-tenancy-model.md` as an enforcement model. It states **how** a tenant-scoped actor is prevented from seeing or affecting another tenant's data, how a request's tenant context is resolved once and bound for its whole execution, how a bridge's grant is issued and verified, and how least privilege is enforced across every actor class the specification now recognizes. It does not re-derive the tenant boundary, the steward plane, the role-and-permission matrix, or the forbidden-crossing rule — each is fixed and cited, never restated.

This is a Design-phase artifact realizing `02-governance-and-security/03-access-control-and-tenancy-model.md` in full, and closing three named handovers from documents already written: `02-platform-data-model-design.md` §12 (registry scoping, bridge-grant issuance and verification, boundary proof — none of which that document designs); `03-architecture-realization-design.md` §4.1 row 2, §4.3, §7.1 (the Partial-coverage runtime portion of cross-tenant-instance prevention); and `01-invariant-enforcement-design.md` §8 (the mechanism underneath the INV-01/INV-02 check locations that document already fixes, named there as "the sharpest boundary this document holds"). It reads `02-platform-data-model-design.md` §3–§4 and `01-technology-stack-design.md` §14.5, §14.7 as already-fixed structure and decision, and cites rather than re-derives them.

**Verified versus reasoned (`PROCESS.md` §12.3).** Every claim in this document is **reasoned** — from the cited specification, the cited upstream design documents, and the structural properties of the schema and datastore `02-platform-data-model-design.md` and `01-technology-stack-design.md` already fix. No claim rests on a time-sensitive ecosystem, tooling-maintenance, or adoption fact.

---

## 1. Purpose and Reading Order

This document answers six questions:

- **How access to the shared platform-global registry (`platform.tenants`, `platform.applications`) is scoped**, so a tenant-scoped actor's query never returns more than its own row (§3).
- **How a request's tenant/application context is resolved once, at the point of entry, and bound for the whole of that request's execution** — the runtime portion `03-architecture-realization-design.md` §4.3 hands here (§4).
- **How isolation is proven at the boundary rather than assumed** — the connection-level enforcement and the mandatory self-verification underneath the check locations `01-invariant-enforcement-design.md` §5.1 already fixes for INV-01 and INV-02 (§5).
- **How least privilege is enforced across all five actor classes the specification now recognizes** — builder, extender, end user, the autonomous platform-operating agent, and the external contract consumer — how a builder-persona's own grant, the `tenant_users` row that enforcement reads, comes to exist through admission — including a newly admitted tenant's own first such row, established by the Tenant-admission steward role's own admission act rather than by any tenant owner's onward grant — and how a tenant's or application's own registry-level standing changes through a transition into `suspended` or `offboarding` (§6).
- **How a bridge's grant is issued, scoped, and verified**, and how cross-tenant access is rendered structurally impossible rather than merely forbidden by policy (§7–§8).
- **Whether the partitioning shape or the exclusion of native row-level security should be reconsidered on an isolation-strength finding, distinct from the portability finding that excluded it originally** (§9).

It is structured as a pyramid: the isolation mechanism at the data boundary first, then runtime context resolution, then the invariant-check mechanisms that boundary supports, then role-and-permission enforcement, then bridging and grants, then the cross-tenant-impossibility synthesis, then the row-level-security reconsideration, then the design decision record, then the boundaries and handovers, then precedence and binding rules.

---

## 2. Scope and What This Document Does Not Own

This document owns: the mechanism that scopes access to `platform.tenants`/`platform.applications` so a tenant-scoped actor's query returns only its own row (§3); the runtime tenant-context resolution and binding mechanism (§4); the boundary-verification mechanism underneath INV-01 and INV-02's already-fixed check locations (§5); the least-privilege enforcement model across all five actor classes, the admission mechanism that issues a builder-persona's `tenant_users` grant, the owner-admission mechanism that issues a newly admitted tenant's own first such grant and the identity anchor it references, and the status-transition mechanism that moves `platform.tenants`/`platform.applications` into `suspended` or `offboarding` (§6); bridge-grant issuance and verification for both bridging mechanisms `02-platform-data-model-design.md` §4.2 fixes (§7); the structural argument for cross-tenant access as impossible, not merely disallowed (§8); and the row-level-security reconsideration this document is explicitly authorized to make (§9).

This document does **not** own, and does not decide:

- **The physical schema, the registry, the bridge record, or the role-binding tables** (`02-platform-data-model-design.md` §3–§4) — cited, never restated.
- **The seven components, the module boundaries, the dependency-direction mechanism, or the three-layer separation** (`03-architecture-realization-design.md` §3–§5) — this document's mechanism runs inside the Isolation and Trust module that document already fixes, and extends its import-boundary tool with one additional rule (§5.3) rather than defining a new one.
- **Which invariant runs which check, where each check runs, or what a violation looks like at the point of halt** (`01-invariant-enforcement-design.md` §5, §8) — already fixed; this document supplies the verification mechanism those checks run, never a second check location.
- **Session, credential, and authentication mechanics** (`03-authentication-and-identity-design.md`) — this document consumes an already-authenticated identity; it does not establish one.
- **Residency and region enforcement** — `06-compliance-and-data-residency-design.md`'s. That document creates the region-scoping mechanism itself, extending this document's Registry Accessor return value and connection-acquisition function rather than building a parallel path; this document neither designs nor claims region scoping. *(Corrected 2026-08-06, `BACKLOG.md` §1e: this bullet previously grouped four concerns and routed the reader to "a boundary in §11," which carries no restatement of any of them. Each is now named directly and the forward pointer is dropped. `04-security-controls-design.md` §3.3 and §11 attribute region scoping to this document on the strength of the old wording — that attribution was never correct and is superseded here.)*
- **Secrets-scanning technology and emission-boundary redaction** — `04-security-controls-design.md` §2.1–§2.2's, as extended by `07-data-governance-and-privacy-design.md` §7.3 and `08-audit-and-traceability-design.md` §6.2.
- **Audit-trail capture, storage, immutability, and tamper-evidence** — `08-audit-and-traceability-design.md`'s.
- **Extension- and marketplace-origin sandboxing mechanics** — `06-integration-and-extensibility-design.md`'s, `06-marketplace-design.md`'s, and `07-connector-marketplace-design.md`'s (none yet written); `04-security-controls-design.md` §5.3 states this as an open first-order question for each, and nothing here narrows it.
- **Any independent technology, topology, or scope decision beyond the one named in §9** — this document records five ADRs (§10), and states plainly where no further one is warranted.

---

## 3. Scoping the Shared Registry: `platform.tenants` and `platform.applications`

`02-platform-data-model-design.md` §4.1 states the problem precisely: the registry "necessarily hold[s] every tenant's row, because the platform must route a request to the correct schema before it can act," and hands this document the job of ensuring a tenant-scoped actor's access to it "never returns more than that actor's own row." This section designs that mechanism.

### 3.1 The Registry Accessor: the Sole Read Path

**No component outside the Isolation and Trust module (`03-architecture-realization-design.md` §3.1) ever issues a raw query against `platform.tenants` or `platform.applications`, except the one pre-authentication path §5.3 names and bounds.** Every other read of either table passes through one function, the **Registry Accessor**, whose signature accepts only an **already-authenticated actor identity** (`platform_user_id`, established by `03-authentication-and-identity-design.md` before this function is ever called) — never a client-supplied `tenant_id` or `application_id` used to widen a result set.

The Registry Accessor's query shape is fixed and is not parameterized by anything the caller controls beyond the following:

| Actor's Standing | What the Registry Accessor Returns | Why It Cannot Return More |
|---|---|---|
| Holds one or more `tenant_users` bindings (builder personas, extender) | Exactly the `platform.tenants` and `platform.applications` rows referenced by that actor's own binding rows (`tenant_<id>.tenant_users`, joined against `platform.applications.tenant_id`). | The query's `WHERE` clause is bound to the actor's own binding rows, resolved server-side from the authenticated identity — never to a `tenant_id` the caller supplies. A caller that names a different tenant in a request parameter is not denied by a check; there is no code path in the Registry Accessor that accepts such a parameter as a filter-widening input at all. |
| Holds a `platform.steward_role_bindings` row (one of the four steward roles, §6.1 below) | The full `platform.tenants` and `platform.applications` tables — the visibility `01-business-and-ux/04-personas-and-roles.md` §5 and `02-governance-and-security/03-access-control-and-tenancy-model.md` §4 require for governing "above all tenants." | This is the one legitimate case where registry-wide visibility is correct, not a leak — governance requires seeing that tenants exist to admit, suspend, or govern them. The boundary this visibility must never cross is stated in §3.2. |
| Holds neither | Nothing. | Deny-by-default (`02-governance-and-security/01-system-invariants.md` §3): an actor with no binding and no steward role has no path to any registry row, including its own — because it has none. |
| An actor requests narrowing to a specific application within its own tenant's scope (e.g., "act as application X") | The named application's row, but **only** after the Registry Accessor confirms that application's `tenant_id` matches one the actor's own binding already covers. | This is a narrowing selection over an already-established result set, never a widening one — the actor cannot use this parameter to reach an application outside its own binding's tenant. |

### 3.2 What Steward Visibility Does Not Confer

A steward role's registry-wide read access is bounded by the same rule `02-governance-and-security/03-access-control-and-tenancy-model.md` §4 and §7 already fix: **the steward governs the boundary without entering it.** Concretely, holding a `platform.steward_role_bindings` row grants the Registry Accessor's registry-wide read path (§3.1) and nothing else:

- It confers **no** connection-scoping grant (§5.1) to any `tenant_<id>` or `tenant_<id>_app_<id>` schema. A steward role can see that a tenant and its applications exist, admit or suspend them, and observe the registry-level facts §3.1's fixed catalog tables hold — it cannot open a connection scoped to read a tenant's own `tenant_users`, `application_bridges`, or any application's builder-defined data, because no such grant exists (§5.1's connection-scoping mechanism grants schema access per tenant/application binding, and a steward role holds none).
- Where a specific steward action genuinely requires entering a tenant's own schema (an operational necessity, not a routine one — for example, executing an approved, human-authorized data-recovery action), that action requires a distinct, explicitly-issued grant scoped to exactly that tenant and that action, issued and revoked around the operation — it is never a standing property of holding a steward role. Designing that distinct grant's issuance mechanism, and the human-in-the-loop protocol that authorizes it, belongs to `05-meta-operations/04-human-in-the-loop-protocol.md`; this document states only that no steward role holds it by default.

### 3.3 Why This Is Verifiable

The claim "a tenant-scoped actor's query never returns more than its own row" is checkable two ways: (1) the Registry Accessor's own source has exactly one code path per row of §3.1's table, none of which accepts a caller-supplied identifier as a widening filter — reviewable directly, and testable by a fixed suite of adversarial inputs (a request naming a foreign `tenant_id` must return the same result as a request naming none); (2) no other module in `components/*` imports the platform-schema data-access module for `platform.tenants`/`platform.applications` at all — enforced by the same import-boundary mechanism `03-architecture-realization-design.md` §4.2 already builds, extended with one additional rule this document owns (§5.3).

---

## 4. Runtime Tenant-Context Resolution

`03-architecture-realization-design.md` §4.1 row 2 proves, at build time, that no component module hardcodes a tenant identifier and no per-tenant code variant exists — and explicitly rates itself only **Partial**, handing "the runtime portion — correct per-request tenant resolution and enforcement" here (§4.3, §7.1). This section designs that runtime portion.

### 4.1 The Context Resolution Point

Every request that reaches the platform core passes through exactly one **Context Resolution Point**, inside the Isolation and Trust module, before any other component module's handler runs. At that point:

1. The request's already-established actor identity (`03-authentication-and-identity-design.md`'s output — this document consumes it, never establishes it) is passed to the Registry Accessor (§3.1).
2. The Registry Accessor returns the actor's tenant binding(s), any application-level narrowing (`tenant_<id>.tenant_user_application_scope`), and the resolved `schema_name` (`platform.applications.schema_name`) the request's action targets.
3. These are assembled into one **Resolved Request Context** object: `{ platform_user_id, actor_class, tenant_id, application_id (where scoped), schema_name (where resolved), role, permission scope, grant reference (where the actor is a client, extender, or external contract consumer — §6, §7) }`.
4. The object is **frozen at creation** — no subsequent step in the request's execution may mutate any field of it.

### 4.2 Binding Every Subsequent Operation to the Resolved Context

The Resolved Request Context is threaded through the request's entire execution via the runtime's own request-scoped storage mechanism (under the V1.0 default stack, Node.js's `AsyncLocalStorage`; under a different candidate stack, the equivalent request-scoped or ambient-context primitive, per `03-architecture-realization-design.md` §9's stack-portability convention — the property is stack-neutral, only the primitive that carries it is stack-bound).

The binding is structural, not conventional, for one reason: **the function that acquires a database connection (§5.1) accepts only the Resolved Request Context object as its input — never a raw tenant or application identifier a caller could supply independently.** A component module that wants to read or write tenant- or application-scoped data has no function available to it that takes an identifier string; it has only one that takes the context object already resolved for the request it is handling. This closes the gap `03-architecture-realization-design.md` §4.3 names: there is no code path by which a request's execution could name a tenant or application other than the one resolved for it at entry, because every downstream call is parameterized by the frozen object, not by a value any later step could substitute.

### 4.3 No Mid-Request Context Switch

Because the Resolved Request Context is frozen at creation (§4.1) and every downstream call is parameterized by that same object (§4.2), no operation within one request's execution can resolve against a different tenant or application than the one established at entry — including for a multi-step operation (an API-mediated bridge call, §7.1) that in effect asks the platform to act "as" a second application. Where such a call is legitimate, it is modeled as a **new**, independently-resolved request against the second application's own contract (§7.1), carrying its own Resolved Request Context — never as a mutation of the first request's context.

---

## 5. Connection-Scoped Isolation and Boundary Self-Verification

This section is the mechanism `01-invariant-enforcement-design.md` §8 names as its "sharpest boundary": that document fixes that INV-01 and INV-02 are checked, where each check runs, and what a violation looks like (§5.1 of that document) — it explicitly does not design how isolation is verified at the data boundary or how a bridge's grant is verified. This section supplies that mechanism, without restating the check locations already fixed.

### 5.1 Structural Access: Schema-Scoped Database Privilege, Not a Predicate

`01-technology-stack-design.md` §14.5 excludes native row-level security on portability grounds and requires isolation to be "structural rather than predicate-based." This document realizes that requirement as **database-privilege grants scoped to exactly one schema**, using ordinary `GRANT`/`REVOKE` — a mechanism every candidate engine (PostgreSQL, MySQL/MariaDB, SQL Server) supports natively, unlike RLS.

- **One database role per application schema**, created at the same time as the application's schema (`tenant_<id>_app_<id>`, `02-platform-data-model-design.md` §3.3), granted `USAGE` and the necessary table privileges on **that schema only**. **One database role per tenant schema** (`tenant_<id>`), granted identically, scoped to that tenant's own schema only.
- **No schema is granted to a role by default.** Every candidate engine denies access to an unnamed schema unless a grant exists; this document adds no new deny-by-default rule, it relies on the one every candidate engine already enforces at the privilege layer. Provisioning a new tenant or application (§11's onboarding case) is exactly one additional `GRANT` statement, issued once, at creation — there is no companion `REVOKE` step to remember for every other schema, because no other schema was ever granted.
- **The Context Resolution Point's connection-acquisition step (§4.2) switches the active database role to the one matching the Resolved Request Context's `schema_name`** before any query in that request executes (`SET ROLE`, or the equivalent contained-schema/contained-database mechanism under MySQL or SQL Server). A query issued under this connection has no privilege to name, address, or read any schema other than the one the Resolved Request Context resolved — not because a predicate filters it out, but because the privilege to open that path does not exist for this connection.

This is materially stronger than the predicate-based isolation `01-technology-stack-design.md` §14.5 rejected ("a `tenant_id` predicate the abstraction layer is trusted to append on every query is closer to assumed than verified"): a missing `WHERE` clause in a shared-schema design is a silent, in-band failure; a missing schema grant in this design means the connection has no privilege to run the query at all, and the query fails loudly, before any row is returned.

### 5.2 The Self-Verification Read: Proving the Boundary, Not Assuming It

`02-governance-and-security/03-access-control-and-tenancy-model.md` §3 requires isolation to be "verified at the tenant boundary itself, not assumed from the absence of an observed breach." Connection-scoping (§5.1) makes a crossing structurally hard to reach; this section adds the check that would catch it if some defect in the connection-acquisition step ever mis-scoped a connection regardless.

- **At the application level:** immediately after the connection role is switched (§5.1), the mechanism performs one mandatory read of `<schema>.application_metadata` (`02-platform-data-model-design.md` §3.3 — the table that exists precisely "so any query executed inside the schema can independently confirm which application and tenant it belongs to, without trusting an external join") and compares its `application_id`/`tenant_id` against the Resolved Request Context. **A mismatch is an INV-01 violation, full stop** — the request halts before any further query executes, per the halt behavior `01-invariant-enforcement-design.md` §3.3, §5.1 already fixes; this document supplies the read that feeds that check, not a second check location.
- **At the tenant level** (a request whose scope is tenant-wide, not narrowed to one application — e.g., an access administrator's own action), the equivalent check reads the connection's own resolved schema name via the database driver's standard schema-introspection call (`current_schema()` under PostgreSQL; the equivalent under MySQL or SQL Server) and compares it against `tenant_<Resolved Request Context.tenant_id>`. This uses a capability every candidate engine already exposes and requires no new table — consistent with reading `02-platform-data-model-design.md` §3, not restating or inventing structure inside it.
- **This read is mandatory and unconditional**, run on every request that resolves an application- or tenant-scoped connection, never only when a defect is suspected. It is what makes "verified at the boundary" concrete rather than aspirational: the boundary is checked by executing a read against the schema's own self-description and comparing it to what was resolved, every time, not inferred from the connection-scoping mechanism's own correctness.

### 5.3 The One Additional Import-Boundary Rule This Document Owns

`03-architecture-realization-design.md` §4.1 fixes five import-boundary rules, none scoped to registry-table access specifically. This document adds a sixth, run by the same tool (`03-architecture-realization-design.md` §4.2 — dependency-cruiser or the ESLint boundary-rule set under the V1.0 default stack, or the stated equivalent under a different stack), owned here because it is specific to this document's own concern:

**No module outside `components/isolation-and-trust` may obtain a handle capable of directly querying `platform.tenants` or `platform.applications`** — whether by importing the Registry Accessor's own data-access module or by any independently-implemented query path against either table — **with exactly one named exception, stated below.** This closes, at build time, the paths by which a defect elsewhere in the codebase could bypass the Registry Accessor (§3.1) rather than merely failing to call it correctly.

**Corrected 2026-08-17 (`BACKLOG.md` §1u; ADR-062, §10.3).** This rule originally read "may import the platform-schema data-access module" — naming one specific module rather than the property that module's exclusivity exists to protect. A second, independently-written module could satisfy that literal wording (it would never import the named module) while still opening an independent path to query the same tables, leaving the rule's letter intact and its purpose defeated. The rule is restated above in terms of the property it protects — a query-capable handle on either table, however obtained — so a future second path cannot again satisfy the letter while missing the purpose.

**The one exception: the Region Resolution Point.** `08-multi-region-distribution-design.md` §6.1–§6.2 (ADR-045) fixes that region routing must resolve before authentication completes, keyed by an address-supplied identifier — a requirement the Registry Accessor's own signature structurally cannot serve (§3.1 above: "never a client-supplied `tenant_id`... used to widen a result set"). Reusing the Registry Accessor for pre-authentication routing would require weakening that guarantee, not composing with it — a conclusion this document does not disturb. The Region Resolution Point, realized as its own distinct module inside `components/distribution`, is exempted from the rule above to the extent, and only to the extent, that it reads `region_of_record` from `platform.tenants`/`platform.applications` via the regionally-resident read replica (`08-multi-region-distribution-design.md` §3.2), for the single tenant its own request's address names, before authentication, for locality resolution alone. It resolves no role, permission, or grant content; it never widens beyond the addressed tenant; and it is consulted exactly once, before the request re-enters the sequence this document's own mechanism governs (§4.1) — the identical non-widening discipline the Registry Accessor itself observes (§3.1's table), applied to a caller this document does not otherwise reach.

**Coverage, stated honestly.** Full coverage, less this one named exception: every module outside `components/isolation-and-trust` other than the Region Resolution Point receives only the Resolved Request Context object (§4.1) or the connection already scoped by it (§5.1) — never a handle capable of querying the registry tables directly. A Full-coverage claim with an unstated exception is worse than a narrower claim honestly made; this is the narrower claim.

---

## 6. Least-Privilege Enforcement Across Five Actor Classes

`02-governance-and-security/03-access-control-and-tenancy-model.md` §4–§8 fixes the steward plane, the role-and-permission matrix, and least privilege as the standing posture. The specification now recognizes **five** actor classes; this section states, for each, where its grant lives and how the mechanisms of §3–§5 enforce it.

| Actor Class | Where the Grant Lives (`02-platform-data-model-design.md` §5) | How §3–§5's Mechanism Enforces Its Least-Privilege Boundary |
|---|---|---|
| **Builder personas** (tenant owner, access administrator, application builder, operator, publisher) | `tenant_<id>.tenant_users`, narrowed by `tenant_<id>.tenant_user_application_scope` where the role is application-scoped. | The Resolved Request Context (§4.1) carries exactly the scope (`tenant-wide` or the named applications) the binding row states; the connection-scoping mechanism (§5.1) grants no schema beyond that scope. A tenant owner or access administrator (always tenant-wide, per the tenancy model §6) is granted the tenant schema and, where an action targets one application, that application's schema — never a second tenant's. |
| **Extender** | `tenant_<id>.tenant_users`, scoped by the grant its extension represents. | "Bound entirely by its own grant" (`02-governance-and-security/03-access-control-and-tenancy-model.md` §5) is enforced identically to a builder persona's application-scoped grant (row above) — the Resolved Request Context's permission scope is drawn from the extension's own binding, never from the platform primitive it extends. `03-architecture-realization-design.md` §4.1 row 4 (no core dependency on a specific extension) is unaffected and orthogonal — that rule protects generality; this row protects tenant/application scope — and holds regardless of the Extender authorship question that document's §10 leaves open. |
| **End-user personas** (authenticated end user, public consumer, end-user administrator) | Not a platform structure (`02-platform-data-model-design.md` §5) — builder-defined content, recognized through the generic access primitive (C-03). | The Context Resolution Point (§4) resolves the **application's own tenant and schema** identically to any other request against that application; the end-user identity and its permission content are `02-data-model-and-entity-design.md`'s and `03-authentication-and-identity-design.md`'s to define, but the connection they act through is scoped by this document's mechanism exactly as any other actor's is — end-user scope is always exactly one application (`02-governance-and-security/03-access-control-and-tenancy-model.md` §6), and no mechanism in §3–§5 grants a connection wider than the one application resolved for the request. |
| **Autonomous platform-operating agent** (`05-meta-operations/01-agent-operating-charter.md`) | `platform.steward_role_bindings`, identically to a human holding the same steward role — the agent "operates the platform's own lifecycle... a role entirely internal to the platform" (`01-business-and-ux/04-personas-and-roles.md` §5), not a tenant-scoped actor. | Governed by exactly §3.1's steward row and §3.2's boundary: registry-wide visibility for governance, no standing connection-scoping grant to any tenant's or application's own schema. "The agent's authority never extends to observing, affecting, or disclosing one tenant's state to another" (`05-meta-operations/01-agent-operating-charter.md` §2) is the same boundary §3.2 already states for any steward role — this document creates no separate, weaker, or stronger enforcement path for autonomous versus human-directed steward action; only the audit trail (owned by `08-audit-and-traceability-design.md`) distinguishes actor type, never this document's grant mechanism. |
| **External contract consumer** (T70, 2026-08-03) | A client grant within the meaning of `02-governance-and-security/03-access-control-and-tenancy-model.md` §6 — "the scoping identity of an external caller... authorized to act against platform capabilities on a tenant's behalf" — issued by a tenant, scoped to whatever the tenant grants and never wider. | Resolved identically to any other client-scoped grant (§7.2's grant-verification mechanism): the Resolved Request Context carries the grant's own scope, drawn from the tenant's own permissions and never exceeding them (`02-governance-and-security/03-access-control-and-tenancy-model.md` §6, "a client's grant is always a subset of its tenant's own"). It holds no implicit authority and no exemption from §3–§5's connection-scoping or self-verification mechanism by virtue of acting externally or autonomously — the mechanism does not distinguish a human-directed external caller from an autonomous one; both are bound by the same issued grant, checked the same way. |

**No authority is held by default, for any of the five.** Every row above resolves to an explicit grant record the Registry Accessor or the tenant-scoped binding tables already hold; where no such record exists for a given actor and a given action, the Context Resolution Point (§4.1) resolves no scope, the connection-scoping mechanism (§5.1) grants no schema, and the action is refused — the same deny-by-default posture stated once here rather than restated per row.

### 6.1 Admission: How a Builder-Persona's `tenant_users` Grant Comes to Exist

The table above names `tenant_<id>.tenant_users` as "Where the Grant Lives" for every builder persona without stating how that row comes to exist. `02-governance-and-security/03-access-control-and-tenancy-model.md` §5 (added by T79, authorized by `DECISIONS.md` D-36) closes that gap at the specification level: it names the act — **admission** — states that the authority to perform it is nothing new, only the exercise of the tenant owner's own already-fixed "May Grant Onward To" entry in the matrix of that section, and fixes three preconditions that must all hold before admission takes effect. It names no table, token, workflow, or state machine. This section supplies the mechanism.

**Restraint, stated up front.** This section does not re-decide who may admit — the specification's matrix already fixes that only the tenant owner's onward-grant entry authorizes a builder role to be granted, and this section reads that matrix rather than restating or narrowing it. It does not redesign identity establishment, which remains `03-authentication-and-identity-design.md`'s in full (this document's own boundary against it, §11 below). It does not extend the role-and-permission matrix or introduce a role, a grant, or a scope the matrix does not already fix.

**The three preconditions, checked one at a time for whether each reduces to a structural guarantee or remains a predicate.** §5.1's own argument — that a connection-scoped privilege boundary is materially stronger than a predicate an abstraction layer is trusted to apply — is the standard this section holds each precondition to, honestly, rather than assuming all three qualify.

| Precondition (spec §5) | Structural or Checked | Mechanism |
|---|---|---|
| The membership is bound to exactly one tenant. | **Structural.** | The `tenant_users` INSERT executes over the same connection the admitting actor's own request already resolved and scoped to exactly one `tenant_<id>` schema (§4.2, §5.1); `tenant_users` is a per-tenant-schema table (`02-platform-data-model-design.md` §3.2), and that connection holds no privilege to address any other tenant's schema. There is no code path by which the write could target a `tenant_<id>` schema other than the one the admitting actor's own Resolved Request Context already names — the identical guarantee §8's fact 2 already states for every other write this document governs, reused here rather than re-derived. |
| The identity of the person being admitted is established. | **Structural, in part — stated honestly where it is not.** | `tenant_users.platform_user_id` is a foreign key to `platform.platform_users` (`02-platform-data-model-design.md` §3.2), the platform's own identity anchor, populated only once `03-authentication-and-identity-design.md`'s mechanism has established that identity (cited, not redesigned). The INSERT fails at the database level, before any row is written, if no such anchor row exists — the same "fails loudly, before any row is returned" property §5.1 already claims for schema privilege, applied here to referential integrity instead of privilege. **What this does not cover:** the foreign key proves an anchor row exists; it does not itself prove that identity is currently in good standing. That further condition, where the deny-by-default posture of spec §8 requires it, rests on whatever status `platform.platform_users` carries and on `03-authentication-and-identity-design.md`'s own mechanics — neither redesigned here. |
| The role admitted never exceeds what the admitting authority itself holds. | **Checked — not structural, stated plainly.** | No schema-level constraint encodes the role hierarchy of the matrix (`02-governance-and-security/03-access-control-and-tenancy-model.md` §5), and nothing about connection privilege or referential integrity stops an over-scoped INSERT from being *attempted*. This precondition is enforced by the **Admission Authorization Check**, an application-level check run at the same INV-02 decision point every other authorization decision in this document runs at (§4.1, the identical pattern §7.1 already establishes for the bridge-authorization check): it reads the admitting actor's own resolved `role` field, already carried on the Resolved Request Context assembled for their own authenticated request (§4.1), and refuses the admission where that role's own "May Grant Onward To" entry (matrix, §5 — read, never restated) does not cover the role being admitted. |

**Grant issuance: a single INSERT, not a two-part write.** Admission is a grant issuance, and §7 is this document's closest analogue for that shape — but admission's mechanism differs from both of §7's bridging mechanisms in one respect, stated because the difference is deliberate, not an omission. §7.2's schema-level bridge issues a database-level `GRANT`/`REVOKE` in the same transaction as its own record, because a bridge opens a **new** cross-schema access path a connection did not already have — the atomic-commit discipline exists precisely so "the row and the grant are never allowed to diverge." Admission opens no new access path at all: the newly admitted person's access is realized entirely by the Registry Accessor and Context Resolution Point (§3, §4) reading the `tenant_users` row on their own, separately-authenticated future request — the identical mechanism that already governs every other builder persona in the table above. No second database role is created, no schema grant is issued, and no separate verification mechanism exists for a newly admitted person; from the moment the row commits, they are an ordinary instance of the already-designed builder-persona access path. The grant issuance mechanism is therefore: one INSERT into `tenant_<id>.tenant_users`, populating the already-fixed columns (`platform_user_id`, role, `scope_type`, `granted_at`, `granted_by` — `02-platform-data-model-design.md` §3.2), executed only once all three preconditions above are established, over the connection already scoped to the admitting tenant. Because there is only one write and no companion effect, §7.2's atomicity concern — the row and a second effect diverging — does not arise here; there is nothing for the row to diverge from.

**Grant verification** is not a separate mechanism either: the row is read, and the admitted person recognized, through the identical Registry Accessor / Context Resolution Point path (§3, §4, §6) any other builder persona's binding is already read through. Nothing new is designed for it.

**Where any precondition cannot be established, admission does not proceed** — the specification's own rule (§5), matching the deny-by-default posture of `02-governance-and-security/01-system-invariants.md` §3 and the uncertainty-resolves-to-denial rule of spec §8, cited here and not restated as new content.

**Evidence produced, checked against the standing index, not the frozen table.** `08-audit-and-traceability-design.md` §4.3 is a point-in-time reconciliation, superseded for lookup purposes by its own §4.5 standing index (`BACKLOG.md` §1v) — this check was run against §4.5, not §4.3. Reuse before minting, applying the H26c test (is the fit direct or imperfect, stated honestly either way):

- **A precondition-3 failure (role exceeds the admitting authority) reuses `authority-refusal`** (Authorization and grant events) — a direct fit: that type's own established fit-note already reads "a refusal for exceeding a resolved grant is exactly this category's own wording" (§4.3, §4.5), which describes precondition 3's own failure exactly.
- **A precondition-1 failure (the admitted person's identity anchor does not exist) reuses the generalized `invariant-check` event, `invariant_id = INV-02`** — the existing shape every INV-02 check already lands under (§5); this is not a grant-scope excess, so `authority-refusal`'s own wording would not fit, and the two are kept distinct on the identical reasoning `08-audit-and-traceability-design.md` §4.3 already applies to keep `basis-refusal` and `authority-refusal` apart.
- **Precondition 2 has no failure mode to capture.** Because the write is structurally confined to the admitting actor's own tenant schema (above), there is no code path by which an admission could be attempted against a foreign tenant for a refusal event to describe — the identical "no naming path exists" argument §8 already makes.
- **The successful admission itself — the row's own creation — has no existing type.** `extension-registration` and `data-administration-grant-configuration`, the closest sibling structures, each narrow an **already-existing** `tenant_users` binding (`02-platform-data-model-design.md` §3.2); neither captures the base row's own creation. One new type is minted: **`builder-admission`**, landing under Authorization and grant events — a direct fit, matching the sibling grant-configuration types' own rating — `outcome = applied`, `actor_reference` resolving to the admitting tenant owner, `target_reference` resolving to the newly admitted person's `platform_user_id` and the created `tenant_user_id`.

**The index amendment this minting owes is flagged, not made here.** `08-audit-and-traceability-design.md` §13 authorizes a minting ticket to add the corresponding row to that document's §4.5 index "regardless of which document is the ticket's stated primary subject" — but that same document's own §4.5 states the authorization is "necessary but not sufficient" until the ticket-prompt-assembly step names it as a second target, which this ticket's own Document(s) field does not. This ticket's Document(s) field and Output both name this document alone; adding the row there would exceed that scope on the stricter, controlling rule (`PROCESS.md` §3's Executor boundary), regardless of the target document's own invitation. The row `builder-admission | 02-tenant-isolation-and-access-control-design.md §6.1 | Direct` is owed to `08-audit-and-traceability-design.md` §4.5's Authorization and grant events table, and to that section's running count, as a follow-up — named here rather than left to be rediscovered, the same discipline `02-platform-data-model-design.md`'s H45 and H46 additions already applied to their own owed structures.

### 6.2 Status Transition: How `platform.tenants` and `platform.applications` Move to `suspended` or `offboarding`

`02-platform-data-model-design.md` §3.1 fixes `active` / `suspended` / `offboarding` as the status vocabulary of both `platform.tenants` and `platform.applications`, and states nothing about what moves a row between them. `02-governance-and-security/04-auth-and-identity-spec.md` §5 already requires a session to terminate the moment "the tenant, application, or account the session belongs to has been removed, suspended, or disabled" — an obligation `03-authentication-and-identity-design.md` §4.4 already realizes mechanically for the *consequence* ("removing, suspending, or disabling a tenant, application, or `platform.platform_users` account revokes every `platform.sessions` row it affects in the same commit as the removal itself"), without ever designing the transition that is its own trigger. This section is that mechanism (`DECISIONS.md` D-41, `BACKLOG.md` §1ag(b)) — it realizes an obligation the specification already states and a schema column `02-platform-data-model-design.md` already carries; it grants no new capability.

**Restraint, stated up front, on three fronts.** This section does not redesign session expiry or the session-termination mechanism — `03-authentication-and-identity-design.md` §4.4 owns how a `platform.sessions` row is actually revoked; this section fixes only that its own write commits in the same transaction that mechanism already anticipates. It does not redesign data disposition on `offboarding` — `07-data-governance-and-privacy-design.md` §4 (the Retention Sweep and its deletion mechanism) and `02-platform-data-model-design.md` §10 (the never-hard-deleted convention for these two tables) already govern what happens to a tenant's or application's own data once this section's write commits; this section triggers that already-specified handling, never a second one. It does not authorize a builder to deprecate their own built application as a first-class capability — `DECISIONS.md` D-40 declined that question, and this section designs the transition mechanism only, for whatever reason a tenant or application enters `suspended` or `offboarding` today (policy, or the tenant's own exit choice), never a deprecation program, its UI, or its notifications.

**The authority shape here is not admission's.** §6.1's admission is an act of *delegation* — the tenant owner exercising its own already-fixed "May Grant Onward To" entry to extend a role to someone else. A status transition is not a delegation; it is an act on the tenant's or application's own registry-level existence, and the matrix (`02-governance-and-security/03-access-control-and-tenancy-model.md` §4–§5) grounds it in two different places accordingly, not one:

| Transition | Who May Trigger It | Authority Source |
|---|---|---|
| `active` → `suspended` (tenant or application) | The platform steward, specifically the **Tenant admission** steward role. | `02-governance-and-security/03-access-control-and-tenancy-model.md` §4: "Admits and removes tenants at the platform boundary" — the same role this document's own §3.1/§3.2 already credits, informally, with the ability to "admit or suspend" a tenant and its applications. No other steward role's own definition names this authority: Platform operations explicitly "owns and operates the platform-provided primitives... themselves — distinct from operating any tenant's built software," so it is not the source here either. |
| `active`/`suspended` → `offboarding` (tenant) | Either the **Tenant admission** steward role (an administrative removal, symmetric with its own "removes tenants" text) or the **tenant owner** (a voluntary exit). | Steward path: the identical citation above. Tenant-owner path: `01-business-and-ux/04-personas-and-roles.md` §3, "owns the tenant's account boundary" — the root-of-authority phrase this document has not previously had occasion to read against a write, and `02-governance-and-security/03-access-control-and-tenancy-model.md` §5's permission-scope row, "Hold full authority over its tenant's members, roles, and built software," scope boundary "One tenant." |
| `active`/`suspended` → `offboarding` (application) | The **tenant owner** only. | `02-governance-and-security/03-access-control-and-tenancy-model.md` §5's tenant-owner row above, read for its "built software" clause. **No steward-triggered path is asserted here.** The Tenant admission role's own definition names "tenants," never applications, as what it removes; extending it to an individual application's own offboarding would be an inference this section's citations do not support, so none is made. |

No other role is named. Operator holds *delegated*, narrower authority, "One tenant, optionally narrowed to specific applications" (`02-governance-and-security/03-access-control-and-tenancy-model.md` §5); Publisher's own row scopes it to "the applications and extensions it is authorized to publish" (same table). Neither is a granting role ("None — an operating role" and "None — a reach role," same table), and neither is the tenant's or application's own root authority over its registry-level existence. This section extends the matrix to no new role, grant, or scope it does not already fix, exactly as §6.1 does for admission.

**The honesty pattern §6.1 sets, applied here — and it resolves differently.** §6.1's three preconditions split two-structural-one-checked because the row being written, `tenant_<id>.tenant_users`, is a **per-tenant schema table**, reachable only through a connection §5.1 already scopes to exactly the admitting actor's own tenant. A status transition writes to `platform.tenants` or `platform.applications` — the **platform-global registry** — and no connection this document scopes ever holds privilege there at all: §5.1 grants one database role per tenant schema and one per application schema, never one for the `platform` schema itself, and §3.2 states plainly that holding a steward role "grants the Registry Accessor's registry-wide read path (§3.1) and nothing else" — no connection-scoping grant to any tenant's or application's own schema. **No precondition below reduces to a structural guarantee.** Every one is an application-level check, run at the same INV-02 decision point every other authorization decision in this document runs at (§4.1) — stated honestly, not assumed structural because §6.1's precedent found two preconditions that way.

| Precondition | Structural or Checked | Mechanism |
|---|---|---|
| The actor's resolved standing authorizes this specific transition (table above). | **Checked.** | The Status Transition Authorization Check reads the actor's own resolved `role` and, where relevant, `steward_role_bindings` entry, already carried on the Resolved Request Context assembled for their own authenticated request (§4.1) — the identical pattern §6.1's Admission Authorization Check and §7.1's bridge check already establish — and refuses where neither the table's steward-role nor tenant-owner path is satisfied. |
| Where the actor is a tenant owner (never a steward), the target row belongs to that actor's own tenant. | **Checked, and the sharpest contrast with §6.1.** | §6.1's identical-shaped precondition (single-tenant binding) was **structural**, because the write itself could not physically reach a different tenant's schema. Here it cannot be, because the write reaches a platform-global table no connection-scoping confines at all. The check instead compares the target `tenant_id` (or, for an application, `platform.applications.tenant_id`) named in the request against the actor's own resolved `tenant_id` on the Resolved Request Context — never a caller-supplied identifier trusted on its own — and refuses on any mismatch. A steward's own Tenant-admission standing is, by the table above, not tenant-bound at all, so this check does not apply to that path. |
| The target row's current `status` admits the transition requested — `active` only, for a transition to `suspended`; `active` or `suspended`, for a transition to `offboarding`; never `offboarding` itself, which is terminal (`02-platform-data-model-design.md` §10, "a removed tenant or application is marked with a terminal status rather than deleted outright"). | **Checked.** | No schema-level constraint enforces this ordering — `status` is an ordinary column, not a database-level state machine. The check reads the target's current `status` immediately before the write and refuses a transition whose starting state does not admit it. This document does not design a transition *out of* `offboarding`; `DECISIONS.md` D-41 authorizes this mechanism for the transition into `suspended` or `offboarding` only, and no reactivation path is designed here. |

**The write itself: the Registry Status Mutator, the write-side counterpart to the Registry Accessor.** No document, including this one's own §3.1, has previously designed *any* write path to `platform.tenants` or `platform.applications` — tenant and application provisioning (§5.1's "onboarding case") is presumed platform-core, DDL-capable administrative code for the same structural reason this write is: no per-request connection ever holds privilege on the `platform` schema. The status-transition write reuses that same class of connection, never a new one: a function accepting only an already-authenticated actor identity, a target tenant or application reference, and the requested status — never a caller-supplied predicate — that runs the two checks above and, only where both pass, issues a single `UPDATE` against the named row's `status` column over platform-core's own administrative connection. This mirrors §3.1's Registry Accessor discipline (identity in, never a widening filter the caller controls) applied to a write instead of a read, and mirrors §6.1's admission mechanism in shape (one write, gated by checks, no companion database-privilege statement of its own — this write changes a value on an already-existing row; it opens no new schema-level access path the way §7.2's bridge grant does, so nothing exists for a companion statement to protect).

**Composition, not redesign, with session termination.** `03-authentication-and-identity-design.md` §4.4 already fixes that "removing, suspending, or disabling a tenant, application, or `platform.platform_users` account revokes every `platform.sessions` row it affects in the same commit as the removal itself." The Registry Status Mutator's `UPDATE` is that removing, suspending, or disabling: it commits in the same transaction as the session-revocation write that document's own mechanism performs, exactly as that document's table already anticipates. Which `platform.sessions` rows are affected, and how they are identified, is that document's own determination, not redesigned here.

**Composition, not redesign, with data disposition.** An `offboarding` transition is the event that starts a tenant's or application's own data-disposition path — but this document does not design that path. `02-platform-data-model-design.md` §10 already fixes that neither row is hard-deleted on this write; it is marked with the terminal status this section's mechanism writes, "from INV-08 (reversibility and recovery) and from the referential needs of the audit trail itself." `07-data-governance-and-privacy-design.md` §4 already fixes the Retention Sweep and its deletion mechanism for the personal data such a tenant or application holds, evaluated against each element's own Retention Obligation (purpose end, consent withdrawal, or ceiling). This section's write is the fact that a tenant's or application's disposition-relevant purpose has concluded; evaluating and acting on that fact against personal data remains entirely `07-data-governance-and-privacy-design.md`'s own mechanism.

**Evidence produced, checked against the standing index, applying the H26c test.** No existing `event_type` captures a `status` transition on `platform.tenants`/`platform.applications`. The closest sibling, `publication-status-change` (`04-publishing-and-delivery-design.md` §13), is an imperfect fit examined and rejected: it captures a different column (`publication_status`, reachability) on a different actor's grant (the Publisher role) for a different fact (whether an application is reachable to its end users, never whether the application itself still exists in good standing) — reusing it would blur two genuinely distinct facts the same table happens to carry. One new type is minted: **`registry-status-change`**, landing under **Authorization and grant events** — the identical category `builder-admission` and `publication-status-change` both land under, on the identical reasoning: this is, in substance, an exercise of the triggering actor's own resolved authority over the registry, not a tenant-boundary check, a security control, a residency obligation, a data-lifecycle action, an invariant halt, or an autonomous-agent change.

- `result` — the status transitioned to (`suspended` or `offboarding`), following the identical one-type/two-directions discriminator `publication-status-change` already establishes for its own two-direction fact, rather than minting a second type per direction.
- `target_reference` — an opaque reference to the tenant or application row acted on (its own resolution states which).
- `actor_reference` — the triggering actor: the steward holding the Tenant-admission binding, or the tenant owner.
- `outcome` — `applied` (the transition committed) or `refused`. A refusal for failing the first or second precondition above (unauthorized actor, or a tenant owner naming a target outside their own tenant) **reuses `authority-refusal`** — a direct fit, the identical reasoning §6.1 already applies for its own precondition-3 reuse: both are a refusal for exceeding what the actor's own resolved grant covers. A refusal for the third precondition (the target's current status does not admit the requested transition) has no existing type to reuse — it is not a grant-scope excess at all — and is captured directly by `registry-status-change` itself, `outcome = refused`, on the identical precedent `publication-status-change` already sets for capturing its own non-grant-shaped refusal within its own type rather than forcing an ill-fitting reuse.

**The index amendment this minting owes is made here, not flagged.** Unlike §6.1's `builder-admission` — whose own ticket named this document only as a Dependency Input, leaving the row flagged rather than made (`BACKLOG.md` §1ah, closed by H60) — this ticket's own prompt names `08-audit-and-traceability-design.md` §4.5 as a second `Document(s)` target conditioned on a new type being minted, closing the process-template gap `P05` fixed after §6.1 was written. The row `registry-status-change | 02-tenant-isolation-and-access-control-design.md §6.2 | Direct` and the corresponding count update are made directly in that document, in this same ticket.

### 6.3 Owner Admission: How a Newly Admitted Tenant's Own First `tenant_users` Binding Comes to Exist

§6.1 designs admission into an **already-provisioned** tenant: its trigger is an existing tenant owner's own onward grant, and its precondition set presupposes that owner's own `tenant_users` row already exists to be read off a Resolved Request Context. `02-governance-and-security/03-access-control-and-tenancy-model.md` §4 (added by T85) fixes the act that produces the first such row — "Admission establishes the tenant owner's own first identity and its top authority in one act": admitting a tenant creates the tenant boundary of that document's §3 and, within the same act, establishes the tenant owner's own first `platform.platform_users` identity record and grants that owner the top-authority scope §5's matrix fixes for the tenant-owner role, within the newly admitted tenant boundary alone. No mechanism in the library supplies that row. §6.1's trigger cannot produce it — there is no prior tenant owner in a tenant being admitted; `04-scalability-availability-and-performance-design.md` §5.3's onboarding mechanism covers the registry rows, both schema levels, and the corresponding database roles and grants, and names no identity or binding row; and §6.2's own observation that provisioning's write is "presumed platform-core, DDL-capable administrative code" fixes the connection class, never the identity half. This section supplies it (`BACKLOG.md` §2's Tenant provisioning entry), as a **composition with §6.1's mechanism rather than a second one**: the same one-INSERT shape, the same fixed columns, a different trigger, and a precondition set that resolves differently in three places — each stated below rather than assumed to carry over.

**Restraint, stated up front, on four fronts.** This section does not re-decide who may admit a tenant: the Tenant-admission steward role already holds that authority, and spec §4 states it plainly — "Admission requires no criteria beyond the steward's own existing authority." That sentence is read here, never restated, and the act is never gated on anything further. It does not design schema, role, or grant creation, which is `04-scalability-availability-and-performance-design.md` §5.3's already-designed provisioning event, composed with below and never re-designed. It does not design credential issuance — password, invite, or federated sign-in — which remains `03-authentication-and-identity-design.md`'s in full, exactly as §6.1 and §11 already treat it for builder admission and exactly as spec §4's own closing sentence routes it. And it extends the role-and-permission matrix to no new role, grant, or scope: the only role this path can grant is the tenant-owner role the matrix already fixes, within the tenant boundary spec §3 already fixes.

**Same shape, different trigger.** The mechanism is §6.1's, unchanged in shape: **one INSERT into `tenant_<id>.tenant_users`**, populating the same already-fixed columns (`platform_user_id`, role, `scope_type`, `granted_at`, `granted_by` — `02-platform-data-model-design.md` §3.2), with no companion database-privilege statement, for the identical reason §6.1 gives — the row opens no new access path, and the newly admitted owner's access is realized entirely by the Registry Accessor and Context Resolution Point (§3, §4) reading that row on their own, separately-authenticated future request. Two column values are fixed by this path rather than supplied by its caller: `role` is always the tenant-owner role, and `scope_type` is always `tenant-wide` — the scope §6's own first table row already fixes for a tenant owner, reading `02-governance-and-security/03-access-control-and-tenancy-model.md` §6. `granted_by` resolves to the admitting steward's own `platform.platform_users` identity, an ordinary identity-anchor reference on the identical convention `platform.tenants.created_by` already uses for the registry row the same act writes (`02-platform-data-model-design.md` §3.1) — a value, never a schema change.

**The connection this write executes over is not §6.1's, and that is the first place the composition departs.** §6.1's INSERT executes over the connection the admitting tenant owner's own request already resolved and scoped to their tenant (§4.2, §5.1). No such connection exists here: a steward holds no connection-scoping grant to any `tenant_<id>` schema at all — §3.2 states plainly that holding a `platform.steward_role_bindings` row "grants the Registry Accessor's registry-wide read path (§3.1) and nothing else" — and the `tenant_<id>` schema this row belongs in was created moments earlier by the same act. The write therefore executes over **the same platform-core administrative connection class provisioning itself already requires** — the dedicated, out-of-band provisioning allocation `04-scalability-availability-and-performance-design.md` §5.3 already fixes, held separate from the shared per-replica pool ordinary tenant requests draw from — reused rather than a new one invented — the identical reuse §6.2's Registry Status Mutator already makes, for the identical structural reason.

**The preconditions, checked one at a time against §6.1's own three rather than assumed to carry over.** §6.1 holds each of spec §5's three preconditions to §5.1's standard — a structural guarantee or an honest predicate — and finds two structural and one checked. Running the same method here without assuming its result (the discipline §6.2's own precondition table established when the same method resolved differently) yields three verdicts that differ from §6.1's and two preconditions §6.1 has no counterpart for.

| Precondition | Structural or Checked | Mechanism |
|---|---|---|
| The identity of the person being admitted is established (spec §5; spec §4's "establishes the tenant owner's own first identity... in one act"). | **Applies — and is established *by* this act, not found already true. Structural in part, exactly as far as §6.1's own counterpart and no further.** | On §6.1's path the identity anchor pre-exists the admission, and the `platform_user_id` foreign key to `platform.platform_users` is a floor the INSERT fails against absent one. Here the anchor is the act's own first write: the admission act inserts one `platform.platform_users` row for the prospective tenant owner, carrying the identity attributes `02-platform-data-model-design.md` §3.1 already fixes as that table's content and nothing further — no credential, no enrolled factor, no session, each of which is `03-authentication-and-identity-design.md` §4.1's structure and mechanism, cited and not designed. Where an anchor for that person already exists — a person already holding a binding in another tenant, which `02-platform-data-model-design.md` §5 explicitly permits ("the identity anchor is global precisely so one human's platform credential is not duplicated per tenant") — the act reuses it and creates no second row. The same foreign key remains the floor: the binding INSERT fails at the database level if the anchor write did not succeed. **What this does not cover** is identical to §6.1's own honest limit — the foreign key proves an anchor exists, never that the identity is in good standing or able to authenticate against it; that remains `03-authentication-and-identity-design.md`'s, not redesigned here. |
| The membership is bound to exactly one tenant. | **Applies unchanged as an obligation; structural, on a different argument than §6.1's.** | §6.1 discharges this by connection-scoping — its write could not physically reach another tenant's schema. That argument is unavailable here, because the administrative connection above holds privilege across schemas by construction. Two facts replace it, neither a predicate a caller could omit: the `tenant_<id>` identifier this write targets is the one the same admission act itself just generated for the tenant it is admitting, never a value any caller supplies — the identical identity-in-never-a-caller-controlled-filter discipline §3.1's Registry Accessor and §6.2's Registry Status Mutator both already hold to; and `tenant_users` is a per-tenant-schema table (`02-platform-data-model-design.md` §3.2), so one binding row has no representation that could span two tenants even in principle (§8's fact 1). |
| The role admitted never exceeds what the admitting authority itself holds. | **Applies — and is discharged by construction here, where §6.1 needs an application-level check. The sharpest contrast with §6.1, and in the opposite direction from §6.2's.** | §6.1 states honestly that no schema-level constraint encodes the matrix's role hierarchy, so its Admission Authorization Check must compare the role requested against the admitting actor's own "May Grant Onward To" entry — necessary there because the tenant owner's entry covers *every* builder role below it and the caller chooses among them. The steward roles' own entry in that same matrix reads "Tenant owners, by admission" — exactly one role, and exactly this act. Because this path's write accepts no role and no scope parameter at all (the row below), there is nothing for a check to compare: the only grant it can issue is the single grant the admitting authority's own matrix entry names. The rule of spec §8 is satisfied by the absence of a choice, not by a predicate evaluated over one. |
| **New — no §6.1 counterpart.** The acting identity holds the Tenant-admission steward role specifically. | **Checked.** | §6.1 needs no counterpart because its own trigger *is* the matrix entry it checks. Here the trigger is a steward act, and three of the four steward roles do not carry it: spec §4 assigns "Admits and removes tenants at the platform boundary..." to Tenant admission alone, and Platform operations' own definition scopes that role instead to operating "the platform-provided primitives... themselves — distinct from operating any tenant's built software..." The **Owner Admission Authorization Check** reads the acting identity's own `platform.steward_role_bindings` entry off the Resolved Request Context assembled for their own authenticated request (§4.1) — the identical pattern §6.1's Admission Authorization Check, §6.2's Status Transition Authorization Check, and §7.1's bridge check already establish — and refuses where the actor holds another steward role, or none. An existing tenant owner cannot reach this path at all: their own onward-grant entry authorizes §6.1's admission within their own tenant, never the creation of another tenant's first owner. |
| **New — no §6.1 counterpart.** The role granted is always and only the tenant-owner role, at `tenant-wide` scope. | **Structural.** | The write path exposes no `role` and no `scope_type` parameter; both values are fixed in the mechanism itself, exactly as §3.1's Registry Accessor exposes no caller-supplied `tenant_id` to widen a result set and §6.2's Registry Status Mutator exposes no caller-supplied predicate. There is therefore no code path by which an admission act could grant a role other than tenant-owner, or narrow a first owner's scope to named applications — not a check that could be bypassed, but an input that does not exist. This is what discharges the precondition above it. |

**Ordering and atomicity, stated because the composition makes them a real question.** `tenant_users` lives inside the `tenant_<id>` schema, so `04-scalability-availability-and-performance-design.md` §5.3's own schema-creation DDL necessarily precedes this section's write within the admission act — the only constructible ordering, not a preference. The act's two identity-half writes — the `platform.platform_users` insert, where one is needed, and the `tenant_users` insert — are ordinary DML against already-existing tables and **commit in a single transaction**, so no state exists in which an identity anchor is created for a prospective owner without the binding that makes them one. Whether the provisioning DDL itself commits inside that same transaction is `04-scalability-availability-and-performance-design.md` §5.3's own concern and is not decided here. What this document does fix is the consequence where the identity half does not commit: the tenant row and its schemas exist, no `tenant_users` row does, and therefore **no actor holds tenant-owner authority in that tenant** — the Registry Accessor resolves no binding, the Context Resolution Point resolves no scope, and every action is refused, exactly as §6's own closing paragraph and spec §8 already fix for any actor without a grant. That is deny-by-default operating correctly, not a recovery state this document designs around; the repair is the same admission act, performed again.

**Grant verification is not a separate mechanism**, identically to §6.1: from the moment the row commits, the newly admitted tenant owner is an ordinary instance of the builder-persona access path §6's own table already governs, read through the same Registry Accessor and Context Resolution Point every other binding is read through. Nothing new is designed for it, and no mechanism in §3–§5, §7, or §8 changes.

**Evidence produced, checked against the standing index (`08-audit-and-traceability-design.md` §4.5), not the frozen table (§4.3) — and reused, not minted.** Applying the H26c test (is the fit direct or imperfect, stated honestly either way):

- **The successful act reuses `builder-admission`** (Authorization and grant events, §6.1) — a direct fit, not an imperfect one. That type's own definitional fact is a base `tenant_users` row's own creation, which is exactly what this act performs, at `outcome = applied`. The one difference is who acted: `actor_reference` resolves to the admitting steward and `actor_kind` to `human-steward` — or `autonomous-agent-process`, where the platform-operating agent holds that binding (§6's own fourth actor-class row) — where §6.1's resolves to a builder persona. Both are fields the base record already carries (`08-audit-and-traceability-design.md` §4.2), and `source_mechanism` distinguishes the emitting path — the Owner Admission Authorization Check here, §6.1's Admission Authorization Check there — the identical field along which `authority-refusal` is already extended by four documents without a second type being minted. `target_reference` resolves to the newly admitted tenant owner's `platform_user_id` and the created `tenant_user_id`, as §6.1 already fixes. **No new `event_type` is minted, and §4.5's index needs no new row**; the amendment that row does owe is named in §11, not made here.
- **A refusal of the new steward-standing precondition reuses `authority-refusal`** — the identical direct fit §6.1 and §6.2 each already establish for a refusal that exceeds what the actor's own resolved grant covers.
- **The other four preconditions have no failure mode to capture.** The identity-anchor precondition's own anchor is created by this act rather than found, so §6.1's missing-anchor `invariant-check`/INV-02 case does not arise on this path at all; a failure of the anchor write itself is an ordinary write failure, not an authorization refusal, and the single transaction above leaves no partial state for an event to describe. The single-tenant and fixed-role preconditions have no code path that could attempt the excluded thing, and the grants-never-exceed-the-grantor precondition is discharged by the fixed-role one — the identical "no naming path exists, so there is no refusal to describe" argument §6.1 applies to its own single-tenant precondition and §8 applies to cross-tenant access generally.
- **This section mints nothing for the registry rows the same act writes.** `platform.tenants` and `platform.applications` row creation is `04-scalability-availability-and-performance-design.md` §5.3's own write, outside this section's identity-and-binding half; §6.2's `registry-status-change` captures a *transition* of an existing row's `status`, never its creation, and nothing here extends it to one.

---

## 7. Bridging: Grant Issuance and Verification

`02-platform-data-model-design.md` §4.2 fixes two bridging mechanisms and the `application_bridges` record; it hands this document how a bridge's grant is actually issued and verified. Both mechanisms stay inside one tenant's own boundary — neither is available, or made available by this document, across tenants (§8).

### 7.1 API-Mediated Bridge

No schema-level change occurs. Application A's client identity calls application B's own published contract exactly as any other consumer would (`05-api-contract-design.md`'s contract, C-12).

- **Grant issuance.** When a tenant creates a `tenant_<id>.application_bridges` row naming applications A and B with `bridge_type = api-mediated`, this document's mechanism issues no new database privilege — the call is authorized through the same INV-02 decision point every request passes (§4.1), with one addition: **the authorization decision for a cross-application call within one tenant additionally checks that an active `application_bridges` row exists naming exactly this pair, before the call proceeds.** Absent that row, the call is refused identically to a call from an entirely unrelated, ungranted client — there is no special "same-tenant" exemption that bypasses this check.
- **Grant verification.** The check reads: (a) both applications' `tenant_id` values from the registry (§3.1) are equal — establishing the call is same-tenant, never cross-tenant; (b) an active `application_bridges` row exists naming this exact pair; (c) the row's `bridge_type` authorizes the specific action requested. All three conditions are evaluated before application B's handler is invoked, at the same authorization decision point INV-02 already fixes (`01-invariant-enforcement-design.md` §5.1) — this is the verification mechanism underneath that check, not a second check location.

### 7.2 Schema-Level Bridge

A direct database-level grant between two application schemas, available only because both already sit inside the same tenant's connection and credential scope (`02-platform-data-model-design.md` §4.2).

- **Grant issuance.** When a tenant creates a `tenant_<id>.application_bridges` row with `bridge_type = schema-level`, the platform-core service, in the **same database transaction** as the row's insertion, issues the corresponding `GRANT SELECT` (or the narrower grant the bridge's declared access scope requires) on the named tables or a view of application A's schema, to the database role of application B's schema (§5.1). Because both statements commit atomically, no state exists in which the bridge row is recorded without the grant, or the grant exists without the recorded row.
- **Grant verification.** Removing the `application_bridges` row is, symmetrically, a transaction that both deletes the row and issues the corresponding `REVOKE` in the same commit. No separate reconciliation step is needed, and none exists, because the row and the grant are never allowed to diverge — the database's own transactional guarantee is the verification mechanism, not a periodic audit that checks the two agree.
- **What makes this impossible across a tenant boundary.** The transaction that issues the `GRANT` is only ever invoked from the same code path that creates the `application_bridges` row, and that row's own foreign keys are constrained to two applications sharing one `tenant_id` (`02-platform-data-model-design.md` §4.1) — there is no code path by which this document's grant-issuance mechanism could be invoked with two applications of different tenants, because the record that triggers it cannot itself name that pair.

---

## 8. Cross-Tenant Access as Structurally Impossible

`02-governance-and-security/03-access-control-and-tenancy-model.md` §7 requires cross-tenant access to be framed as forbidden absolutely, and `02-platform-data-model-design.md` §4 requires the two bridging mechanisms to stay inside one tenant's boundary. This section states why a crossing is not merely disallowed by policy but has no structural path to occur, synthesizing three independent facts established above — **all three would have to fail simultaneously**, not just one, for a crossing to succeed:

1. **No naming path exists.** `02-platform-data-model-design.md` §4.1 already establishes that no table, view, or foreign key in the schema can name two different tenants together. Even an actor that somehow obtained a second tenant's schema name has no query construct through which to address it from this fact alone.
2. **No grant path exists.** Even given a name, §5.1's connection-scoping mechanism grants a connection privilege on exactly the schemas the Resolved Request Context (§4.1) resolves for the current actor and request — never on a schema belonging to a tenant the actor holds no binding in. §7.2 shows the one case where a cross-schema grant is created at all (the schema-level bridge) is itself incapable of naming two tenants, because the record that triggers it cannot.
3. **No silent path exists.** Even if a defect in §5.1's provisioning ever mis-scoped a connection despite (1) and (2), §5.2's mandatory, unconditional self-verification read would detect the mismatch against the Resolved Request Context and halt the request as an INV-01 violation before any cross-tenant data is returned — converting what would otherwise be a silent breach into a blocked, escalated, auditable event.

**No actor class of §6, and no bridging mechanism of §7, is exempt from all three.** The marketplace (`02-governance-and-security/03-access-control-and-tenancy-model.md` §7) is the sole mediated builder-to-builder interaction, and it is not a fourth path around these facts: a marketplace transaction exchanges a published artifact — an object one tenant obtains, not a grant that lets it reach into another tenant's own schema, connection, or registry visibility. Nothing in §3–§7 provides such a grant, and none is added here.

---

## 9. The Row-Level-Security Reconsideration

`02-platform-data-model-design.md` §12 explicitly authorizes this document to overturn the partitioning shape "on a scalability or enforcement finding" — distinct from `01-technology-stack-design.md` §14.5's exclusion of native row-level security, which was decided **on portability grounds only** ("MySQL/MariaDB has no native equivalent"). This section makes the isolation-strength assessment that exclusion never received.

**Finding: no reconsideration is warranted.** The mechanism designed in §3–§5 already achieves, structurally and portably, what RLS would add, and does so more strongly than RLS could for the case where RLS would actually apply:

- **RLS is a predicate-based mechanism that operates within one shared schema.** It has no application to the per-tenant and per-application schemas of `02-platform-data-model-design.md` §3.2–§3.3, because those are already separated at the schema level — a connection either has the privilege to address a schema or it does not (§5.1), which is a stronger, non-predicate guarantee than any row-filtering predicate could add on top of an already-separate namespace.
- **The one place RLS would genuinely apply — the shared `platform.tenants`/`platform.applications` tables — is exactly where §3's Registry Accessor mechanism already achieves RLS's row-filtering effect, portably.** A native RLS policy and the Registry Accessor's identity-scoped, deny-by-default query (§3.1) both return only the rows an actor's own binding covers; the Registry Accessor achieves this in ordinary application code, verifiable by direct code review and by a fixed adversarial test suite, on every candidate engine identically — where a native RLS policy's correctness would depend on an engine-specific feature (present differently in PostgreSQL and SQL Server, absent in MySQL/MariaDB) that this platform's portability obligation (`01-technology-stack-design.md` §2.5) requires it to avoid depending on regardless of isolation strength.
- **The self-verification read (§5.2) is a check RLS does not provide.** RLS enforces a filter; it does not itself prove, after the fact, that the filter was applied correctly for the connection in use. This document's mandatory read-and-compare against the schema's own self-description (or the connection's resolved schema name) is a distinct, additional guarantee neither the shape this document inherited nor RLS would supply on its own.

**Conclusion: the partitioning shape stands, and RLS's exclusion stands — now for an isolation-strength reason independently sufficient on its own, in addition to the portability reason `01-technology-stack-design.md` §14.5 already recorded.** Per the same discipline `01-invariant-enforcement-design.md` §7 applied when no genuine decision was owed: no ADR is recorded for *not* overturning the shape, because there is no decision here with a real alternative this document is choosing among — the mechanism of §3–§5 was the genuine decision, and it is recorded in full in §10.

---

## 10. Design Decision Records

Recorded inline, per the convention `01-technology-stack-design.md` §9 establishes. `01-invariant-enforcement-design.md` §7 recorded no ADR of its own and left ADR-018 as the next available identifier; this document consumes it. (ADR-017 is separately reserved for `05-mobile-application-delivery-design.md`, per `DECISIONS.md` D-25 — a different document's obligation, unaffected by this one.) ADR-061 (§10.2) is recorded later, at this document's second amendment, realizing the admission mechanism T79 specified; its identifier is verified next-free against `ADR-REGISTER.md` at the time of that amendment, which lists ADR-060 as the highest registered entry. ADR-062 (§10.3) is recorded at this document's third amendment, resolving the §5.3 collision `BACKLOG.md` §1u found; its identifier is verified next-free against `ADR-REGISTER.md` at the time of that amendment, which lists ADR-061 as the highest registered entry. ADR-063 (§10.4) is recorded at this document's fourth amendment, realizing the status-transition mechanism `DECISIONS.md` D-41 authorized; its identifier is verified next-free against `ADR-REGISTER.md` at the time of that amendment, which lists ADR-062 as the highest registered entry. ADR-065 (§10.5) is recorded at this document's fifth amendment, realizing the owner-admission mechanism T85 (`DECISIONS.md` D-60) specified and `BACKLOG.md` §2's Tenant provisioning entry scoped; its identifier is verified next-free against `ADR-REGISTER.md` at the time of that amendment, which lists ADR-064 as the highest registered entry.

### 10.1 ADR-018 — Tenant-Isolation Enforcement Mechanism: Connection-Scoped Structural Access, Registry-Scoped Identity Resolution, and Boundary Self-Verification

- **Status:** Team-Approved — decided under the `DECISIONS.md` D-16 delegation and recorded with its criteria; in force, no lead sign-off required (`01-technology-stack-design.md` §9; `DECISIONS.md` D-37, correcting this record's prior self-approved `Approved` label).
- **The question it answers:** Given schema-per-application partitioning nested under a per-tenant level (ADR-004) and the specification's requirement that isolation be verified at the boundary rather than assumed, what concrete mechanism makes a tenant-scoped actor's access to the shared registry return only its own row, makes a connection unable to address a schema outside its resolved scope, and proves that boundary at runtime rather than assuming it — and does an isolation-strength finding (distinct from ADR-004's portability finding) warrant reconsidering the exclusion of native row-level security?
- **Cost to reverse:** **Brutal** for the core mechanism — the connection-scoping and registry-accessor pattern is the enforcement layer every subsequent access to stored tenant and application data depends on; reversing it means re-securing every schema's access path, matching the grade `implementation-document-map.md` already assigns this document (`PROCESS.md` §12.1's most expensive rung). **Low** for the specific engine-level provisioning primitive (`SET ROLE` versus a contained-schema/contained-database equivalent) — swapping the provisioning mechanism for a different candidate engine changes tooling only, not the grant shape, the Registry Accessor's query discipline, or the self-verification read.
- **Upstream decisions assumed:** ADR-004 (`01-technology-stack-design.md` §14.7 — per-application schema partitioning, nested per-tenant, RLS excluded on portability grounds); `02-platform-data-model-design.md` §3–§4 (the schemas, registry, and bridge record this mechanism runs against); `03-architecture-realization-design.md` §4 (the import-boundary tool this document's one additional rule, §5.3, runs on) and §4.3, §7.1 (the runtime-resolution handover this ADR discharges); `01-invariant-enforcement-design.md` §5.1, §8 (the INV-01/INV-02 check locations this mechanism supplies the verification for).
- **Verified vs. reasoned:** Reasoned — the mechanism is derived directly from the cited specification's requirements (verified at the boundary, structural not predicate-based, portable across every candidate engine) and the structural properties of the datastore and schema already fixed; no time-sensitive ecosystem or tooling-maintenance claim is made.
- **Context:** Three upstream documents each explicitly handed this document a named, unresolved piece: `02-platform-data-model-design.md` §12 (registry scoping, bridge-grant issuance and verification, boundary proof); `03-architecture-realization-design.md` §4.3, §7.1 (the runtime portion of cross-tenant-instance prevention its own mechanism rates only Partial); `01-invariant-enforcement-design.md` §8 (the mechanism underneath INV-01/INV-02's already-fixed check locations, named there as this document's sharpest boundary). `02-platform-data-model-design.md` §12 additionally authorized reconsidering the partitioning shape on a scalability or enforcement finding, distinct from ADR-004's portability-only exclusion of RLS.
- **Decision:** (1) A Registry Accessor as the sole, identity-scoped read path to `platform.tenants`/`platform.applications` (§3); (2) a Context Resolution Point that resolves tenant/application context once per request into a frozen Resolved Request Context threaded through the request's entire execution via request-scoped storage (§4); (3) schema-scoped database-privilege grants, switched per request, as the structural access-control primitive — never a trusted predicate (§5.1); (4) a mandatory, unconditional self-verification read against each schema's own self-description (or the connection's resolved schema name) as the boundary proof (§5.2); (5) one additional import-boundary rule restricting registry-table access to the Isolation and Trust module (§5.3); (6) bridge-grant issuance and verification for both bridging mechanisms, atomic with the `application_bridges` record (§7); and (7) no reconsideration of the partitioning shape or the RLS exclusion — the mechanism above already exceeds RLS's isolation strength for the shared registry and is inapplicable in the already-separated per-tenant/per-application schemas (§9).
- **Alternatives considered:** *A trusted `tenant_id`/`application_id` predicate appended by the abstraction layer* — rejected; this is precisely the "assumed, not verified" failure mode `01-technology-stack-design.md` §14.5 already named, and this document's job was to design something stronger, not restate the rejected default. *Native row-level security on `platform.tenants`/`platform.applications` specifically* — rejected; unchanged on portability (absent in MySQL/MariaDB) and, newly assessed, not stronger than the Registry Accessor's identity-scoped, deny-by-default query, which is verifiable in application code and tests on every candidate engine identically rather than dependent on an engine-specific feature's correctness (§9). *Fully dynamic, per-request database credentials issued by an external secrets/KMS system, rotated per request* — rejected as disproportionate operational cost at the ~100-schema V1.0 figure (`DECISIONS.md` D-26); named as a candidate hardening for `04-scalability-availability-and-performance-design.md` to weigh if and when schema count approaches the NFR §5/§7 horizon `02-platform-data-model-design.md` §11 already sharpens, not a V1.0 requirement this document imposes now.
- **Consequences:** Binds every document that reads or writes tenant- or application-scoped data to route through the Resolved Request Context and the connection-scoping mechanism rather than inventing its own access path — named directly: `03-authentication-and-identity-design.md` (must supply the authenticated identity the Context Resolution Point consumes, and inherits INV-02's grant and session mechanics per `01-invariant-enforcement-design.md` §8); `02-data-model-and-entity-design.md` and `01-application-construction-design.md` (builder-defined entities and the construction surface are read and written only through a connection this mechanism has already scoped); `03-data-administration-design.md`, `07-cross-system-data-layer-design.md`, and `06-integration-and-extensibility-design.md` (each inherits this mechanism as the access path underneath its own derived or extension-facing surface); `04-scalability-availability-and-performance-design.md` (connection-pool design must accommodate one database role per schema at the ~100-schema V1.0 figure, and at whatever figure the NFR horizon eventually requires); `04-security-controls-design.md` (the Registry Accessor and connection-scoping mechanism are governed platform-core code, reviewed under the platform's own pipeline, not an untrusted surface). No downstream document may design a second tenant-scoping mechanism; each consumes this one.

### 10.2 ADR-061 — Builder Admission: Structural Single-Tenant Confinement and FK-Anchored Identity, One Application-Level Grants-Never-Exceed-the-Grantor Check, and a Bare Row Insertion as the Grant Itself

- **Status:** Team-Approved.
- **Cost to reverse:** **Moderate.** No new stored schema, table, or column is introduced — `tenant_<id>.tenant_users` already carries every column this mechanism writes (`02-platform-data-model-design.md` §3.2). Reversing the shape decided here (for example, requiring a companion database-privilege statement alongside the row, mirroring §7.2) would not touch already-admitted persons' access — the Registry Accessor and Context Resolution Point already read their `tenant_users` row identically regardless — but it would require retrofitting that companion statement for every already-admitted person's existing row, and updating the one write path every future admission goes through. A real but bounded migration, touching one mechanism and no builder-defined data, on the same grading `02-platform-data-model-design.md`'s ADR-037 applies to a comparably-scoped, per-tenant access-scoping decision.
- **Upstream decisions assumed:** `02-governance-and-security/03-access-control-and-tenancy-model.md` §5 (admission's three preconditions and the tenant owner's own "May Grant Onward To" authority, added by T79); `DECISIONS.md` D-36 (T79's authorization); `02-platform-data-model-design.md` §3.2 (the `tenant_users` table and its already-fixed columns, including the `platform_user_id` foreign key this decision's precondition-1 argument depends on); this document's own §3–§5 (the Registry Accessor, Context Resolution Point, and connection-scoping mechanism precondition 2 reuses rather than re-derives) and §7.1 (the authorization-check-at-the-INV-02-decision-point pattern the Admission Authorization Check follows); `03-authentication-and-identity-design.md` §5–§6 (the Resolved Request Context's `role` field the Admission Authorization Check reads, and the step-up gate that fires in front of the binding-table write); `08-audit-and-traceability-design.md` §4–§5 (the base event record, the `authority-refusal` and `invariant-check` types reused, and the category the new `builder-admission` type lands under). `08-audit-and-traceability-design.md`'s own ADR-025 remains Provisional — Pending Lead Approval; this decision depends only on its already-designed event model and categories, never on its approval status (`PROCESS.md` §12.2).
- **Verified vs. reasoned:** Reasoned throughout — every claim is either a structural property (foreign-key rejection behavior, connection-scoping reuse, single-statement atomicity) or a direct application of the cited specification and upstream design documents; no claim rests on a time-sensitive ecosystem, tooling-maintenance, or vendor-behavior fact (`PROCESS.md` §12.3).
- **Context:** T79 (spec-phase, `DECISIONS.md` D-36) fixed admission's three preconditions without naming any table, token, workflow, or state machine, explicitly leaving the mechanism to this document. `03-authentication-and-identity-design.md` §6.1, written after this document's own ADR-018, already names this document as the owner of "the write" (`tenant_<id>.tenant_users` insertion) its own step-up gate fires in front of — an unfulfilled handover until this amendment. §6's own table names `tenant_<id>.tenant_users` as "Where the Grant Lives" for every builder persona without ever stating how that row comes to exist.
- **Question this ADR answers, phrased for reuse:** Given a specification that fixes who may admit and three preconditions admission must clear before it takes effect, but names no table, token, or workflow, and given an already-fixed connection-scoping and request-resolution mechanism that governs every other write to a tenant schema, what concrete mechanism issues the `tenant_users` grant row itself — which of the three preconditions can be discharged by composing with that already-fixed mechanism, which reduce to an application-level check, and does the grant require a companion database-privilege statement the way a schema-level bridge (§7.2) does?
- **Criteria applied, and how each resolved:**

  | # | Criterion | How It Resolved |
  |---|---|---|
  | 1 | Can the membership-bound-to-exactly-one-tenant precondition be discharged by an already-fixed structural guarantee, or does it need a new check? | **Structural.** The write executes over the same connection §5.1 already scopes to exactly the admitting actor's own tenant schema; no code path lets that connection address a different `tenant_<id>` schema, so the row cannot be created anywhere but the resolved tenant — reusing §5.1's own argument, not adding a new one. |
  | 2 | Can the admitted person's identity-established precondition be discharged structurally? | **Partially.** The `tenant_users.platform_user_id` foreign key to `platform.platform_users` makes the INSERT itself fail at the database level absent an existing identity anchor — a structural floor. Whether that identity is additionally in good standing is `03-authentication-and-identity-design.md`'s own account-status concern, not re-decided here. |
  | 3 | Can the role-never-exceeds-the-admitting-authority precondition be discharged structurally? | **No — an application-level check.** No schema-level constraint encodes the matrix's role hierarchy; nothing about connection privilege or referential integrity prevents an over-scoped INSERT from being attempted. Stated honestly as a check, evaluated at the same INV-02 decision point every other authorization check in this document runs at. |
  | 4 | Does the grant need a companion database-privilege statement, the way the schema-level bridge (§7.2) does? | **No.** §7.2's `GRANT`/`REVOKE` exists because a bridge opens a *new* cross-schema access path a connection did not already have. Admission opens no new access path: the newly admitted person's access is realized entirely by the pre-existing Registry Accessor / Context Resolution Point mechanism reading the row on their own, separately-authenticated future request. A single INSERT is the entire mechanism; there is no second effect that could diverge from it, so §7.2's atomicity discipline has nothing to protect against here. |
- **Decision:** (1) A builder-persona grant is issued by a single INSERT into `tenant_<id>.tenant_users`, populating the already-fixed columns (`platform_user_id`, role, `scope_type`, `granted_at`, `granted_by`), executed over the connection the admitting actor's own request already resolved and scoped to their tenant (§4, §5.1) — never a second, specially-acquired connection. (2) Precondition 2 (single-tenant binding) is discharged structurally, by reuse of §5.1's connection-scoping; precondition 1 (identity established) is discharged partially structurally, by the already-fixed `platform_user_id` foreign key; precondition 3 (grants never exceed the grantor) is discharged by an application-level Admission Authorization Check run at the same INV-02 decision point §7.1 already establishes the pattern for. (3) No companion database-privilege statement accompanies the row, unlike §7.2's schema-level bridge — the row is the entire grant. (4) Evidence reuses `authority-refusal` for a precondition-3 failure and the generalized `invariant-check`/INV-02 shape for a precondition-1 failure; one new event type, `builder-admission`, is minted for the successful grant's own definitional fact, because no existing type captures a base `tenant_users` row's own creation — only its narrowings (`extension-registration`, `data-administration-grant-configuration`).
- **Alternatives considered:** *A companion database-privilege GRANT alongside the row, mirroring §7.2's schema-level bridge* — rejected under criterion 4; admission opens no new access path a grant statement would need to create. *A dedicated pre-check function that re-verifies single-tenant binding independently of connection-scoping* — rejected under criterion 1; it would duplicate a guarantee §5.1 already provides structurally, adding a checkable-and-therefore-bypassable step where none is needed. *Treating the admitted person's identity establishment as requiring a fresh, synchronous re-verification of their own current session* — rejected; the admitted person is not a party to the admitting request at all, so nothing about their own live session is available to check at that moment — the foreign key's referential guarantee is the correct and only available structural floor. *Reusing `authority-refusal` for every admission failure, including a missing identity anchor* — rejected under criteria 2 and 3's contrast; `authority-refusal`'s own established wording ("exceeding" a resolved grant) does not fit a missing identity anchor, which is an INV-02-shaped absence, not a grant-scope excess — collapsing the two would blur a distinction `08-audit-and-traceability-design.md` §4.3 already preserves deliberately for the analogous `basis-refusal`/`authority-refusal` case.
- **Consequences:** Binds no downstream document to a new access path — the newly admitted person is, from the moment of commit, an ordinary instance of the builder-persona row §6's own table already enforces; §3–§6 require no change. Mints one discriminated `event_type` (`builder-admission`) this document's own §6.1 defines; the corresponding row and count update to `08-audit-and-traceability-design.md`'s standing index (§4.5) are owed but not made by this amendment, flagged there (§6.1) as a follow-up, since that document was named to this ticket only as a Dependency Input, not as an editable target. Confirms `03-authentication-and-identity-design.md` §6.1's forward reference — "the write itself is `02-tenant-isolation-and-access-control-design.md`'s, but the step-up gate in front of it is this document's" — as now fulfilled on its first half. Registering ADR-061 in `ADR-REGISTER.md` remains an Orchestrator tracker-maintenance action at this ticket's close (`PROCESS.md` §3), not discharged by this document.

### 10.3 ADR-062 — Resolving the Region Resolution Point's Registry Read Against §5.3: A Named, Bounded Exception, and a Purpose-Accurate Restatement of the Rule Itself

- **Status:** Team-Approved.
- **Cost to reverse:** **Low** for the rule-statement itself — the exception is one named clause inside §5.3, one cross-reference at §3.1, one cross-reference in §13's binding rule, and two cross-references in `08-multi-region-distribution-design.md` (§6.2, §8); removing all five is a documentation-only change touching no stored schema, no stored data, and no runtime mechanism. **High** for what removing it would force in substance, stated separately because the two are not the same cost: revoking the exception without also revoking the mechanism it legitimizes would leave `08-multi-region-distribution-design.md`'s own already-shipped Region Resolution Point (ADR-045) in violation of §5.3 again, and correcting that would require relocating the read path into `components/isolation-and-trust` — the same component-boundary-change grade ADR-015 (`03-architecture-realization-design.md` §11.2) already assigns a codebase-topology move — since confining the read to a table that cannot bootstrap its own answer is not constructible at all (criterion 1, below). The rule-statement's own reversal is cheap; reversing the decision it records is not.
- **Upstream decisions assumed:** ADR-018 (§10.1 above — the Registry Accessor's fixed signature and the import-boundary rule this ADR restates, never re-derives); ADR-014 (`03-architecture-realization-design.md` §11.1 — the dependency-cruiser mechanism §5.3 runs on, and that document's own §3.1 module table, which forecloses locating region-specific logic inside Isolation and Trust); ADR-045 (`08-multi-region-distribution-design.md` §13.1 — the Region Resolution Point's own placement, read shape, and its finding that Registry Accessor reuse would weaken the accessor's guarantee rather than compose with it, none of which this ADR revisits); `02-platform-data-model-design.md` §3.1, §3.2 (`region_of_record` as a column of `platform.tenants`/`platform.applications`; `tenant_<id>.tenant_host_connections` as a distinct, per-tenant-schema table the `platform` schema does not include). `BACKLOG.md` §1u is the finding this ADR closes; `TICKET.md` H54 and decision D-b (Team, `DECISIONS.md` D-16 delegation) route the resolution here. ADR-014, ADR-018, and ADR-045 each remain Provisional — Pending Lead Approval; this decision depends on their already-designed content, never on their approval status (`PROCESS.md` §12.2).
- **Verified vs. reasoned:** Both, stated separately (`PROCESS.md` §12.3). **Verified:** that `region_of_record` is fixed as a column of `platform.tenants` and `platform.applications` (`02-platform-data-model-design.md` §3.1, read directly); that `tenant_<id>.tenant_host_connections` is a per-tenant-schema table distinct from the `platform` schema (§3.2 of the same document, read directly); and that `03-architecture-realization-design.md` §3.1's own module table forbids "any region-specific... branch" inside Isolation and Trust (read directly) — each a structural fact about the design as written, not a time-sensitive one, and each checked against the current file rather than asserted from a partial read (`BACKLOG.md` §1af(a)). **Reasoned:** the finding that confining the read to `tenant_host_connections` alone is not constructible, which follows from composing two already-fixed facts (`tenant_host_connections` is physically inside the very per-tenant schema whose region this read exists to resolve; no already-fixed structure maps a request's own address to a tenant without a registry read) rather than from any external or tooling claim.
- **Context:** `BACKLOG.md` §1u found that `08-multi-region-distribution-design.md` §6.2's Region Resolution Point reads `region_of_record` — a column §5.3 restricts to `components/isolation-and-trust` at "Full coverage" — from `components/distribution`, and that neither document mentions the other's rule (verified here by direct reading of both, not by grep alone). That document's own justification for not reusing the Registry Accessor was independently checked and found sound; the question this ADR answers is the import-boundary rule alone.
- **Question this ADR answers, phrased for reuse:** Given a Full-coverage import-boundary rule restricting two registry tables to one module, and a second, independently-designed mechanism elsewhere in the library that must read one column of those same tables before authentication, for a reason the restricting document's own accessor cannot serve — is the restricting rule violated as written, is the second mechanism mis-placed, or does the rule need a named exception; and, whichever holds, does the rule's own wording already say what it means?
- **Criteria applied, and how each resolved:**

  | # | Criterion | How It Resolved |
  |---|---|---|
  | 1 | Can the read be confined to `tenant_<id>.tenant_host_connections` alone — a table §5.3 does not cover? | **No — not constructible.** `tenant_host_connections` (`02-platform-data-model-design.md` §3.2) is physically inside the `tenant_<id>` schema, itself hosted in exactly one region (`08-multi-region-distribution-design.md` §3.2); reaching it therefore requires already knowing that tenant's region — the fact this read exists to resolve. No already-fixed structure maps a request's own address to a tenant without a registry read (`01-application-runtime-and-lifecycle-design.md` §3.3's "opaque reference" is resolved through the registry, never around it). This candidate is rejected on constructibility, not preference. |
  | 2 | Should the read instead be legitimized by moving it into `components/isolation-and-trust`, called by Distribution? | **No — foreclosed by an already-fixed rule, not by preference.** `03-architecture-realization-design.md` §3.1's own module table fixes, for Isolation and Trust, "What Never Lives Inside It": "any region-specific... branch," and fixes region topology and routing (C-14) as Distribution's own module content instead; relocating it would trade one boundary violation for another, inside a document this ticket does not reopen. |
  | 3 | Is the rule as originally worded ("import the platform-schema data-access module") violated in letter, in purpose, or both? | **Purpose, decisively; letter, ambiguously — and the ambiguity is itself the defect.** `region_of_record` is a column of `platform.tenants`/`platform.applications`, so a query for it is exactly what §5.3 exists to confine to Isolation and Trust — the purpose is violated regardless of implementation. Whether the *letter* was violated turned on whether the Region Resolution Point's own "second, deliberately narrower read path" (`08-multi-region-distribution-design.md` §6.2) imports the same named data-access module the Registry Accessor uses or an independently-written one — a fact neither document's text committed to. A rule whose violation depends on an implementation detail neither document states is not a rule a static tool can be configured against reliably; the defect is the rule's own formulation, not merely a missing exception. |
  | 4 | Given 1–3, what is the correct fix? | **Restate §5.3 in terms of the property it protects — no module outside Isolation and Trust may obtain a query-capable handle on either table, by any means — and name the one exception explicitly**, rather than leave a name-one-module rule a second, differently-named module could satisfy letter-only. This is the only candidate of the four that makes "Full coverage" true as written, not merely true in intent. |

- **Decision:** (1) §5.3 is restated from "may import the platform-schema data-access module" to a property-based rule — no module outside `components/isolation-and-trust` may obtain a handle capable of directly querying `platform.tenants` or `platform.applications`, by any means — so a future second, differently-named module cannot again satisfy the rule's letter while defeating its purpose. (2) One named, bounded exception is added: the Region Resolution Point (`08-multi-region-distribution-design.md` §6.1–§6.2), reading only `region_of_record`, only for the tenant its own request's address names, only before authentication, only from the regionally-resident read replica, never resolving role, permission, or grant content. (3) The "Full coverage" claim is restated as "Full coverage, less this one named exception" — an honest, narrower claim, per this document's own standing rule that a Full-coverage assertion a shipped design quietly violates is worse than one stated with its exception. (4) `08-multi-region-distribution-design.md` §6.2 and §8 each receive a cross-reference to this exception and to §5.3, closing the "neither document mentions the other" gap `BACKLOG.md` §1u found. (5) No mechanism in either document is redesigned: the Registry Accessor's signature, the region topology, the placement unit, and the routing sequence all stand exactly as ADR-018 and ADR-045 already fixed them.
- **Alternatives considered:** *Confining the read to `tenant_host_connections` alone* (criterion 1) — rejected as not constructible. *Relocating the read path into `components/isolation-and-trust`* (criterion 2) — rejected; foreclosed by `03-architecture-realization-design.md` §3.1's own module table, a document this ticket does not reopen. *Leaving §5.3 worded exactly as shipped, on the theory that the Region Resolution Point's own module happens not to import the named data-access module* — rejected; this is precisely the letter-intact-purpose-defeated outcome criterion 3 identifies, and would leave a Full-coverage claim this document's own standing discipline forbids leaving unstated. *Widening the exception to cover any pre-authentication read generally, rather than naming the Region Resolution Point specifically* — rejected; a named, single exception is checkable by the same static tool the rule itself runs on, while an open-ended pre-authentication carve-out would re-introduce exactly the unbounded, convenience-shaped exception `BACKLOG.md` §1u's own second candidate warned against.
- **Consequences:** `08-coding-standards-and-patterns-design.md`'s own dependency-cruiser configuration (extended from ADR-014 by that document's own §4) is bound to add this sixth rule and its one named exception as a concrete, enumerable source-path rule — not designed here, and not made here, since that document is a Dependency Input to this ticket, not an editable target; flagged forward as this ADR's own owed follow-up, on the identical discipline §6.1's `builder-admission` minting already applied to its own out-of-scope index amendment. `01-agent-runtime-and-control-design.md` §9.3 ("`BACKLOG.md` §1u — Checked, Not Resolved") now describes a closed collision as open and quotes §5.3's superseded wording; both are stale as of this amendment and are handed forward as a follow-on ticket, since that document is not named to this ticket's Document(s) field. `BACKLOG.md` §1u, `TICKET.md` H54's row, decision D-b, and the `ADR-REGISTER.md` entry for this ADR are Orchestrator tracker-maintenance actions at this ticket's close (`PROCESS.md` §3), not discharged by this document.

### 10.4 ADR-063 — Status Transition: A Platform-Global Write With No Structural Precondition, Two Authority Sources for `offboarding`, and One New Discriminated Event Type

- **Status:** Team-Approved.
- **Cost to reverse:** **Moderate.** No new stored schema, table, or column is introduced — `platform.tenants.status` and `platform.applications.status` already carry every value this mechanism writes (`02-platform-data-model-design.md` §3.1). Reversing the authority table or the precondition set decided here does not touch any already-transitioned row's own stored state — the Registry Accessor already reads `status` identically regardless of which mechanism wrote it — but it would require updating the one write path (the Registry Status Mutator) every future transition goes through, and re-verifying every historical transition's own authorization against the corrected rule where that matters for audit reconstruction. A real but bounded migration, touching one mechanism and no builder-defined data, on the same grading ADR-061 applies to a comparably-scoped mechanism decision.
- **Upstream decisions assumed:** `DECISIONS.md` D-41 (this ticket's own authorization, and its finding that the mechanism realizes an obligation already stated rather than a new capability); `DECISIONS.md` D-40 (the boundary this decision does not cross — no builder-facing deprecation capability); `02-platform-data-model-design.md` §3.1 (the `status` column and its now-explicit enum on both tables, §3.1's own amendment in this same ticket); `02-governance-and-security/03-access-control-and-tenancy-model.md` §4–§5 (the Tenant-admission steward role's own definition and the tenant owner's permission-scope row this decision's authority table reads); `01-business-and-ux/04-personas-and-roles.md` §3 (the tenant owner's "owns the tenant's account boundary" phrase); this document's own §3–§5 (the Registry Accessor's read-only discipline and the connection-scoping mechanism this decision's central finding contrasts against) and §4.1, §7.1 (the INV-02 decision-point pattern the Status Transition Authorization Check follows); `03-authentication-and-identity-design.md` §4.4 (the session-termination mechanism this decision's write commits alongside, without redesigning it); `07-data-governance-and-privacy-design.md` §4 and `02-platform-data-model-design.md` §10 (the data-disposition and never-hard-deleted mechanisms this decision's `offboarding` write triggers, without redesigning them); `08-audit-and-traceability-design.md` §4–§5 (the base event record, the `authority-refusal` type reused, and the category the new `registry-status-change` type lands under, per that document's own §13 authorization and this ticket's own second-target instruction). `08-audit-and-traceability-design.md`'s own ADR-025 remains Provisional — Pending Lead Approval; this decision depends only on its already-designed event model and categories, never on its approval status (`PROCESS.md` §12.2).
- **Verified vs. reasoned:** Reasoned throughout — every claim is either a structural property (no connection ever holding platform-schema privilege, per §5.1's own fixed grant shape) or a direct application of the cited specification and upstream design documents; no claim rests on a time-sensitive ecosystem, tooling-maintenance, or vendor-behavior fact (`PROCESS.md` §12.3).
- **Context:** D-41 (`BACKLOG.md` §1ag(b)) found no document owning the transition of either table's `status` column into `suspended` or `offboarding`, while `02-governance-and-security/04-auth-and-identity-spec.md` §5 already required a session to terminate the instant that transition occurs and `03-authentication-and-identity-design.md` §4.4 already fixed the transactional mechanism that makes it so — without either document naming what triggers it. D-41 routed the placement question to this ticket, weighing this document (owns the closest structural analog, admission, §6.1) against `02-platform-data-model-design.md` (owns the schema itself, and carries its own placement-reasoning precedent, ADR-042).
- **Question this ADR answers, phrased for reuse:** Given a specification obligation and a schema column with no document naming who may write to it or under what preconditions, and given that the write targets the platform-global registry rather than any structure this document's own connection-scoping mechanism ever grants a connection privilege on, what concrete mechanism performs the write, which authority governs each of the two target statuses, do any of admission's own structural guarantees carry over to a platform-global write, and does the transition need a new discriminated event type?
- **Criteria applied, and how each resolved:**

  | # | Criterion | How It Resolved |
  |---|---|---|
  | 1 | Which document should own this mechanism — this one, or `02-platform-data-model-design.md`? | **This document**, on the identical reasoning `TICKET.md` H58 already applied to admission's own placement: the schema-owning document fixes *where* a fact lives and explicitly, repeatedly, hands mechanism elsewhere (`02-platform-data-model-design.md` §12's own boundary list); this document is where the write-path, authority-check, and connection-scoping precedent already live, and `03-authentication-and-identity-design.md` §4.4 already attributes the structurally identical case (grant revocation) to "`02-tenant-isolation-and-access-control-design.md`'s binding-table mechanism" — the same attribution this decision now supplies for status transition. |
  | 2 | Do admission's structural guarantees (§6.1, connection-scoping confining a write to one tenant) carry over to a status-transition write? | **No — and this is the decision's central finding.** Admission's structural guarantee holds because `tenant_users` is a per-tenant schema table, reachable only through a connection already scoped to that tenant. `platform.tenants`/`platform.applications` are platform-global; §5.1 grants no connection any privilege there at all, and §3.2 states holding a steward role "grants the Registry Accessor's registry-wide read path (§3.1) and nothing else." Every precondition here is therefore checked, not structural — the honest conclusion §6.1's own methodology, applied without assuming its own result, requires. |
  | 3 | Is `offboarding` reachable by one authority source or two? | **Two, for the tenant case; one, for the application case — an asymmetry the citations themselves produce, not a symmetry imposed on them.** The Tenant-admission steward role's own definition names "removes tenants," supporting an administrative path; the tenant owner's "owns the tenant's account boundary" and "full authority over its tenant's... built software" supports a voluntary-exit path for the tenant itself and for an application within it. Only the tenant-level steward citation names tenants; extending it to an individual application would be an inference the cited text does not support, so the application-level path is tenant-owner only. |
  | 4 | Does the transition need a new discriminated `event_type`? | **Yes, one, not two.** No existing type captures a `status` transition on either table; the closest candidate, `publication-status-change`, is an imperfect fit on inspection (a different column, actor, and fact). One type, `registry-status-change`, covers both target statuses via `result`, on the identical one-type/two-directions precedent `publication-status-change` itself already sets. |

- **Decision:** (1) A status transition is issued by the **Registry Status Mutator**, a single `UPDATE` against the named row's `status` column over platform-core's own administrative connection — the same class of connection tenant/application provisioning already requires, reused rather than a new one invented, gated by two checks run at the same INV-02 decision point every other authorization check in this document runs at (§6.2). (2) `suspended` is reachable only by the Tenant-admission steward role; `offboarding` is reachable by the Tenant-admission steward role or the tenant owner for a tenant, and by the tenant owner alone for an application. (3) No precondition here reduces to a structural guarantee, unlike two of admission's three — the actor's-standing check and the tenant-owner scope-match check are both application-level, because the write targets a table no connection-scoping mechanism ever grants privilege on. (4) A third precondition, absent from admission's shape entirely, checks the target's current `status` against the transition requested, since `status` carries no schema-level ordering constraint and `offboarding` is terminal. (5) The write commits in the same transaction `03-authentication-and-identity-design.md` §4.4 already anticipates for session revocation, and triggers, without redesigning, the data-disposition mechanisms `07-data-governance-and-privacy-design.md` §4 and `02-platform-data-model-design.md` §10 already fix. (6) One new event type, `registry-status-change`, is minted under Authorization and grant events, `result` carrying the target status; an actor-standing or scope-match refusal reuses `authority-refusal`; a status-precondition refusal is captured by `registry-status-change` itself.
- **Alternatives considered:** *Placing this mechanism in `02-platform-data-model-design.md` instead* — rejected under criterion 1; that document's own repeated boundary statements hand mechanism to this one, and `03-authentication-and-identity-design.md` §4.4 already attributes the sibling case here. *Assuming the actor-standing and scope-match checks are structural, by analogy to admission* — rejected under criterion 2; the analogy does not hold once the target table's own connection-privilege shape is checked directly, and asserting structural coverage this mechanism does not have would repeat exactly the failure `BACKLOG.md` §1t and this document's own §5.3 correction (ADR-062) both warn against. *Granting the Tenant-admission steward role authority over an individual application's own `offboarding`, for symmetry with the tenant case* — rejected under criterion 3; the cited text supports tenants, not applications, and symmetry is not itself a citation. *A dedicated pre-check function re-verifying the actor's tenant binding independently of the Resolved Request Context* — rejected; it would duplicate a value §4.1 already resolves, adding a checkable-and-therefore-bypassable step where the existing object already carries the answer. *Reusing `authority-refusal` for every refusal, including a status-precondition failure* — rejected; that failure is not a grant-scope excess, and forcing the reuse would blur a distinction this document's own §6.1 precedent (precondition-1 vs. precondition-3) already establishes the discipline for keeping separate.
- **Consequences:** Binds no downstream document to a new access path — the Registry Status Mutator is the sole write path to either table's `status` column, and every existing reader (the Registry Accessor, §3.1) continues to read the result exactly as before. Mints one discriminated `event_type` (`registry-status-change`), with its `08-audit-and-traceability-design.md` §4.5 row and count update made directly in this same ticket, per that ticket's own second-target instruction — unlike ADR-061's `builder-admission`, whose index row remained flagged rather than made. Confirms `03-authentication-and-identity-design.md` §4.4's own "tenant, application, or account removal" row as now attributable to this document's mechanism, on the identical convention its "grant revocation" row already uses — that document's own citation update is not made here, since it is a Dependency Input to this ticket, not an editable target, and is flagged forward in this document's own §11. Registering ADR-063 in `ADR-REGISTER.md` remains an Orchestrator tracker-maintenance action at this ticket's close (`PROCESS.md` §3), not discharged by this document.

### 10.5 ADR-065 — Owner Admission: Composing With ADR-061's One-INSERT Shape Over Provisioning's Own Administrative Connection, a Fixed Single-Role Grant That Discharges Grants-Never-Exceed-the-Grantor Structurally, and the Identity Anchor as the Act's Own First Write

- **Status:** Team-Approved.
- **Cost to reverse:** **Moderate.** No new stored schema, table, or column is introduced — `platform.platform_users` and `tenant_<id>.tenant_users` already carry every column this mechanism writes (`02-platform-data-model-design.md` §3.1, §3.2) — and no new `event_type` is minted. Reversing the shape decided here (for example, letting the admitting steward name the first owner's role, or splitting the identity half out of the admission act into a separate later step) would not touch any already-admitted owner's own access — the Registry Accessor and Context Resolution Point read their binding row identically regardless of what wrote it — but it would require updating the one write path every future tenant admission goes through, and re-verifying that every already-admitted tenant holds exactly one owner row where a later split could have left one without. A real but bounded migration, touching one mechanism and no builder-defined data, on the same grading ADR-061 and ADR-063 each apply to a comparably-scoped mechanism decision.
- **Upstream decisions assumed:** `02-governance-and-security/03-access-control-and-tenancy-model.md` §4 (the Tenant-admission steward role, its "no criteria beyond the steward's own existing authority" rule, and the one-act obligation this decision realizes — added by T85) and §5 (the two matrix rows this decision reads: the steward roles' "Tenant owners, by admission" onward-grant entry, and the tenant-owner row's own always-tenant-wide scope); `DECISIONS.md` D-60 (T85's authorization); ADR-061 (§10.2 — the one-INSERT shape, the no-companion-privilege-statement finding, the grant-verification path, and the precondition methodology, all composed with rather than re-decided); ADR-063 (§10.4 — the finding that a write reaching outside every connection-scoped schema reuses platform-core's own administrative connection rather than a new one, and its precedent for re-running ADR-061's method instead of inheriting its result); `04-scalability-availability-and-performance-design.md` §5.3 (the provisioning event this mechanism composes with — the registry rows, both schema levels, the roles and grants, and the dedicated provisioning connection allocation); `02-platform-data-model-design.md` §3.1, §3.2, §5 (`platform.platform_users` as the global identity anchor, `tenant_users`' own fixed columns and its `platform_user_id` foreign key, and the one-anchor-many-tenants rule this decision's reuse case relies on); this document's own §3.2 (a steward role holds no connection-scoping grant — the fact that makes §6.1's connection reuse unavailable here), §4.1 and §7.1 (the INV-02 decision-point pattern the Owner Admission Authorization Check follows); `03-authentication-and-identity-design.md` §4.1 (the credential and enrollment structures this decision neither writes nor designs); `08-audit-and-traceability-design.md` §4.2, §4.5 (the base record whose existing `actor_kind`, `actor_reference`, and `source_mechanism` fields make `builder-admission` reusable, and the standing index this reuse was checked against). `08-audit-and-traceability-design.md`'s own ADR-025 remains Provisional — Pending Lead Approval; this decision depends only on its already-designed event model and categories, never on its approval status (`PROCESS.md` §12.2).
- **Verified vs. reasoned:** Reasoned throughout — every claim is either a structural property (foreign-key rejection behavior, the absence of a role or scope parameter on the write path, single-transaction commit of two DML statements, a steward's own absence of any schema grant under §5.1's fixed grant shape) or a direct application of the cited specification and upstream design documents; no claim rests on a time-sensitive ecosystem, tooling-maintenance, or vendor-behavior fact (`PROCESS.md` §12.3).
- **Context:** T85 (spec-phase, `DECISIONS.md` D-60) fixed that admission "establishes the tenant owner's own first identity and its top authority in one act" while naming no table, write, or connection. `BACKLOG.md` §2's Tenant provisioning entry found the resulting gap and scoped it precisely: §6.1 designs admission into an already-existing tenant and presupposes an existing tenant owner's onward grant as its trigger; `04-scalability-availability-and-performance-design.md` §5.3 designs provisioning's schema, role, and grant creation and names no identity or binding row; and §6.2's own observation that provisioning's write is "presumed platform-core, DDL-capable administrative code" fixes only the connection class. The first `tenant_users` row of every tenant the platform will ever admit had no designed mechanism anywhere in the library.
- **Why this is recorded as an ADR at all, checked rather than minted reflexively.** The contrary reading — that this merely re-applies ADR-061's already-approved shape to a second trigger and therefore warrants no record, on the same discipline §9 and `01-invariant-enforcement-design.md` §7 apply to a non-decision — was considered and rejected on two grounds. First, two of ADR-061's own findings do not carry over: its connection reuse is not constructible here (criterion 2), and its application-level grants-never-exceed-the-grantor check is not needed here (criterion 4). A decision that overturns two of a prior decision's findings is a change of substance, not a widening of its scope. Second, this decision genuinely chooses among alternatives with real trade-offs — where the first owner's identity anchor comes from (criterion 3), whether the first role is caller-supplied (criterion 4), and what an uncommitted identity half leaves behind (criterion 6) — which is exactly the test §9 applies when it declines to record a non-decision. ADR-063 is the controlling precedent: it too reused §6.1's one-write shape, it too ran over platform-core's administrative connection, and it was recorded precisely because its preconditions resolved differently. What is **not** recorded here is anything ADR-061 already decided — the one-INSERT shape, the absence of a companion privilege statement, and the grant-verification path are consumed unchanged, not re-decided.
- **Question this ADR answers, phrased for reuse:** Given an already-designed admission mechanism whose trigger is an existing role-holder's own onward grant, and a specification obligation that an authority sitting outside every tenant establish a brand-new tenant's very first role-holder in one act, what mechanism issues that first binding row — which of the existing mechanism's structural guarantees carry over, which resolve differently, which preconditions are new, where does the new role-holder's own identity record come from, and does the act need a new discriminated event type or does an existing one already describe the identical fact under a different actor?
- **Criteria applied, and how each resolved:**

  | # | Criterion | How It Resolved |
  |---|---|---|
  | 1 | Is this a new admission mechanism, or a composition with §6.1's? | **A composition.** One INSERT, the same fixed columns, no companion privilege statement, the same verification path — ADR-061's findings on all four are consumed unchanged; only the trigger, the connection, and the precondition set differ. A second, parallel admission mechanism was rejected: it would duplicate a write shape already decided and leave the library with two answers to "how does a `tenant_users` row come to exist." |
  | 2 | Can §6.1's connection reuse carry over? | **No — not constructible.** §3.2 fixes that a steward holds no connection-scoping grant to any tenant schema, and the target schema was created moments earlier by the same act, so no request-scoped connection to it exists to reuse. The write reuses provisioning's own dedicated administrative connection allocation (`04-scalability-availability-and-performance-design.md` §5.3), the identical reuse ADR-063 already made for the identical structural reason. Rejected on constructibility, not preference. |
  | 3 | Where does the first owner's own `platform.platform_users` record come from? | **A write of the admission act itself, committing in one transaction with the binding row.** Three candidates were weighed: *(a)* the act establishes it — chosen, and the only reading consistent with spec §4's "in one act"; *(b)* it pre-exists through a self-service registration step — rejected, because no such flow is modeled anywhere in the library (`DECISIONS.md` D-56, D-60; spec §4's own first bullet); *(c)* the binding row is written first against a placeholder and the anchor filled in later — rejected as not constructible, since the `platform_user_id` foreign key rejects the binding INSERT outright absent the anchor. |
  | 4 | Does grants-never-exceed-the-grantor need §6.1's application-level check here? | **No — discharged by construction.** The steward roles' own matrix entry names exactly one grantable role ("Tenant owners, by admission"), and the write path exposes no role or scope parameter, so no choice exists for a check to bound. Letting the admitting steward name the first role instead was rejected: it would introduce a caller-supplied authorization input where the matrix offers exactly one legitimate value — precisely the shape §3.1's Registry Accessor discipline exists to exclude. |
  | 5 | Does the act need a new `event_type`? | **No — `builder-admission` is reused, a direct fit.** The captured fact is identical (a base `tenant_users` row's own creation, `outcome = applied`); only the actor differs, and `actor_kind`, `actor_reference`, and `source_mechanism` already carry that difference (`08-audit-and-traceability-design.md` §4.2). Minting an `owner-admission` type was rejected: it would split one fact across two types on an actor distinction the base record already discriminates — the over-minting the standing index (§4.5) exists to prevent. |
  | 6 | What is designed for the case where the identity half does not commit? | **Nothing new.** A tenant with no binding row has no actor holding tenant-owner authority, and every action within it is refused by the deny-by-default posture §6 and spec §8 already fix; the repair is the same act, performed again. A compensating cleanup step, a provisional owner, and a partially-admitted tenant status were each rejected — the first two invent state, and the third would extend `platform.tenants.status`' own closed vocabulary (`02-platform-data-model-design.md` §3.1), which a document that does not own that schema may not do. |

- **Decision:** (1) A newly admitted tenant's first `tenant_users` row is issued by the same one-INSERT mechanism ADR-061 already fixed, populating the same already-fixed columns, with no companion database-privilege statement — a composition with §6.1, never a second mechanism. (2) The write executes over the platform-core administrative connection allocation provisioning itself already requires, not over a request-scoped connection, because a steward holds no schema grant on any tenant schema (§3.2). (3) The prospective owner's `platform.platform_users` anchor is established by the admission act as its own first write, reusing an existing anchor where the person already holds one, and commits in a single transaction with the binding row; credential issuance is not part of it. (4) Of spec §5's three preconditions, single-tenant binding remains structural but on a different argument (the tenant identifier is the act's own, never a caller's, and `tenant_users` is a per-tenant-schema table); identity-established remains structural in part, on the same foreign key, with the anchor created rather than found; and grants-never-exceed-the-grantor is discharged by construction rather than by §6.1's application-level check. (5) Two preconditions §6.1 has no counterpart for are added: the acting identity holds the Tenant-admission steward role specifically (checked, by the Owner Admission Authorization Check at the same INV-02 decision point), and the role granted is always and only tenant-owner at `tenant-wide` scope (structural — the write path exposes no such parameter). (6) The successful act reuses the existing `builder-admission` event type and a steward-standing refusal reuses `authority-refusal`; no new type is minted and no new row is owed to `08-audit-and-traceability-design.md` §4.5.
- **Alternatives considered:** *A second, parallel admission mechanism for the owner case* — rejected under criterion 1. *Reusing the admitting steward's own request connection for the write* — rejected under criterion 2 as not constructible. *A self-service or pre-existing identity record the admission act merely binds* — rejected under criterion 3; no registration flow is modeled in the library, and spec §4 fixes the identity as established by the act. *Letting the admitting steward name the first owner's role, or narrow its scope to named applications* — rejected under criterion 4, and separately foreclosed by the matrix, which fixes the tenant owner as always tenant-wide. *Minting an `owner-admission` event type for the steward-triggered case* — rejected under criterion 5. *Deferring the binding row to the new owner's own first successful sign-in* — rejected; it would make a tenant's own top authority contingent on a credential event `03-authentication-and-identity-design.md` owns, leave the tenant ownerless for an unbounded interval, and contradict spec §4's "in one act" obligation directly. *Extending §6.2's `registry-status-change` to cover a registry row's creation as well as its transition* — rejected; creation is `04-scalability-availability-and-performance-design.md` §5.3's own write and outside this mechanism's identity-and-binding half, and stretching a transition type over a creation fact would blur exactly the distinction ADR-063's own criterion 4 established.
- **Consequences:** Binds no downstream document to a new access path — from the moment the row commits, the newly admitted tenant owner is an ordinary instance of the builder-persona access path §6's own table already governs, and §3–§5, §7, and §8 require no change. Mints no `event_type`, so `08-audit-and-traceability-design.md` §4.5 needs no new row; what that index does owe is a note on its existing `builder-admission` row recording that the type now also covers this steward-triggered trigger, on the identical convention `authority-refusal`'s own row already uses for its four extensions — owed, not made here, since that document is a Dependency Input to this ticket rather than an editable target (§11). Owes `04-scalability-availability-and-performance-design.md` §5.3 a citation to §6.3 for the identity-and-binding half of the onboarding event its own paragraph enumerates without naming — likewise flagged in §11, not made here. Leaves one genuine gap in a document this ticket does not touch, named in §11 rather than absorbed: `03-authentication-and-identity-design.md` designs the credential and enrollment *structures* (§4.1) and the step-up gate in front of a credential change (§6.1), but no mechanism by which a brand-new identity — the first owner this act creates — obtains its first credential, which spec §4's own closing sentence assigns to that document. Registering ADR-065 in `ADR-REGISTER.md` remains an Orchestrator tracker-maintenance action at this ticket's close (`PROCESS.md` §3), not discharged by this document.

---

## 11. Boundaries and Handovers

Each boundary is stated from this document's side and is bidirectional in effect: neither side absorbs the other's concern.

- **Against `02-platform-data-model-design.md`.** Already written; read, not restated. This document's mechanism runs against the schemas, registry, and bridge record that document fixes, and discharges exactly the three items its §12 hands here (registry scoping, bridge-grant issuance and verification, boundary proof), plus the status-transition mechanism §6.2 now supplies for the `status` column and its enum that document's §3.1 already fixes (that document's own §3.1 enum for `platform.applications.status` was made explicit, matching the `platform.tenants` row, in this same ticket). This document does not alter that document's schema; where §5.2's self-verification read needs a self-describing structure, it uses `application_metadata` (§3.3, already fixed) and the connection's own schema-introspection call — it invents no new table, and §6.2's Registry Status Mutator writes an existing column's existing value, never a new one.
- **Against `03-architecture-realization-design.md`.** Already written; read, not restated. This document discharges the Partial-coverage runtime portion of §4.1 row 2 (§4 above) and adds one import-boundary rule (§5.3) run by the same tool that document's §4.2 already builds. It does not redesign the module boundaries, the dependency-direction table, or the deployment shape — the Resolved Request Context and connection-scoping mechanism operate inside the Isolation and Trust module that document already places.
- **Against `01-invariant-enforcement-design.md`.** Already written; read, not restated. That document fixes that INV-01 and INV-02 are checked, where each runs, and what a violation looks like; this document supplies the mechanism underneath — the Registry Accessor, the Context Resolution Point, connection-scoping, and the self-verification read. This document adds no second check location and does not alter the halt-and-escalate behavior or evidence emission that document already fixes for either invariant.
- **Against `03-authentication-and-identity-design.md`.** This document consumes an already-authenticated actor identity as the sole input to the Registry Accessor (§3.1) and the Context Resolution Point (§4.1); it does not design session lifecycle, credential mechanics, MFA, SSO, or federated identity — each is that document's, cited never restated. That document's output is a precondition this document's mechanism assumes holds before it ever runs. That document's own step-up gate (`03-authentication-and-identity-design.md` §6.1) fires immediately in front of the binding-table write §6.1 above designs, requiring a fresh verification of the admitting actor before the write proceeds; this document designs the write itself and the checks that gate it, never the step-up mechanism firing ahead of it. That document's own §4.4 already fixes that "removing, suspending, or disabling a tenant, application, or `platform.platform_users` account" revokes every affected `platform.sessions` row "in the same commit as the removal itself" — §6.2's Registry Status Mutator is that removal, and its write commits alongside that session-revocation write; this document does not design which sessions are affected or how they are identified, only that its own write is the trigger that document's mechanism already anticipates. That document's own credential and enrollment structures (§4.1) are what §6.3's owner-admission act deliberately does not write: the act establishes a `platform.platform_users` anchor and its binding only, and how the newly admitted owner first obtains a credential against that anchor — password, invite, or federated sign-in — is that document's, exactly as `02-governance-and-security/03-access-control-and-tenancy-model.md` §4's own closing sentence routes it. **Owed, not made here, on two counts.** (a) `03-authentication-and-identity-design.md` §4.4's "Tenant, application, or account removal" row names no owning document, unlike its "Grant revocation" row's explicit citation to this document's binding-table mechanism; the identical citation is now owed to that row, naming §6.2. (b) That document designs the credential and enrollment *structures* (§4.1), the verification methods that read them (§3), the step-up gate in front of a credential change (§6.1), and recovery for an identity that has already enrolled a channel (§7) — but no mechanism by which a brand-new identity holding none of them, which §6.3's act creates one of per admitted tenant, obtains its first credential. That is a genuine gap in a document this ticket does not touch, named here rather than absorbed into this document (which does not own credential mechanics) or left to be rediscovered. Neither (a) nor (b) is made here, since that document is a Dependency Input to this ticket, not an editable target.
- **Against `04-security-controls-design.md`.** The Registry Accessor and connection-scoping mechanism (§3, §5) are governed platform-core code inside the Isolation and Trust module, entering the codebase through the platform's own pipeline and review gates exactly as `03-architecture-realization-design.md` §5.2 already fixes for any change inside `components/*` or `guardrail/` — this document does not design the pipeline's secrets-scanning, input-validation, or certification mechanics.
- **Against `07-data-governance-and-privacy-design.md`.** Already written; read, not restated. An `offboarding` transition (§6.2) is the event that starts a tenant's or application's own data-disposition path; that document's Retention Sweep and deletion mechanism (§4) evaluate and act on the personal data such a tenant or application holds, against each element's own Retention Obligation. This document does not design retention, deletion, consent, minimization, or redaction — it supplies the triggering fact only. That document's own §2 already defers the storage shape of a platform-primitive identity's data-governance attributes to `02-platform-data-model-design.md` "for the platform's own registry and identity tables"; this document neither owns nor alters that storage shape, and reaches `platform.tenants`/`platform.applications` only through the `status` column §6.2 already writes for an unrelated reason.
- **Against `02-data-model-and-entity-design.md`, `01-application-construction-design.md`, `03-data-administration-design.md`, `07-cross-system-data-layer-design.md`, and `06-integration-and-extensibility-design.md`.** Each depends on this document directly or transitively (`implementation-document-map.md`). This document fixes the access path — the Resolved Request Context and the connection it scopes — that each of those documents' own construction, entity, administrative, or extension surface must be read and written through; it does not design any of their surfaces, entity models, or sandboxing mechanics.
- **Against `04-scalability-availability-and-performance-design.md`.** This document fixes one database role per schema as the connection-scoping unit (§5.1), a design point that document's connection-pool and migration-fan-out resolution (`02-platform-data-model-design.md` §11, §12) must account for at whatever schema count it is designed against. This document does not design pooling strategy, scaling triggers, or HA/failover topology. §6.3's owner-admission mechanism **composes with** that document's §5.3 onboarding event rather than restating or re-designing it: that section owns the registry rows, both schema levels, the database roles and grants, and the dedicated provisioning connection allocation §6.3's own write reuses; §6.3 owns only the identity anchor and the first `tenant_users` binding, and fixes the one ordering the composition requires (the `tenant_<id>` schema exists before its binding row is written). **Owed, not made here:** that section enumerates the onboarding event's writes without naming the identity-and-binding half, and a citation to §6.3 is owed there, but is not made since that document is a Dependency Input to this ticket, not an editable target.
- **Against `05-meta-operations/04-human-in-the-loop-protocol.md`.** §3.2 names one steward action — reaching into a tenant's schema for an approved, exceptional operational need — as requiring a distinct, explicitly-issued grant outside the standing steward visibility this document defines. This document states only that no steward role holds that grant by default; the issuance, approval, and routing mechanics for such a grant are that document's to design.
- **Against `08-audit-and-traceability-design.md`.** Already written; read, not restated. Admission (§6.1) and a status transition (§6.2) are each access-consequential actions per the specification and produce audit evidence through that document's own consolidated event model; this document designs the mechanism whose outcome that evidence describes, never the audit-event schema, its storage, or its tamper-evidence mechanism. Where admission's successful grant needed a discriminated `event_type` no existing type covered, this document mints `builder-admission`; where a status transition's own successful write needed one, this document mints `registry-status-change` — both under the Authorization and grant events category that document's own §5 already fixes. `builder-admission`'s corresponding index row (§4.5) was flagged rather than made at that amendment, since that ticket named this document only as a Dependency Input; `registry-status-change`'s row and count update **are made directly** in that document, in this same ticket, which names it as a second `Document(s)` target per that document's own §13 authorization and `PROCESS.md` §4's standing-registry rule. Owner admission (§6.3) mints nothing at all: it **reuses `builder-admission`** for the identical fact under a different actor, checked against §4.5's standing index and found a direct fit, so that section's own type count and row set are unchanged. **Owed, not made here:** that row's `Minted In` cell now understates the type's coverage, and a note recording that §6.3 extends its trigger to the Tenant-admission steward role's own admission act is owed there — on the identical convention `authority-refusal`'s own row already uses for its four extensions — but is not made, since a reuse gives this ticket no second-target authorization and that document is a Dependency Input here.
- **Against `08-multi-region-distribution-design.md`.** Already written; read, not restated. That document's Region Resolution Point (§6.1–§6.2 there, ADR-045) is the one named exception to §5.3's import-boundary rule (§10.3, ADR-062) — this document states the exception's bound; that document states the mechanism it bounds. This document does not design region topology, the placement unit, or the routing sequence; that document does not restate, and may not widen, the exception's scope beyond what §5.3 names.

---

## 12. Precedence and Ownership Boundaries

When an element of this document meets any other consideration, it is resolved by the fixed precedence of `02-governance-and-security/01-system-invariants.md` §6 and `01-business-and-ux/01-vision-and-charter.md`, which this document inherits rather than restates.

- **The specification prevails.** Nothing in this document narrows, expands, or alters the tenant boundary, the steward plane, the role-and-permission matrix, the client/application scoping rules, the forbidden-crossing rule, or the least-privilege default of `02-governance-and-security/03-access-control-and-tenancy-model.md` §3–§8; where a mechanism here appears to conflict with that document, the specification governs and this document is corrected, not the reverse.
- **INV-01 and INV-02 are floors, never spent.** Every mechanism in §3–§8 exists to make these invariants hold unconditionally; none is degraded to simplify a build, meet a deadline, or satisfy a request, and no bridging or grant mechanism (§7) narrows either invariant to make cross-application access easier.
- **The upstream design documents are realized here, not revisited.** The schema, registry, and bridge record of `02-platform-data-model-design.md`, the module boundaries and dependency-direction mechanism of `03-architecture-realization-design.md`, and the check locations of `01-invariant-enforcement-design.md` are inspected and extended by the mechanism above; none is redesigned or re-derived.
- **The partitioning shape and the RLS exclusion are affirmed here on an independent, isolation-strength basis, not merely inherited unexamined.** §9 records the assessment `02-platform-data-model-design.md` §12 explicitly authorized this document to make; the conclusion is no change, stated as a finding, not assumed as a default.
- **Bridging never widens beyond one tenant.** Both mechanisms of §7 are structurally incapable of naming two different tenants (§8); no future document may extend either mechanism to reach across a tenant boundary without that being a change to INV-01 itself, which no design document may make.

This document owns the registry-scoping mechanism (§3), runtime tenant-context resolution (§4), the connection-scoping and boundary self-verification mechanism underneath INV-01/INV-02 (§5), the least-privilege enforcement model across all five actor classes, the admission mechanism that issues a builder-persona's grant, the owner-admission mechanism that issues a newly admitted tenant's own first grant (§6.3), and the status-transition mechanism that moves `platform.tenants`/`platform.applications` into `suspended` or `offboarding` (§6), bridge-grant issuance and verification (§7), the cross-tenant-impossibility synthesis (§8), and the row-level-security reconsideration (§9). It does not own the schema, the module structure, the invariant-check locations, session and credential mechanics, session termination itself, data-disposition mechanics, or any other mechanism named in §11 — each is owned where `implementation-document-map.md` and the cited specifications already place it.

---

## 13. Binding Rules

These rules hold for every element of this document and are subordinate to the specification and the charter.

- **No component outside the Isolation and Trust module ever queries `platform.tenants` or `platform.applications` directly, except the Region Resolution Point's own narrowly-scoped, pre-authentication read of `region_of_record`** (§5.3, `08-multi-region-distribution-design.md` §6.2, ADR-062). Every other access passes through the Registry Accessor, whose query shape accepts only an already-authenticated actor identity — never a client-supplied identifier used to widen a result set (§3.1); this rule is enforced both by the Registry Accessor's own fixed query shape and by the import-boundary rule of §5.3, which names this one exception explicitly rather than leaving it undetected.
- **A steward role's registry-wide visibility confers no schema-connection grant to any tenant's or application's own data.** The steward governs the boundary without entering it (§3.2); any exceptional entry requires a distinct, explicitly-issued grant outside this document's standing mechanism.
- **Every request resolves its tenant/application context exactly once, at entry, into a frozen object bound to that request's entire execution.** No subsequent step may mutate it, and no downstream function accepts a raw tenant or application identifier as an alternative to it (§4).
- **Database access is scoped by schema-level privilege grants, switched per request, never by a trusted predicate.** No engine-native construct is used without an equivalent for every supported engine (`01-technology-stack-design.md` §14.5, §26), and no isolation guarantee in this document rests on an application-layer filter a caller could omit (§5.1).
- **Isolation is proven, not assumed, on every request that resolves a scoped connection.** The mandatory self-verification read of §5.2 runs unconditionally; a mismatch is an INV-01 violation that halts before any further query executes.
- **All five actor classes — builder, extender, end user, the autonomous platform-operating agent, and the external contract consumer — are bound by the same mechanism.** No actor class holds an exemption from connection-scoping, self-verification, or the deny-by-default posture of §6 by virtue of acting autonomously, externally, or on a grant rather than a persona-defined role.
- **A bridge's grant is issued and verified atomically with its record, for both bridging mechanisms, and neither mechanism can name two different tenants.** The API-mediated path checks the bridge record at the same INV-02 decision point every request passes (§7.1); the schema-level path issues and revokes its database grant in the same transaction as the record itself (§7.2).
- **A builder-persona grant is issued by one connection-scoped INSERT into `tenant_<id>.tenant_users`, never accompanied by a database-privilege statement of its own.** Admission takes effect only once single-tenant binding (structural, reusing §5.1's connection-scoping), the admitted identity's existence (structural, the `platform_user_id` foreign key), and the grants-never-exceed-the-grantor rule (checked, the Admission Authorization Check) are each independently established, and does not proceed where any cannot be (§6.1).
- **A newly admitted tenant's own first `tenant_users` row is issued by that same one-INSERT mechanism, triggered by the Tenant-admission steward role's own admission act rather than any tenant owner's onward grant, over the administrative connection provisioning itself already uses — never a request-scoped one, because a steward holds no schema grant (§3.2).** The prospective owner's `platform.platform_users` anchor is established by the same act, committing in one transaction with the binding row; the role granted is always and only tenant-owner at `tenant-wide` scope, a value the write path exposes no parameter for; grants-never-exceed-the-grantor is therefore discharged by construction rather than checked; and where the identity half does not commit, the tenant holds no owner and every action within it is refused, exactly as deny-by-default already requires (§6.3). Credential issuance for that first owner is not part of this mechanism and is not designed anywhere in this document (§11).
- **A `platform.tenants`/`platform.applications` status transition is issued by the Registry Status Mutator, a single `UPDATE` over platform-core's own administrative connection — no per-request connection ever holds privilege on the `platform` schema, so no precondition here is structural.** `suspended` is reachable only by the Tenant-admission steward role; `offboarding` is reachable by the Tenant-admission steward role or the tenant owner for a tenant, and by the tenant owner alone for an application. Every precondition — the actor's standing, a tenant owner's own tenant match, and the target's current status admitting the transition requested — is an application-level check, and the transition does not proceed where any cannot be established (§6.2). The write commits in the same transaction `03-authentication-and-identity-design.md` §4.4 already fixes for session revocation, and triggers, without redesigning, the data-disposition mechanisms `07-data-governance-and-privacy-design.md` §4 and `02-platform-data-model-design.md` §10 already fix.
- **Cross-tenant access requires all three of §8's structural facts to fail at once, not merely one.** No naming path, no grant path, and no silent, unverified path exist simultaneously for any actor class or bridging mechanism this document governs.
- **The partitioning shape and the RLS exclusion stand, affirmed on an independent isolation-strength basis (§9).** No ADR is recorded for this non-decision, exactly as the discipline `01-invariant-enforcement-design.md` §7 already set when no genuine alternative was being chosen among.
- **This document records five ADRs.** ADR-018 (§10.1) is the genuine, independent decision realizing the isolation-enforcement mechanism this document was originally commissioned to design; ADR-061 (§10.2) is the genuine, independent decision realizing the admission mechanism T79 (`DECISIONS.md` D-36) later specified; ADR-062 (§10.3) is the genuine, independent decision resolving the Region Resolution Point's registry read against §5.3 (`BACKLOG.md` §1u); ADR-063 (§10.4) is the genuine, independent decision realizing the status-transition mechanism `DECISIONS.md` D-41 authorized; ADR-065 (§10.5) is the genuine, independent decision realizing the owner-admission mechanism T85 (`DECISIONS.md` D-60) specified — recorded, rather than treated as a re-application of ADR-061's approved shape, because it overturns two of that decision's own findings and chooses among genuine alternatives, on the reasoning §10.5 states in full. Every other design choice above realizes a location, structure, or boundary the cited specification and the upstream design documents already fix.
